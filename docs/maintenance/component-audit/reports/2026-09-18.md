# 2026-09-18 组件审计

- **Round**:4(第四天,`roundRollover: false`,不执行 §5b 轮末横切;`remainingInRound: 18`)
- **Base main SHA**:`8dab72f096e876fd196e64c3b5ee6f1fefa9b830`
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>`,`openFindingReentry` / `priorityBacklog` 均为空(昨日 `foundation-inline-code` 的 open finding 已由 PR #132 / #134 落地守卫后 resolved,今晨 `audit:verify` 报 deferred 为空)
- **选簇原因**:seed 取与上批(`foundation-inline-code` / `foundation-annotation-popover` / `foundation-doc-annotation`)关系分最高的 `foundation-term-annotation`(直接依赖 popover,并共享 popover / inline-markdown 两个依赖);扩张取 `foundation-split-pane-handle`(plan 的 `why` 为空,即仅同命名空间关联分,按被依赖数 3 的并列规则胜出),再取其直接依赖者 `foundation-split-pane`,合计 535 LOC,本批 3 个组件
- **开放 PR 检查**:`gh pr list --state open` 为空,无需继续既有 audit PR
- **自上次 evidence 以来的变更**:`foundation-term-annotation` / `foundation-split-pane` 的 evidence `headSha` 为 `1a7afc5`(09-03,Round 3 同簇 modified),`foundation-split-pane-handle` 为 `e240db1`(09-17,`--record --affected` 复审)。`git diff <headSha>..HEAD` 对三项**既有** scope(owning files、composables、tests、references 与 gallery 页面)均**零变更**;本次新入 split-pane scope 的三个路径自 `1a7afc5` 起有变更(`endpoint-reference.vue` 36 行、`webhook-reference.vue` 126 行 / #136、`references/kits/api-docs/index.md` 30 行),已在本轮逐个核过(见 SplitPane「scope 变更」);上游 `AnnotationPopover` / `InlineMarkdown` / `utils/annotation` / `CodeRail` 自 09-03 起也无改动;`SidebarNav.vue` 在 09-04(`5353f73`,`isItemActive` / `itemScenarios` 下沉进 `resolved` 的渲染性能重构)改过 41 行,但它只在 handle 的 scope 内,且早于 handle 的 evidence `e240db1`(09-17),自该 evidence 起零变更;`package.json` 仅 `test:registry` 脚本加入 `type-scale-pairing` 测试。因此本次是对各项 evidence 快照的第二次从零全量复审,重心放在「上一轮只靠阅读判定的行为」实测核证与 scope 完整性
- **浏览器实测限制**:与 09-17 相同,无人值守会话无法启动 preview;运行时核证以 vitest 挂载(临时 probe spec,已删除、不提交)与 gate 产物完成。三项分发文件与已部署 gallery 内容一致,本次未做远端浏览器测量

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `foundation-term-annotation` | none | verified | clean;probe 实测三个继承成员 id 全部降级纯文本 + 告警,claim 成立 |
| `foundation-split-pane-handle` | none | verified | clean;scope 补入本次实际引用的 `sidebar-nav.md` / `focus-a11y.md`;一条观察(修饰键组合被消费)与 reka 原语同款,不立 finding |
| `foundation-split-pane` | none | verified | clean;scope 补入权威 reference、两个 gallery 消费点与 `utils/breakpoints.ts`;两条观察(同帧 move+up 终值、`direction` 运行时翻转)均无 reference 承诺,不立 finding |

**零代码改动,无功能提交**;evidence `headSha` 直接等于 base `8dab72f`(worktree 在 record 前与 main 完全一致,CI 在 synthetic merge 树上重算 evidence 时 scope 内容不变)。

## 各组件详情

### foundation-term-annotation(TermAnnotation,98 行)

历次:#39 晋升、Round 2 clean、08-16 诊断条件对齐、09-03 Round 3 修复原型链查找(modified)。本次四维度复审:

- **晋升 / 精简**:foundation 归属正确(概念形态,消费任意 glossary);`registryDependencies` 3 项(popover 壳 + labels 类型、inline-markdown 渲染定义、use-glossary 注入)与源码 auto-import 引用一致;`files[]` 1 个文件。labels 合并 idiom 是候选 3、`provideGlossary` / `useGlossary` 的 provide/inject 映射是候选 9,均已裁定不抽;`:labels="labels"` 把含 `category` / `learnMore` 的整份 labels 透传给壳,壳的 `t` 以 `{ ...defaults, ...props.labels }` 合并、多余键无消费者,无需在本组件做键排除。
- **优化(实测核证)**:临时 probe 挂载 `id="__proto__"` / `"hasOwnProperty"` / `"toString"`(glossary 未提供):三者均 `button=false`、渲染默认 slot 文本、`console.warn` 各 1 次——`Reflect.has` + `Object.hasOwn` 守卫对 `__proto__` 这类 getter 型继承成员同样成立,09-03 用例(`constructor` + reactive late-add)之外再无缺口,源码注释与 overlays.md「未命中时优雅降级为纯文本(stale markdown 永不破页)」的 universal claim 成立。触发器 / 面板的焦点、明暗、宽度由壳承担(09-17 已实测 reka 提供 dialog 命名与 trigger aria);`wrap-anywhere` 溢出由 `term-annotation.spec.ts` 断言。
- **重构**:`<script setup>` + TS;`TermAnnotationLabels extends AnnotationPopoverLabels` 从 SFC 导出;props(`id` / `entry` / `labels`)、默认 slot、`#actions` close-再-导航契约与 overlays.md L57–64 逐条一致(`entry` 已在文档,`f-084ace82f441` 维持 resolved,recorder 按原 id 保留)。
- **为什么没改看起来可改的**:`watchEffect` 告警在 `resolved` 为空且 `id` 不变时不会重复触发(依赖未变不重跑),无需像 ResponseExample 那样 `warnOnce`;`<slot v-else>{{ id }}</slot>` 在 `entry` 缺失且无 `id` 时渲染空——这是 caller 自己传空 entry 的情形,源码注释已定为「caller's own business」。

### foundation-split-pane-handle(SplitPaneHandle,174 行)

历次:08-11 补 spec + 修三处实现缺口(modified)、08-26 / 09-03 clean、09-17 因共享 reference 变化 affected 复审。本次全量复审:

- **晋升 / 精简 / 重构**:无。纯展示 + a11y、只发 intent 事件的边界与 references(`kits/api-docs/index.md` SplitPaneHandle 段、`sidebar-nav.md` L99 / L117)一致;三个消费方(SplitPane / CodeRail / SidebarNav)都以 3× 响应 `coarse`,「具体步长仍由父组件决定」的 claim 准确。
- **优化(复核无回归)**:`role="separator"` + `aria-orientation` + `aria-label` / `aria-controls` + `aria-valuenow/min/max` + `tabindex=0`,`disabled` 全部退为 `undefined`(无语义 spacer);grip 形态 `focus-visible:outline-2 outline-offset-2 outline-primary` 与 `focus-a11y.md` 实底规格一致,edge 形态 1px→2px primary 规则为 reference 明文的唯一焦点指示;`touch-none` 阻止拖拽时的浏览器手势;`transition` / `transition-colors` 均为 Tailwind 明确属性列表(非 `transition: all`),150ms 颜色 / 透明度过渡在 `main.css` 全局 reduced-motion 兜底之下;全部颜色走 `border-default` / `bg-accented` / `bg-primary` 语义 token,尺寸走 4px 刻度(`w-3` / `w-2` / `h-6 w-1` / `w-px` / `w-0.5`)。`split-pane-handle.spec.ts` 覆盖主轴方向键、正交键与 Tab 透传、Home / End / Enter、Shift coarse、disabled、pointer button 过滤。
- **观察(不立 finding)——修饰键组合**:probe 实测 `Alt` / `Meta` / `Ctrl` + `ArrowLeft` 都会 `emit('step', -1, false)` 且 `preventDefault`,即把手聚焦时浏览器的「后退」组合键(Win/Linux `Alt+←`、macOS Chrome `⌘+←`)被消费为 24px 微调。核证依据:① reka-ui 2.9.10 的 `SliderImpl` 与 `SplitterGroup` 键盘处理同样对方向键无条件 `preventDefault`、不检查修饰键,Nuxt UI 全部滑块 / 分栏原语行为同款;② 三份 reference 只承诺「方向键 / Home / End / Enter」与 Shift 语义,未承诺透传其它修饰键;③ 08-11 已裁定 RTL 方向语义不立案,修饰键守卫同属跨契约(牵动 `useSplitPane` 两个 kit 消费方成为 co-review owner)。没有被违反的条款,不立 finding;若产品希望放行浏览器导航组合键,属契约新增,建议先改 reference 再动把手。
- **scope 变更**:本节判定 edge 形态与键盘 / ARIA 消费契约引用了 `references/kits/api-docs/sidebar-nav.md` L99 / L117,grip 形态焦点规格引用了 `references/foundations/focus-a11y.md`,两份此前都不在 handle 的 scope 内(focus-a11y.md 已有 2 个 item 纳入,foundations 文档并非约定排除);按「scope 覆盖实际核过的 references」补入,使这两处契约变化能触发 handle 复审。

### foundation-split-pane(SplitPane,263 行)

历次:08-13 / 08-26 各修 2 处、09-03 修 getter default(modified)。本次全量复审:

- **晋升 / 精简**:无。263 行全部服务「空间分割 + 拖动」单一职责,内容优先重分配留在 kit `<CodeRail>`(ADR-010)复核成立;`registryDependencies` 6 项与 `useBreakpointGate` / `useSplitPane` / `useRafTask` / `SplitPaneHandle` / `breakpoints` 类型引用一致。
- **优化(复核无回归)**:SSR 恒 stacked → 挂载后按 `enabledFrom` 翻转;`useRafTask` 合帧写 `mainSize`(避免 RO 同步反馈环,注释与 kit reference L202「坑」一致);`maxClamp` 在未测量时回退硬上限、测量后减 `minOpposite + HANDLE_PX`;`ariaMax` 在 `Infinity` 时回退当前值、`End` 惰性;sticky 把手 `align-self: start` + `calc(100dvh - top - 2rem)`;`fixedPane="end"` 时拖拽 `invert` 与 nudge 方向翻转。`split-pane-handle.spec.ts` 的「SplitPane separator relationship」组 + 三个 composable spec 覆盖。
- **重构**:`<script setup>` + TS,所有 prop 为 `string | number | boolean` 字面量(MDC 友好契约),`useSplitPane` 收 getter default(09-03 修复在位)。
- **scope 变更(本次唯一实质改动)**:上一轮 scope 只含 `references/components/index.md` 与 gallery 的 `DocsShellReference.vue`,而 SplitPane 的**权威用法契约**在 `references/kits/api-docs/index.md` L126–154(prop 示例、MDC 用法、设计边界),该文件此前只在 handle 的 scope 内;两个正式 gallery 活骨架 `endpoint-reference.vue` / `webhook-reference.vue`(kit reference L119 / L223–224 明文以它们交付「可拖动分栏」pattern)也未在 scope;`enabledFrom` 的取值域由 `foundation/utils/breakpoints.ts` 的 `GeistBreakpoint` 派生(`SplitPane.vue` L5 直接 type import),全 ledger 无任何 item 纳入该文件,增删断点名会静默改掉公开 prop 契约而不触发 co-review。本次核过三处消费点(全部只传字面量 prop:`fixed-pane="end"` + `sticky` + `storage-key` + `default/min/max/minOpposite` + `label`;内外层 `enabled-from` 同步——endpoint / webhook 双 `lg`、DocsShellReference 双 `xl`)后把 reference、两个页面与 `breakpoints.ts` 一并纳入。代价:含 `references/kits/api-docs/index.md` 的 owner 从 15 个增为 16 个。
- **观察(不立 finding)——同帧 move + up**:probe 用 stub rAF 复现「pointermove(140)→ 帧 → pointermove(160)→ pointerup 同帧」:`stop()` 取消待执行的 rAF,终值 140 而非松手点 160。Chrome 会在派发 `pointerup` 前 flush 合并的 move,所以快速甩手时最后 ≤1 帧的位移确实会丢;但面板**没有跳变**——终值与松手点的误差 ≤ 一帧指针位移,用户看到的最后渲染帧就是被持久化的值。没有 reference 承诺「终值 == 松手点」,不立 finding;若将来要精确到松手点,修法是 `onEnd` 里先 `apply()` 再 `stop()`(4 行,落在 `useSplitPane.ts`,会把 CodeRail / SidebarNav 拉为 co-review owner)。**只能放 `onEnd`、不能挪进 `stop()`**:`onKey` 的 Escape 分支是先 `value.value = startVal` 再 `stop()`,在 `stop()` 内 apply 会把回滚覆盖掉。
- **观察(不立 finding)——`direction` 运行时翻转**:`mainSize` 只在 ResizeObserver 回调里按 `props.direction` 取 width / height,翻转后旧值会短暂留在另一轴,期间 `maxClamp` / `aria-valuemax` 取错轴。失效窗口**未实测**(本会话无浏览器,jsdom 判不了布局);理论上 `flex-row ↔ flex-col` 翻转会改变根盒(即被观察的 `containerRef`)自身高度并触发 RO,≤1 帧自纠。裁决依据不在机制而在契约:`references/components/index.md` L74 **只对 `enabledFrom`** 承诺「跟随运行时变化」,kit reference L143 的 MDC 契约要求全部 prop 为 `string | number | boolean` 字面量,三处消费点也都传字面量 `direction`——无承诺、无合法复现路径,与 `storageKey` / `mode` 同属「reference 未承诺可变」(09-03 裁定),不立 finding、不留 follow-up。
- **为什么没改其它看起来可改的**:`stackedStart` 防御性 class(08-26 裁定:根元素接受 fallthrough class,消费方可能把 stacked 容器变 flex)、`storageKey` / `mode` 挂载时读取(09-03 裁定:换存储槽与值域语义,reference 未承诺可变)、`2rem` sticky 底部留白(与 sticky 内容栏的呼吸间距约定一致,DocsShellReference 用同一算式)——理由未变,不动。

## 反向横扫(簇外重复检查)

基线:`8dab72f` 的 `extraction-candidates.md`(候选 1–9,无待决策)。对今日组件提取的特征模式全仓 `rg` 反查 `foundation/ kits/ app/components/`:

| 模式 | 命中 | 判定 |
|---|---|---|
| `role="separator"` + `aria-valuenow/min/max` + 主轴方向键 / Home / End / Enter | 仅 SplitPaneHandle | 全仓唯一 |
| `touch-none` 把手、`position: sticky` + `alignSelf` 视口高把手样式 | 各仅 1 处 | 全仓唯一 |
| `useSplitPane(` 调用 | SplitPane / CodeRail / SidebarNav | 对已落地共享实现的复用,非重复 |
| `useBreakpointGate(` 调用 | SplitPane / CodeRail | 同上 |
| `useResizeObserver` | SplitPane(容器主轴尺寸)、OperationTarget(横向溢出)、EnumTablePanel(纵向溢出) | 后两处即候选 7;SplitPane 量的是容器尺寸而非溢出,形状不同,不抽 |
| `useId()` 派生 per-instance storage key 回退 | SplitPane / CodeRail 各 1 行 | 低于抽取尺度 |
| `Reflect.has` + `Object.hasOwn` 自有属性查表 | 仅 TermAnnotation(FieldAnnotation 经 `entry?.field` 天然免疫,09-03 实测;Request / ResponseExample 的 `Object.hasOwn(vnode.props, 'scenario')` 是受控检测) | 语义不同,不抽 |
| `import.meta.dev \|\| import.meta.test` + `watchEffect` + `console.warn` | 6 处 / 5 组件 | 09-09 已裁定不抽,无新调用点 |
| labels 合并 `{ ...defaults, ...props.labels }` / provide-inject 映射 composable | 候选 3 / 候选 9 | 不重复评估 |

**登记表本次只追加 2026-09-18 扫描记录(无新候选);无待决策候选,不查询 / 不新建 GitHub issue。**

## Gate

在 base 树 `8dab72f`(零功能改动)上执行(本机负载 5–7,无超时):

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 58 files |
| `pnpm test:registry` | ✅ 143 pass / 0 fail |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ postbuild root CSS check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 isolated closure smoke + legacy-config 迁移 smoke 全部通过 |
| `pnpm test:component` | ✅ 33 files / 429 tests |
| `pnpm audit:verify`(record 前) | ✅ 29 个 item evidence 全部成立(base 树未改) |
| `pnpm audit:verify`(record 后) | ✅ 29 项 landed;legacy 0、deferred 0、未审计 0 |
| 分发物 U+FFFD 检查 | ✅ 无 |

临时 probe spec(7 例:修饰键 ×3、同帧 move+up ×1、继承成员 id ×3)全部通过,输出已引用于上文,文件已删除、不提交。

## 遗留与下一步

- 本批无 deferred finding、无人工决策点。
- 三条观察(把手修饰键组合、`useSplitPane` 同帧 move+up 终值、SplitPane `direction` 运行时翻转)已写入 ledger notes,分别为「与 reka 基座一致」「误差 ≤ 一帧指针位移、无跳变」「reference 未承诺可变」,不建议改;若产品侧另有要求,先改 reference。
- 页面级本地化欠账(非组件 finding):`app/pages/kits/api-docs/endpoint-reference.vue` L536 的 SplitPane `label="Resize documentation and code panels"` 与该页 FieldGroup label 仍是英文,而 webhook 参考页(PR #136)的 SplitPane / FieldGroup label 与 DocsShellReference 的 SplitPane label 已中文直写(DocsShellReference L134 / L138 / L237 的 FieldGroup label 同样仍是英文,一并计入);本次该页进了 SplitPane scope,建议与 09-17 的 webhook i18n 条目一并由人工择机处理。
- 09-11 记录的「SidebarNav 分区 header 按钮未显式设 `touch-manipulation`」线索仍待该簇排期时核证;`api-docs-field-item` / `api-docs-code-block` / `api-docs-enum-table` 的 scope 尚未含 `webhook-reference.vue`(09-17 记录),下次审计这三个 owner 时纳入。
- Round 4 剩余 18 个组件按依赖簇继续。

## `?` 常态色升档：受影响 owner 复审

功能提交为 `a8c60fa`（`fix(api-docs): 可省略记号 ? 常态色升为 text-toned，满足 WCAG 1.4.3`，基于 `8ea0b31`（rebase 于 #138 之后）），本节随 `--record --affected` 的 ledger 更新作为第二个提交。不推进每日排程 round。

- 起因：PR #137 复审提出的存量问题——明色主题下可省略记号 `?` 用 `text-dimmed`，对白底约 2.4:1，低于 WCAG 1.4.3 的 4.5:1；`?` 承载「键可缺失」这个事实，不是装饰。用户 2026-09-18 决定升一档。
- 改动：FieldItem 字段行与 FieldAnnotation 预览里的同一记号都由 `text-dimmed` 升为 `text-toned`；悬停不再改文字色，只保留 `bg-elevated`。reference 的 presence 配色条同步写明依据。
- 实测（真实浏览器读 computed color）：明色 `#666666` 对 `#ffffff` 5.74:1，暗色 `#a1a1a1` 对 `#0a0a0a` 7.66:1；字段名 17.9:1、type span `text-muted`，记号仍明显弱于字段名，层级不变。
- 验证：typecheck、`field-presence.spec.ts` 26 例通过（改钉 `text-toned` 且断言不再是 `text-dimmed`）。
- recorder 推导 16 个 owner（#138 把共享 reference 纳入 `foundation-split-pane` 的 scope，故比 rebase 前多 1 个）：`api-docs-field-item`、`api-docs-field-annotation` 为真实改动；其余 14 个仅因 scope 命中共享 reference 而复审，代码与契约未变。open finding 与既有 dependency evidence 原样保留。
- 复审补钉：FieldAnnotation 预览里的 `?` 同样断言 `text-toned` 且不再是 `text-dimmed`，与字段行的正反双钉一致（26 例通过）。
- 对上文「遗留与下一步」的说明：`foundation-split-pane-handle` 与 `foundation-split-pane` 的 Round 4 观察随本次 `--affected` 重录被复审结论替换（recorder 的 notes 是一句话结论，逐次覆盖），观察正文仍见本报告「各组件详情」；`foundation-term-annotation` 不在本次集合，notes 未动。
