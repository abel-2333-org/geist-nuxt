# 2026-09-20 组件审计

- **Round**:4(`roundRollover: false`,不执行 §5b 轮末横切;`remainingInRound: 12`)
- **Base main SHA**:`d126cfe1f6a0551c42738c7fb1d13582de2abc2a`
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>`,`openFindingReentry` / `priorityBacklog` 均为空
- **选簇原因**:seed 取与上批(`api-docs-sidebar-nav` / `api-docs-code-rail` / `api-docs-method-badge` / `foundation-split-pane-handle`)关系分最高的 `api-docs-site-search`(直接依赖 method-badge);扩张取共享 method-badge 依赖的 `api-docs-operation-header`,再取其直接依赖 `api-docs-lifecycle-badge`,plan 计 561 LOC(脚本按文件行数计,实测源码 557 行),本批 3 个组件
- **开放 PR 检查**:`gh pr list --state open` 只有 #148(Codex 的 #144 中性文字对比度 Draft,分支 `abel/144-neutral-contrast`),不是 audit PR,不继续、不触碰;其对 `text-muted` 等 token 的改动在该 PR 内闭环,本次不重复评估 SiteSearch 触发按钮 `text-muted` 的对比度
- **自上次 evidence 以来的变更**:三项 scope 自各自 evidence `headSha`(site-search `a8c60fa` / operation-header `a3a93e0` / lifecycle-badge `763e782`,均为 09-18 的 affected 复审)起 `git diff --stat` **零变更**(site-search 本轮新纳入 scope 的 `docs-shell-data.ts` 在此窗口内有 1 行 i18n 改动:#146 的 `Rule` → `规则`,已核);owning file 上一次实质改动分别是 09-05(#115,SiteSearch 空态 padding / 诊断条件;OperationHeader 标识换行)与 08-01(#59,lifecycle preset 契约表)。因此本次是对三项在 Round 4 的第一次从零全量复审
- **浏览器实测限制**:无人值守会话无法启动 preview;运行时核证以 vitest 挂载、`@nuxt/ui` 4.9.0 源码(`CommandPalette.vue` / `Modal.vue` / `utils/search.js`)与 gate 产物完成

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `api-docs-site-search` | modified | verified | 触发 `UButton` 补 `touch-manipulation`(kit tap 目标触控契约,09-11 / 09-19 同类先例),spec 新增独立用例;其余四维度无 finding |
| `api-docs-operation-header` | none | verified | clean;09-05 三条裁定复核成立,gallery 手写 h1 的 `truncate` 遗留仍交人工 |
| `api-docs-lifecycle-badge` | none | verified | clean;六态 preset、空白回落、size 对齐约定与三处 dependent 单值消费复核成立 |
| `api-docs-method-badge` | modified(co-review) | verified | scope 内 `SiteSearch.vue` 被改,method badge 调用链路零变化 |

功能提交两个:`3f1540e`(`fix(api-docs): SiteSearch 触发按钮纳入 touch-manipulation 触控契约`)与复审后的 `014c1c8`(收窄注释中「顶栏唯一常显 tap 目标」的错误断言,见「复审循环」);ledger evidence 绑定 `014c1c8`;报告、ledger 与候选登记表为其后的簿记提交。

## 各组件详情

### api-docs-site-search(SiteSearch,351 行)

历次:#14 引入、08-12 / 08-13 / 08-25 计划项、#88 持久状态播报、09-05 Round 3 modified。本次四维度复审:

- **晋升 / 精简 / 重构**:无。ADR-009 裁定留在 API Docs kit 复核仍成立;registry `packageDependencies` 只有 `fuse.js`、`registryDependencies` 只有 `api-docs-method-badge`,与源码 import / 模板一致。`SiteSearchItem` / `SiteSearchGroup` 从 SFC 导出;`SiteSearchPaletteItem` 未导出但 `extraGroups` 的 `CommandPaletteGroup<…>` 在结构类型下可由消费方用 `@nuxt/ui` 的 `CommandPaletteGroup` 直接赋值(额外键全部可选,`postFilter` 参数逆变 / 返回协变均成立),不构成契约缺口。
- **优化(本轮落地)——触发按钮缺 `touch-manipulation`**。`SidebarNav.vue` L506 源码注释把 `touch-manipulation` 定义为「kit 的 tap 目标触控契约(FieldItem / SchemaComposition / OperationTarget / `+N` 触发器)」,09-11(SchemaComposition)与 09-19(SidebarNav 分区 header、搜索清除 `UButton`、行链接)两次把缺口按 finding 修复并补 `classes()` 断言。**复现输入**:触屏设备打开任一 gallery 页,SiteSearch 触发按钮是顶栏在触屏上唯一常显的 tap 目标,`max-sm` 下只剩 icon;`@nuxt/ui` 4.9.0 `button.ts` 的 base 不含 `touch-action`,所以快速「点开 → Esc → 再点」在仍启用 double-tap-zoom 启发式的浏览器上可能被判为双击缩放而不是重开。**修法**:`class` 加 `touch-manipulation`(`UButton` 把 class 透传到根 `<button>`,09-19 已核),补 4 行注释说明契约来源(复审后收窄:触发按钮不是顶栏唯一 tap 目标,DocsShell 的汉堡 `UButton` / 域切换器同在顶栏);`site-search.spec.ts` 新增 `SiteSearch touch contract` 独立用例断言触发按钮 `classes()`。severity low。**依据**:Web Interface Guidelines「Touch & Interaction:`touch-action: manipulation`」;kit 既有契约(SidebarNav 源码注释)与 09-11 / 09-19 先例。props / emits / slots / 行为契约均未变,非破坏性。
- **复核无回归的既有契约**(逐条对 `@nuxt/ui` 4.9.0 源码核证):`UModal` 在 `#content` 模式下经 `VisuallyHidden > DialogTitle` 用 `title` 命名 dialog(`Modal.vue` L81–87);`UCommandPalette` 对 `ignoreFilter` 组在 `preserveGroupOrder` 下直接取 `group.items`,`processGroupItems` 对每组统一 `slice(0, resultLimit)`——空查询时每组最多显示 `resultLimit` 项是上游默认行为(未声明 `ignoreFilter` 的组走 `matchAllWhenSearchEmpty` 后同样被切片),reference 只承诺「始终可用 / 保持排序」,09-05 已裁定不为此加分支;`utils/search.js` 的 `highlight()` 只消费 `item.matches` 的 indices,`fuse.fuseOptions.useTokenSearch` 仅决定最短高亮长度,SiteSearch 自己的 Fuse 实例(`threshold 0.1` + `ignoreLocation`,非 token 模式)产出的 `matches` 与高亮一致。搜索 input `aria-label` = `modalTitle` + `autocomplete="off"`、持久 `role="status"` 三态播报、hash 焦点交接单次解码等由 `site-search.spec.ts` 14 用例(13 既有 + 1 新增)锁定。
- **gallery 消费面**:`DocsShell.vue`(`toSiteSearchGroups` + `searchDocsBody` 异步源,`resolveTo` 归一化 `to`)与 `sidebar-nav.vue` 两处实例全部中文 chrome 文案 + `noResultsAnnouncement` 齐备(09-05 修正无回归)。`docs-shell-data.ts` 的 adapter 只把侧栏 groups 映射为 `label / to / method / scenarios / icon` 显示模型、`searchDocsBody` 返回 `label / to / suffix / icon`——正是 reference「只认 display model,不绑定 `@nuxt/content`」的消费示范,本次纳入 scope。
- **Universal claims 核证**:spec「never announces a matching query while replacing a previous empty state」由 MutationObserver 用例锁定;源码「`router.resolve()` already normalizes … decoding again corrupts opaque ids」由 `res_state%25` 用例锁定;reference「静态导航 groups 始终可用」「有任何可用 option、查询为空或面板关闭时保持静默」与实现一致。
- **为什么没改看起来可改的**:
  - 每次键入 `new Fuse(items)` 重建索引——08-25 / 09-05 已复核 docs 索引量级下 < 1ms,缓存实例要引入 `setCollection` 生命周期,不动。
  - `text-muted` 触发按钮文案的对比度——由 #144 / #148 在 token 层统一处理(候选 A 让 `text-muted` 在四种中性背景 ≥ 4.5),本组件不做局部换 token。
  - 空查询时每组被 `resultLimit` 切片——上游默认行为,reference 未承诺不限量,消费方可调 `resultLimit`。
  - `LifecycleStatus` 之类闭合词表在 JS 消费方传非法值时 `meta` 为 `undefined` 会让子组件渲染报错并被 Vue 降级为注释节点——见 lifecycle-badge 段,同理不动。

### api-docs-operation-header(OperationHeader,114 行)

历次:08-05 / 08-19 计划项、#100 保护操作标识、09-05 Round 3 modified(标识换行 + 基线对齐)。本次全量复审:

- **晋升 / 精简 / 重构**:无。「单组件双形态」「与 OperationTarget / LifecycleNotice 正交」「`headingLevel` 上限 2」三条裁定(index.md「Operation identity 分层」)复核成立;`kind` discriminated union 用 `never` 封死反向字段;三条 `registryDependencies`(method / event / lifecycle badge)逐一有真实模板消费。`EndpointIdentity` / `WebhookIdentity` 类型未导出——消费方按 `kind` 字面量传 props,不需要命名该类型,不构成缺口。
- **优化**:无回归。identity 行 `items-baseline` + `<code translate="no" class="min-w-0 wrap-anywhere …">`(09-05 实测 375px 两行完整可读);`#actions` 容器 `ml-auto shrink-0 self-center`;标题 `text-balance`、描述 `max-w-prose text-pretty`;`$slots.actions` / `$slots.description` 条件容器不留空壳;`<header>` 在 `main` / `section` / `article` 内不会误升为 banner landmark;配色全走语义 token;无 U+FFFD。
- **Universal claims 核证**:注释「lifecycle badge carries icon + text, never color alone」——`lifecyclePreset` 六态全有 icon、`label` 空白回落 preset 文案,结构性成立;「an endpoint MUST carry method + path, a webhook MUST carry event」——由 `never` 联合在类型层封死,`operation-header.spec.ts` 两形态 + `chrome-labels.spec.ts` 的 `lifecycle` / `lifecycleLabel` 用例覆盖。
- **为什么没改看起来可改的**:
  - gallery 四页手写的 h1 identity header 9 处(`endpoint-reference.vue` ×3、`webhook-reference.vue` ×3、`DocsShellReference.vue` ×3)仍用 `min-w-0 truncate`——09-05 已记为「页面私有 recipe,交人工顺手对齐」,至今未动;它们分别落在 4–9 个 item 的 scope 内,为一处 class 触发十余个无关 item 的强制联审仍不成比例,继续列在「遗留」。
  - `headingLevel` 不开放 1——参考页注释明言「standalone 单 operation 页手写 h1、外壳多 operation 用 OperationHeader」是刻意分层,08-05 已裁定。

### api-docs-lifecycle-badge(LifecycleBadge 36 行 + lifecycle-preset 60 行)

历次:08-01 首审 modified(preset 契约表 + 空白回落)、08-19 计划项、08-29 / 09-18 affected 复审。本次全量复审:

- **晋升 / 精简 / 重构**:无。域词汇 → tone 映射留在 kit preset 层、foundation `SemanticBadge` 保持 vocabulary-agnostic(index.md「preset 型徽章」);两文件即 registry item 最小闭包,`registryDependencies: [foundation-semantic-badge]` 经闭包携带 `foundation-badge-utils`,`BadgeTone` 裸引用在源仓库与 copy-in 后同构成立。`lifecyclePreset[props.status]` + `.trim() ?` 空白回落与 LifecycleNotice 同形是「同词表的多形态」设计使然(09-08 已裁定不抽 composable)。
- **优化**:无。六态 preset icon + label 齐全;tone 校准分工明文(deprecated 走 neutral 为 sunset 保留 error、避免与字段行红色 Required 冲突);`sunset` 用 `error + solid` 走 Nuxt UI 默认反色文字;`size` 默认 `sm` 是与 HttpMethodBadge / WebhookBadge 同排对齐的显式记录(08-01 裁定);gallery `index.vue` 六态陈列;语义 token 无硬编码;无 U+FFFD。`label` 不带 `translate="no"`——它是可本地化文案而非领域词汇,与 HttpMethodBadge / WebhookBadge 的处理相反是正确的。
- **重构**:`Record<LifecycleStatus, LifecyclePresetEntry>` 使新增词汇漏配即编译报错;`FieldLifecycle` / `EndpointLifecycle` / `LifecycleStatus` 三个类型经 Nuxt auto-import 供 FieldItem(`utils/field.ts` 显式 `import type`)、LifecycleNotice、OperationHeader 消费,`tests/nuxt/api-docs-type-compat.ts` 锁定 `FieldLifecycle` 稳定导入。
- **Universal claims 核证**:lifecycle-preset.ts「a lifecycle value is singular per entity, so states never co-occur」——三个 dependent(`FieldItem` `lifecycle?.status`、`OperationHeader` `lifecycle?: EndpointLifecycle`、`LifecycleNotice` `status`)均为单值 prop,结构性成立;「each hue owns ONE meaning」在 lifecycle 词表内成立(success = healthy / available 覆盖 new 与 active;warning = proceed with care 覆盖 beta 与 maintenance)。跨 preset 的 neutral 复用(WebhookBadge `EVENT` / HttpMethodBadge `UNKNOWN` / lifecycle `deprecated`)靠词形 + icon 区分,claim 的作用域是本 preset,不越界。
- **为什么没改看起来可改的**:
  - JS 消费方传非法 `status` 时 `meta.value` 为 `undefined`,模板取 `meta.tone` 抛错——Vue 的 `renderComponentRoot` 会捕获、交给 `errorHandler` 并渲染注释节点,SSR 与客户端都不会整页崩;lifecycle 是刻意的闭合词表(与 HttpMethodBadge 的开放词表 + `UNKNOWN` 回落相反),类型层已封死,为 TS 契约外的输入加运行时回落属投机防御,不动。
  - reference 表格未列 `size` prop——kit index 表格是压缩的职责摘要而非穷尽 props 契约(08-01 裁定)。

### api-docs-method-badge(HttpMethodBadge)— required co-review

功能 diff 只改 `kits/api-docs/components/SiteSearch.vue`(触发 `UButton` 加 class + 注释)与其 spec;前者在 method-badge 的 base scope 内,recorder 推导其为 required co-review owner,本次在同一 results 中提交完整复审:`#item-leading` 仍只传 `method`,`trim + toUpperCase || 'UNKNOWN'` 归一化、`methodPreset` 色调、`translate=no` 经 SemanticBadge 单根透传到 UBadge 的链路均未触及;`http-method-badge.spec.ts` 原样通过;registry topology 未变;两条 dependency evidence(`@nuxt/ui` / `vue`)前提未变,原样重绑。无 finding。

## 反向横扫(簇外重复检查)

先读 `d126cfe` 的 `extraction-candidates.md` 作基线(无待决策候选,候选 1–9 已了结 / 不建议抽取)。对今日组件提取的特征模式在 `foundation/ kits/ app/components/ app/composables/ app/utils/ playground/` 全仓反查:

| 模式 | 命中 | 判定 |
|---|---|---|
| `preset[props.status]` 封闭联合查表 | LifecycleBadge / LifecycleNotice / HttpMethodBadge(带 fallback)/ SemanticBadge `BADGE_TONE_COLOR` | 09-03 判天然免疫、09-08 判抽 composable 净收益为负,不重复评估 |
| `.trim() ?` 空白回落一行式 | LifecycleBadge / LifecycleNotice / ResponseExample | 09-08 裁定不抽 |
| `watch` + `setTimeout` + `onCleanup` debounce / 陈旧取消 | 仅 SiteSearch;AnnotationPopover 的开合定时器是 hover 意图延迟、`useCopy` 的是反馈复位 | 语义不同,全仓唯一,不是候选 |
| `Fuse` 实例化 | 仅 SiteSearch(`utils/field.ts` 只是注释提及) | 全仓唯一 |
| `items-baseline` 徽章 / 标识首行对齐 | OperationHeader、FieldItem `data-field-identity` 行 | class 级排版约定,非逻辑重复 |
| `headingLevel?: 2 \| 3 \| 4` + `` `h${…}` ``、`defineOptions({ inheritAttrs: false })` + 多根、`tabindex=-1` + rAF focus + reduced-motion、`router.resolve`、`role="status"` live region、`ml-auto flex shrink-0` 行尾动作位、`text-balance` | 09-05 / 09-19 已评估 | 无新调用点,不重复评估 |
| 三个 preset wrapper 的 `size?: BadgeProps['size']` 默认 `sm` | HttpMethodBadge / WebhookBadge / LifecycleBadge | 08-01 裁定的显式对齐记录 |
| `touch-manipulation` tap 目标契约 | 分发面 11 处(09-19 的「8 处」+ SidebarNav 当日补的 3 处)+ 本日 SiteSearch;其余 8 个组件的 `UButton`(CopyButton / CodeBlock / ResponseExample / WebhookProtocol / FieldAnnotation / DocAnnotation / TermAnnotation,以及 AnnotationPopover 的 `#error` retry 按钮)未纳入 | 单一 utility class,属契约一致性而非抽取候选;缺口列入遗留 |

**登记表本次只追加 2026-09-20 扫描记录(无新候选);无待决策候选,不查询 / 不新建 GitHub issue。**

## Gate

完整 gate 跑了两次:第一次在 `3f1540e`(全绿),复审修复后在最终功能提交 `014c1c8` 全量重跑(下表;本机负载 4–7,其它会话同时在跑 build;各项退出码均显式为 0):

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 58 files |
| `pnpm test:registry` | ✅ 143 pass / 0 fail |
| `pnpm typecheck` | ✅ exit 0 |
| `pnpm build` | ✅ postbuild root CSS check 8 markers(含 SiteSearch 的 `max-sm:px-1.5` marker) |
| `pnpm test:consumer` | ✅ 42 个 closure smoke(30 个 isolated + `all-items` / `all-items-path-prefix-false` + 10 个 per-item foundation+kit 闭包)+ legacy-config 迁移 smoke 全部通过(`--sha `014c1c8`` 一致性通过) |
| `pnpm test:component` | ✅ 33 files / 431 tests(含新增 `SiteSearch touch contract`) |
| `pnpm audit:verify`(record 前) | 按预期标出 2 个因 `SiteSearch.vue` 改动而 stale 的 owner(`api-docs-site-search` 计划项 + `api-docs-method-badge` required co-review),其余 27 项 landed |
| `pnpm audit:verify`(record 后,单独执行、不在 gate 日志内) | ✅ 29 项 landed(含本次 3 计划项 + 1 co-review owner);legacy 0、deferred 0、未审计 0 |
| 分发物 U+FFFD 检查 | ✅ 无(`rg -l $'\\uFFFD' foundation kits` 为空) |

聚焦 spec(`site-search` / `operation-header` / `chrome-labels`)49 例(含新用例)在功能改动后、提交前单独跑过一次,全部通过。

## 复审循环(caveman-review,opus sub agent)

**第 1 轮**发现 5 条 🟡、6 条 🔵、1 条 ❓,逐条核证后处置:

- 🟡 采纳(已修,功能提交 `014c1c8`):`SiteSearch.vue` 注释与 spec 注释断言「触发按钮是顶栏唯一常显 tap 目标」为假——`DocsShell.vue` L197 汉堡 `UButton`(`lg:hidden`,触屏宽度可见)、L226 品牌链接、`DocsDomainSwitcher.vue` L41 域切换 `UButton` 同在顶栏;且 kit 组件不应断言消费方 shell 的构成。两处收窄为「高频 tap 目标、`max-sm` 折叠为 icon」。
- 🟡 采纳(报告 / 登记表文字):「分发面 8 处」是 09-19 之前的旧数,实为 11 处;未纳入 `UButton` 的组件漏了 AnnotationPopover 的 `#error` retry 按钮(7 → 8);登记表「09-05 / 08-19 / 08-01」中 operation-header 的上次全量审计应为 09-05。
- 🔵 采纳(报告文字):consumer smoke「42 个 isolated」改为「42 个(30 个 isolated)」;LOC 注明为 plan 计数;新纳入 scope 的 `docs-shell-data.ts` 在 evidence 窗口内有 1 行 i18n 改动;record 后的 `audit:verify` 单独执行、不在 gate 日志内;「49 例在功能提交前」改为「功能改动后、提交前」。
- 🔵 驳回:新用例未传 `searchDelay: 0`——该用例不打开面板、不触发搜索,`searchDelay` 无作用,为一致性加无消费者的 prop 不做。
- ❓ 采纳:gallery 外壳(`DocsShell` 汉堡 / 域切换器)tap 目标同样无 `touch-manipulation`,写入遗留供人工一并决策。

复审已核对项(节选):`SidebarNav.vue` L506 契约原文、`button.ts` base 无 `touch-action`、`Modal.vue` L81–87 / `CommandPalette.vue` 切片 / `search.js` `highlight()` 三条依赖行为、9 处 gallery `truncate` 行号、gate 数字与日志、四项「零变更」逐项 `git diff --stat`、新用例缺 class 时必失败、method-badge co-review 的 scope / itemDigest / dependencies 与 base 完全一致、ledger 状态机与双提交顺序。

**第 2 轮**:在最终功能提交 `014c1c8` 与其后的簿记提交上重新复审;结果与逐条处置记录在 PR #149 的复审评论中,若第 2 轮仍有需修项,另起 `docs(audit)` 提交并更新本节。

## 遗留与下一步

- 本批无 deferred finding、无人工决策点。
- **`touch-manipulation` 契约的覆盖面与文档化(交人工)**:SidebarNav 源码注释自述它是「kit 的 tap 目标触控契约」,但 references 中没有任何条款记录该规则(`rg touch-manipulation references` 为空),且分发面仍有 8 个组件的 `UButton`(CopyButton / CodeBlock / ResponseExample / WebhookProtocol / FieldAnnotation / DocAnnotation / TermAnnotation,以及 AnnotationPopover 的 `#error` retry 按钮——其触发器已纳入、retry 按钮未纳入)未纳入;gallery 外壳自身的 tap 目标(`DocsShell.vue` 汉堡 `UButton`、`DocsDomainSwitcher.vue` 域切换 `UButton`)也没有,人工决策时需一并决定 app/ 面是否纳入同一规则。建议人工二选一:把规则写进 `references/foundations/focus-a11y.md`(或 interaction 相关条款)并在各 owner 下次排期时补齐;或把 SidebarNav 的注释收窄为「本组件与 FieldItem / SchemaComposition / OperationTarget 的约定」。本日不改共享 reference(会触发大面积联审),也不越簇修其它组件。
- **09-05 遗留仍未对齐**:gallery 手写 h1 identity header 9 处 `min-w-0 truncate`(`endpoint-reference.vue` L579 / L755 / L790、`webhook-reference.vue` L331 / L479 / L510、`DocsShellReference.vue` L209 / L304 / L344)与 kit `OperationHeader` 的「wrap, don't truncate」相反,375px 下同样会丢标识尾部;页面私有 recipe,建议人工在一次功能 PR 里顺手对齐并用 `--record --affected` 复审。
- 既有 scope 缺口(09-17 / 09-18 / 09-19 记录,非本簇):`api-docs-field-item` / `api-docs-code-block` / `api-docs-enum-table` 的 scope 尚未含 `webhook-reference.vue`,`foundation-inline-markdown` 未含 gallery 参考页;FieldValueStructure / AnnotationPopover / SidebarScenarioTags 的 `touch-manipulation` 未被 spec 锁定;下次审计相应 owner 时纳入。
- Round 4 剩余 12 个组件按依赖簇继续。
