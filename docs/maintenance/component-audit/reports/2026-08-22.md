# 2026-08-22 组件审计(复核 PR #104)

- **轮次**:round 2(`roundRollover: false`,不触发轮末横切扫描)
- **基线**:`origin/main` @ `5b2cdb10bae6f8e7b102a5547ff5766a0cf278f9`(与 2026-08-21 相同,main 未前进)
- **计划输出**:`api-docs-request-example`、`api-docs-sidebar-nav`(共 1070 LOC)——与 2026-08-21 完全相同
- **处置**:昨日审计 PR #104(`chore/component-audit-2026-08-21`)仍开放且覆盖同一簇,按 preflight 规则**继续/复核该 PR**,不新建同类 PR;PR 分支已基于 `MAIN_SHA`,无需 rebase
- **结论**:独立复扫未发现 #104 遗漏的新 finding;全量 gate 重跑全绿;**ledger 不重录**(无新功能改动,既有 evidence `headSha=1652540a` 继续成立),本文件仅记录复核过程

## 为什么不重录 ledger

计划 item 与 2026-08-21 一致,昨日 record 的 `scope-v1` evidence 已绑定精确功能提交 `1652540a5e349f52f527f59df3f9f0a26cb38319` 与 registry 拓扑,`pnpm audit:verify` 在分支上重算通过。今天没有任何触及分发组件、tests、references 或 registry 的改动,重录只会把 `lastAuditedAt` 改成今天而不带来新证据——属无谓 churn。main 上的 canonical ledger 仍不含 8/21 结论,这是设计(PR 未合并前不进 main),不是缺陷。

## 独立复扫(在 #104 分支 HEAD 上)

### api-docs-request-example — 维持 clean(change: none, status: verified)

重新逐条核对 `RequestExample.vue`(112 行)与 `useExampleScenarioSelection.ts`、`request-example.md`:受控/非受控挂载期判定、未知 id 派生不回写、非受控重排保留/移除丢弃、单次 emit、`USelect` `aria-label`、≤1 场景隐藏选择器、`trustHighlightedHtml` 默认 false 透传——与昨日结论一致,无新问题。

### api-docs-sidebar-nav — 维持 modified / deferred(open finding `f-12d73e8bc49d`)

- **finding 证据复核**:`useSplitPane.ts` L103-104 确有 `Escape` 取消拖拽回滚;`useSplitPane` / `useCodeWrap` 均为 `useCookie` 种子 `useState`(源码中 `localStorage` 仅出现在注释「no localStorage」),昨日「分发组件仅 SidebarNav 用 localStorage」的陈述成立;SidebarNav `onMounted` 后读 localStorage 导致 lg+ 刷新首帧 288px 跳变的描述与代码一致。finding 维持 open,仍交人工决策。
- **补充反向横扫**(昨日未列的维度):`aria-live="polite"` live-region 套路见于 AnnotationPopover / CopyButton / OperationTarget / EnumTable / SiteSearch / SidebarNav 六处,但各自播报内容与触发条件不同,无可抽取的共享逻辑(SiteSearch 多一个 `aria-atomic="true"`,`role="status"` 按 ARIA 规范已隐含 atomic,非不一致);stretched-link(`pointer-events-none relative` 兄弟层)仅 SidebarNav 一处;`document.fonts.ready` 重测仅 SidebarScenarioTags 一处;`getCurrentInstance()?.vnode.props` 受控检测在 Request/ResponseExample 各一处(昨日已评估不抽);`setPointerCapture` 仅 SidebarNav(已并入 finding);`router.resolve` 在 SidebarNav(active 推断)与 SiteSearch(目的地归一化)用途不同,非重复。无新抽取候选。
- **SidebarScenarioTags 测量层核对**:隐藏测量层是 `absolute` flex 容器,其 flex item 的 `min-width:auto` = 文本 min-content 宽度(`whitespace-nowrap`),故即使全部标签总宽超过容器,`offsetWidth` 也不会被压缩到小于自然宽度,fit 数学成立;不记 finding。

### api-docs-method-badge — co-review 结论维持

今日无新功能改动触及其 scope,昨日 co-review 结论(`verified`)继续成立。

## Caveman 复审(今日,第 3 轮视角)

对 #104 完整 diff(代码 + ledger + 报告 + PR 描述)重审,无 🔴 bug / 🟡 risk,仅记 nit / q,均不改:

1. 🔵 nit `references/kits/api-docs/sidebar-nav.md:L38`:props 表写 `searchPlaceholder` 默认 `Search`,代码默认是 `Search…`(`aria-label` 剥省略号后才是 `Search`);a11y 节已说明剥省略号规则,语义可推导。非本 PR diff,修改会触发 scope 重录,收益低于成本,留给人工顺手改。
2. 🔵 nit `tests/component/sidebar-nav.spec.ts` resize 用例:写入 `localStorage['sidebar-nav-spec-resize']` 无 afterEach 清理;用例末尾双击复位到 288 已持久化,且 vitest 按文件隔离环境,结果确定,不改。
3. ❓ q ledger `api-docs-sidebar-nav.scope`:纳入 `foundation/components/SplitPaneHandle.vue` 与 `foundation/composables/useSplitPane.ts`(finding 的比较对象)。效果是 SplitPane 一旦变化 SidebarNav 证据即 stale、强制复核 finding 是否仍成立——这是有意的耦合,可接受;finding 关闭后应从 scope 移除。

## Gate 输出(#104 分支 HEAD `2da9ea55`,今日重跑)

- `pnpm registry:validate`:40 items / 55 files 通过
- `pnpm test:registry`:131 tests 全通过(fail 0)
- `pnpm typecheck`:exit 0
- `pnpm build`:通过(postbuild root CSS check 8 markers)
- `pnpm test:consumer`:通过(isolated-api-docs-sidebar-nav closure smoke)
- `pnpm test:component`:31 files / 326 tests 全通过
- `pnpm audit:verify`:通过;`api-docs-sidebar-nav` 仍为 deferred(1 open finding),未审计 1 个(`foundation-use-raf-task`)
- GitHub checks(PR #104 @ `2da9ea55`):全部 SUCCESS,mergeable

## 遗留与建议下一步

- PR #104 等人工评审合并;合并前 canonical ledger 不会推进,明天的计划仍会选出同一簇——若明天 main 仍未前进,同样走复核路径,不重复造 PR。
- `f-12d73e8bc49d`(SidebarNav resize 收敛到 `useSplitPane`)待人工判定:采纳则单独开实现 PR;不采纳由人工 `--allow-wont-fix` 标记。
- 上述 nit 1 可在人工评审 #104 时顺手修正,或随下次触及 `sidebar-nav.md` 的功能 PR 一并处理。
