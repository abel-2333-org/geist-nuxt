# 2026-09-17 组件审计

- **Round**:4(第三天,`roundRollover: false`,不执行 §5b 轮末横切;`remainingInRound: 21`)
- **Base main SHA**:`688f83c63c3d278dd58115c987b650f1b45d1b0a`
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>`,`openFindingReentry` / `priorityBacklog` 均为空(全仓 open finding 队列为空)
- **选簇原因**:seed 取与上批(`api-docs-field-annotation` / `foundation-inline-markdown` / `api-docs-webhook-protocol`)关系分最高的 `foundation-inline-code`(三者都直接依赖它);扩张取 `foundation-annotation-popover`(plan 的 `why` 为空,即与 seed 仅同命名空间关联分,按并列规则胜出),再取其直接依赖者 `foundation-doc-annotation`(直接依赖 popover),合计 458 LOC,本批 3 个组件
- **开放 PR 检查**:`gh pr list --state open` 为空,无需继续既有 audit PR
- **自上次 evidence 以来的变更**:三项 evidence `headSha` 均为 `9274694`(09-01,Round 3 同簇全量 clean)。`git diff 9274694..HEAD` 对三项 owning files、tests、references 与 gallery 页面均**零变更**。因此本次是对同一功能快照的**第二次从零全量复审**;为避免重复上一轮的静态对照,本次把重心放在「实测核证上游行为」——用临时 spec 实际挂载组件读取渲染结果,并用 `pnpm build` 产物核对 CSS 顺序
- **浏览器实测限制**:Browser pane 的 `preview_start` 在无人值守(scheduled task)会话中被工具直接拒绝(「Dev servers can't be started from unattended sessions」),本次所有运行时核证改由 Nuxt 运行时下的 vitest 挂载 + 构建产物完成;已部署 gallery 与本簇分发文件内容一致,但本次未做远端浏览器测量

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `foundation-inline-code` | none | **deferred** | 新增 1 条 **medium** finding:tailwind-merge 把自定义字号 utility `text-code` 归为文字颜色组——它既不覆盖 ProseCode 的 `text-sm`(13px 只靠 Tailwind 产物排序生效),又会在调用方传任一文字颜色类时被当作颜色删掉、静默退回 14px;修法落在 `geist-foundation` hub 配置,交人工决策 |
| `foundation-annotation-popover` | none | verified | clean;实测核证 reka 已提供 dialog 命名与 trigger aria,壳无需补 aria |
| `foundation-doc-annotation` | none | verified | clean;两点观察不构成 finding |

**零代码改动,无功能提交**;evidence `headSha` 为 `ff6135d`(复审第 1 轮撤回 ledger 后重录时的 HEAD,与 base `688f83c` 仅簿记文件不同,`git diff 688f83c..HEAD` 只含 `docs/maintenance/component-audit/**` 三个文件,scope 内所有文件内容与 base 完全一致;CI 会在 synthetic merge 树上重算 evidence)。

## 各组件详情

### foundation-inline-code(InlineCode,24 行)

历次:Round 2 内建 `translate="no"` 契约(modified)、09-01 Round 3 全量 clean。本次按四维度复审:

- **晋升 / 精简 / 重构**:无。24 行原子只做 ProseCode 委托 + 两处 Geist 校准;`ui.prose: true` 由 `foundation/config/nuxt.ts`(随 `geist-foundation` 分发)保障,消费项目不需额外开启;registry `files[]` 1 个文件、`registryDependencies` 仅 `geist-foundation`,与源码一致。
- **优化 → 本次唯一 finding(medium,deferred;初记 low,复审第 1 轮实证颜色类覆盖后上调)**。上一轮以「渲染 class 含 `rounded-sm` / `text-code`」为据判定校准生效;本次实际挂载读取完整 class:

  ```
  px-1.5 py-0.5 text-sm font-mono font-medium inline-block border border-muted bg-muted rounded-sm text-code
  ```

  `rounded-md` 被 tailwind-merge 去重,`text-sm` **没有**。原因:tailwind-merge 3.6.0 的 font-size class group 只按 t-shirt size(`xs/sm/base/…`)识别,`main.css` 用 `--text-code` 生成的语义 utility `text-code` 落入 text-color 组,与 `text-sm` 不冲突。`pnpm build` 产物 `entry.*.css` 中 `.text-base`(offset 51199)< `.text-sm`(51463)< `.text-xs`(51858)< `.text-code`(52376):13px 只是因为单声明的 `.text-code` 被 Tailwind 4.3.2 排在所有主题字号之后才生效,`line-height` 仍取 `text-sm` 的 `1.25rem`。再挂载 `attrs: { class: 'text-xs' }` 得到 `… text-code text-xs`——两者并存、由 CSS 顺序决定,调用方传字号同样被 `text-code` 压过。**复审第 1 轮补充复现(决定性)**:`node` 直接调用 tailwind-merge 3.6.0,`twMerge('text-red-500 text-code') === 'text-code'`,证明 `text-code` 被归入 **text-color** 组;于是挂载 `attrs: { class: 'text-error' }`(或 `text-toned`)得到 `px-1.5 py-0.5 text-sm … rounded-sm text-error`——`text-code` 被当作被覆盖的颜色**删掉**,字号静默退回 `text-sm` 14px。这意味着原子自己的 class 级校准并未按 nuxt-ui 核心规则 4「class prop 覆盖 theme defaults」的机制覆盖主题 `text-sm`,而 `data-display.md`「仅覆盖两处回到 Geist 基础」在合并层面只成立一处(`rounded-md` → `rounded-sm`);调用方一次正常用法(给错误值加语义颜色)就会破坏文档化的 Copy 13 Mono 契约。`inline-code.spec.ts` 只断言 `text-code` 存在,若 Tailwind 排序算法变化导致 `text-sm` 反超,测试仍绿。
  - **今天没有任何消费点受影响**:分发面内 `InlineCode` 的调用(FieldItem ×2、FieldValueRequirements ×2、EnumTablePanel、FactRow、FieldAnnotation、InlineMarkdown)都只传 `wrap-anywhere` / `min-w-0` / `break-all`(FactRow 另冗余传了原子已默认的 `translate="no"`),无人传字号或文字颜色;当前渲染结果与契约一致(13px)。severity 取 medium 而非 high:无现有调用点命中、无视觉回归,但触发条件是消费方的正常用法且无任何告警。
  - **为什么不在本 PR 修**:根治只能在合并层——`foundation/config/app.ts` 加 `ui.tv.twMergeConfig.extend.classGroups['font-size'] = [{ text: ['code'] }]`(Nuxt UI 的 `tv` 用 `createTV(appConfig.ui.tv)`;已用 `extendTailwindMerge` 同参数实证:`text-sm text-code` → `text-code`,`text-code text-xs` → `text-xs`,`text-red-500 text-code` 两者并存),同时 `main.css` 补 `--text-code--line-height: 1.25rem` 让 `text-code` 自带行高、去掉 `text-sm` 后视觉零变化。这两处属 `geist-foundation` hub 配置,改变**所有消费项目**的 class 合并语义(任何 Nuxt UI 组件 class 里 `text-code` 与主题字号并存时,后者将被去重),按 §7「跨契约边界」标准记 deferred 交人工决策,建议单独实现 PR 并在 `inline-code.spec.ts` 补「渲染 class 不含 `text-sm`」断言。
  - 修法替代项已评估并否决:在原子里改用 `text-(length:--text-code)` 变量写法可让 tailwind-merge 识别为字号,但与全仓 `text-code` 语义 utility 分叉,且 typography.md 的类型阶梯仍需一次性统一,不如在合并层一次解决。
- **Universal claims 核证**:源码注释「machine translation must leave it alone」与 reference「内容按字面值对待」由 `translate="no"` 默认 + fallthrough 覆盖两条用例锚定,成立;registry / gallery 描述「calibrated to Geist: 6px radius, Copy 13 Mono」——6px 成立(`rounded-md` 已去重),13px 当前成立但依据是排序而非覆盖,即本次 finding。
- **依赖证据**:保留 `@nuxt/ui` / `vue`,新增 `tailwind-merge`(3.6.0,字号组识别规则)与 `tailwindcss`(4.3.2,utility 排序)两条前提绑定——两者在 lockfile 均为唯一 resolution;任一升级都应重跑本 finding 的复现。

### foundation-annotation-popover(AnnotationPopover + utils/annotation,296 行)

历次:#30 晋升、Round 2 / 09-01 Round 3 全量 clean。本次全量复审,并实测三项上一轮只靠阅读源码判定的上游行为:

- **晋升**:foundation 归属正确,被 Term / Doc / Field 三形态依赖;`files[]` 2 个文件(壳 + `utils/annotation.ts` label 模型)与 `registryDependencies` 仅 `geist-foundation` 一致;label 模型放 util 而非 SFC 的跨拓扑理由(源码注释 + overlays.md)仍成立。
- **精简**:labels 合并 idiom 是候选 3(不建议抽取);`focusPanel` 的可聚焦 selector、hover 定时器、`OpenSource` 焦点所有权钩子全仓唯一(见反向横扫),无可抽取重复。
- **优化(实测核证)**:
  - 上游 a11y:挂载后打开,触发器 `<button>` 实际带 `aria-haspopup="dialog"` `aria-expanded="true"` `aria-controls="reka-popover-content-…"` 且 `id="reka-popover-trigger-…"`;面板根 `role="dialog"` `aria-labelledby="reka-popover-trigger-…"`(reka-ui 2.9.10 `PopoverContentImpl` 内置)——浮层以触发文本(如「sliding window」)命名,壳不需要自加 `aria-labelledby` 指向 eyebrow,**否决**了「dialog 无可访问名」这一候选 finding;
  - focus 样式:触发器与面板均为 `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary`,与 `focus-a11y.md` 实底控件规格逐字一致;
  - 语义 token:`hover:bg-elevated`、`text-dimmed`、`outline-primary`,无硬编码颜色;`rounded-xs`(3px)在 `spacing-layout.md` 圆角 scale 内;`w-72 sm:w-80` + `max-w-[calc(100vw-2rem)]` 是 overlays.md 明文的宽度契约;
  - 键盘 / 焦点:381 行 focused spec 覆盖 keyboard 进面板、hover 不夺焦、Escape 归焦、外部点击不抢焦、forward-Tab 接管、disabled 定时器卫生,全部通过。
- **重构**:`<script setup>` + TS,props / emits / slots 契约未变;`OpenSource` 状态机的每条分支都有注释说明原因。
- **观察(不记 finding)**:hover 打开面板后再**点击**触发器,面板会关闭(实测 `PANEL AFTER CLICK: CLOSED`)——reka `PopoverTrigger` 的 onClick 是 `onOpenToggle`,壳把 hover 会话交给 click 骨架后 toggle 语义自然如此。overlays.md 契约是「click 为骨架、hover 为增强」「tap / 点击 / Enter 三端行为一致」,toggle 在三端确实一致,没有被违反的条款;若产品上希望「点击 = 钉住」,属契约变更,需人工先改 reference 再动壳,本次不动。
- **为什么没改看起来可改的**:`openSource` 在 keyboard 会话关闭后不复位——下一次打开必经 pointerdown / keydown / hover 定时器三者之一重新赋值,复位是冗余写;`error` 态的 `<p role="status" aria-live="polite">` 与 loading 的 `sr-only role="status"` 是同一 chrome 的两态,合并成一个常驻 live region 会让 loading 文案也可见,不改。
- **依赖证据**:新增 `@nuxt/ui`(UPopover click 模式 as-child 触发器、content prop defu + v-bind 透传焦点钩子)与 `reka-ui`(2.9.10,dialog 命名 / trigger aria / toggle 语义)两条前提绑定——本次「壳不自加 aria」的结论直接依赖它们。

### foundation-doc-annotation(DocAnnotation,138 行)

历次:#39 晋升、Round 2 / 09-01 Round 3 全量 clean。本次全量复审:

- **晋升**:foundation 归属正确(管线无关,只收 async `load`);`registryDependencies` 2 项(`foundation-annotation-popover` → 壳 + labels 类型,`foundation-inline-markdown` → 描述渲染)与源码一致;`UButton` 是 Nuxt UI 原语。
- **精简**:`requestToken` 与 `useFieldAnchor` 的 `navigationToken` 已于 09-01 评估为投机性抽象(fetch 结果守卫 vs 多步导航中止),无新调用点;`idle | loading | ready | error` 四态机全仓唯一。
- **优化(复核无回归)**:导航动作是 `UButton :to` → 真实 `<a>`(Web Interface Guidelines「links use `<a>`」);标题 / 描述 `wrap-anywhere`、描述 `line-clamp-4`,长 URL 不撑破固定宽面板(`annotation-forms.spec.ts` 断言);loading / error / 焦点 / 明暗全部由壳承担。
- **重构**:`<script setup>` + TS;`DocPreview` / `DocAnnotationLabels` 从 SFC 导出供 adapter 类型化;`watch(() => props.to)` 只 key 在 `to` 而不 watch `load` 的理由有注释(inline closure 身份每次渲染都变)。
- **Universal claims 核证**:「a late response can **never** overwrite the newer document's preview」——`ignores a stale response after the target document changes` 用例,成立;「error state **never** dead-ends」——`keeps the page fallback in error state and retries successfully` 用例(错误态仍渲染 `<a href>`),成立;「re-hovering the same link reopens instantly」——`loads once on first open and reuses the cached preview` 用例,成立。
- **观察(不记 finding)**:(1)`load` reject 后关闭再打开,`onOpen` 因 `state !== 'idle'` 不会自动重试,错误态沿用到用户点 Retry——契约写的是「结果按实例缓存」且 Retry 常驻,是否把「重开即重试」定为契约属产品决策;(2)`catch {}` 吞掉 reject 原因不做 dev 告警——错误态在 UI 可见,adapter 的 `load` 可自行记录;与 Term / Field 的「未命中 ref」dev 告警语义不同(那是作者数据错误,这是运行时失败),不强行对齐。

## 反向横扫(簇外重复检查)

基线:`688f83c` 的 `extraction-candidates.md`(候选 1–9,无待决策)。对今日组件提取的特征模式全仓 `rg` 反查:

| 模式 | 命中 | 判定 |
|---|---|---|
| `translate="no"` 字面内容保护 | InlineCode 原子 + CodeBlock `<pre>` + kit 内另 11 处:裸 `<code>` ×4(OperationHeader、OperationTarget ×2、FieldItem)、`<span>` ×2(FieldItem)、`SemanticBadge` ×2(HttpMethodBadge / WebhookBadge)、FieldValueStructure ×2、FactRow ×1(传给 InlineCode 的冗余 attr,原子已默认) | 宿主是非 tonal 表面(标题、地址段、badge、结构 chip),单属性 i18n 约定,不是可抽取实现;不登记。FactRow 的冗余 attr 无害,属 `api-docs-webhook-protocol` 分发文件,不为一行冗余触发 co-review |
| mouse-only hover 开合定时器 / `pointerType === 'mouse'` 门控 | 仅 AnnotationPopover | 全仓唯一 |
| `focusPanel` 可聚焦 selector 字符串 | 仅 AnnotationPopover(ResponseExample / useFieldAnchor 的 `querySelector` 目标不同) | 全仓唯一 |
| `onOpenAutoFocus` / `onCloseAutoFocus` 焦点所有权钩子 | AnnotationPopover;SiteSearch 另有 `onCloseAutoFocus` | SiteSearch 语义是 palette 关闭归焦,形状与 OpenSource 状态机不同,不抽 |
| `requestToken` 陈旧响应守卫 | DocAnnotation;`useFieldAnchor` `navigationToken` | 09-01 已评估为投机性抽象,无新调用点,结论不变 |
| 「close 再导航」`UButton`(`color="neutral" variant="ghost" size="xs" trailing-icon="i-lucide-arrow-right"`) | Term / Doc / Field 三形态 | 与 09-16 记录的 `line-clamp-4 wrap-anywhere` / `decoration-(--ui-primary)/50` 同属家族形态矩阵刻意共享的 class 组合,不抽 |
| labels 合并 `{ ...defaults, ...props.labels }` | 11 处 | 候选 3,不重复评估 |
| `idle \| loading \| ready \| error` 四态机、`aria-busy` 面板 | 各仅 1 处 | 全仓唯一 |
| `font-mono text-[0.8125rem]` arbitrary 13px | app/ 页面 50+ 处(非分发面);`typography.md` 亦按此写法 | 与 `main.css` 已有的 `text-code` 语义 utility 并存,属 token 采纳一致性,记入遗留供人工处理,不作抽取候选 |

**登记表本次只追加 2026-09-17 扫描记录(无新候选);无待决策候选,不查询 / 不新建 GitHub issue。**

## Gate

在 base 树 `688f83c`(零功能改动)上执行:

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 58 files |
| `pnpm test:registry` | ✅ 131 pass / 0 fail |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ postbuild root CSS check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 isolated closure smoke + legacy-config 迁移 smoke 全部通过 |
| `pnpm test:component` | ✅ 32 files / 401 tests |
| `pnpm audit:verify`(record 前) | ✅ 29 个 item evidence 全部成立(base 树未改) |
| `pnpm audit:verify`(record 后,复审重录后再跑) | ✅ 28 项 landed + `foundation-inline-code` deferred(1 open finding `f-4b98b0623a0a`);legacy 0、未审计 0 |
| 分发物 U+FFFD 检查 | ✅ 无 |

临时核证 spec(挂载 InlineCode / AnnotationPopover 读取渲染 class 与 aria,已删除、不提交):首轮 3 例 + 复审第 1 轮 1 例(颜色类覆盖)全部通过,输出见上文引用;tailwind-merge 分组用 `node` 直接调用 `twMerge` / `extendTailwindMerge` 实证。

## 遗留与下一步

- **新增 deferred finding(medium,`f-4b98b0623a0a`;初记 `f-0543595c5d75` low,复审第 1 轮撤回重录)**:`foundation-inline-code` 的 `text-code` 被 tailwind-merge 当作文字颜色类——不去重 `text-sm`,且调用方传文字颜色类时被删掉。**人工决策点**:是否采纳「`foundation/config/app.ts` 扩展 `ui.tv.twMergeConfig` 字号组 + `main.css` 补 `--text-code--line-height`」的根治方案(改变全消费项目 class 合并语义,建议单独 PR,并补 spec 断言 `text-sm` 不在渲染 class 中);若判定现状可接受,需人工以 `--allow-wont-fix` 关闭。
- **顺带遗留(非 finding,超出今日簇)**:`references/foundations/typography.md` 的 Copy 13 / Copy 13 Mono / Label 13 条目仍写 `text-[0.8125rem]`,而 `main.css` 已提供 `text-code` 语义 utility,app/ 页面 50+ 处沿用 arbitrary 写法;建议在上述根治 PR 中一并把 typography.md 的 13px 档指向 `text-code`,页面写法可择机批量替换(非分发面,不影响消费项目)。
- 09-11 记录的「SidebarNav 分区 header 按钮未显式设 `touch-manipulation`」线索仍待该簇排期时核证。
- Round 4 剩余 21 个组件按依赖簇继续;`foundation-inline-code` 带 open finding 将重回 pool。

## PR #127 输出字段存在性：受影响 owner 复审

功能提交为 `3fd4f72`（`feat(api-docs): FieldItem 以类型记号表达输出字段存在性（#127）`，rebase 于 `91e8a86`），本节随 `--record --affected` 的 ledger 更新作为第二个提交。不推进每日排程 round。

### 最终结论

- `FieldNode.presence: FieldPresence`（`optional` / `nullable` 布尔 + `empty` 字面量 + `condition`）三个事实相互独立，不带 `presence` 的行保持原展示，不从缺少 `required` 推断。
- 渲染为类型记号：`name?`、`string | null | ""`；条件句独立成行（`border-accented`），红 / 琥珀继续只属于必填强度轴。唯一文案键 `mayBeOmitted` 只供屏幕阅读器（视觉 `?` 加 `aria-hidden`）。
- 值节点 `ValuePresence` 类型层剔除 `optional`，`describeValuePresence()` 运行时再剥一次；元素 / 解码内容的存在性只在各自作用域块以 `object | null` 渲染，与外层字段互不继承。
- FieldAnnotation popover 共用 `describeFieldPresence` / `presenceTypeExpression`，不预览条件句。
- 与 Issue 验收的一处偏离（用户认可）：metadata 不再区分「按条件可省略」与「可省略」，有条件行即有条件。
- 用户授权本地实现、提交、push 与开 PR；未 merge。

### 验证

- `tests/component/field-presence.spec.ts` 18 例：始终存在 / 仅 nullable / 仅 optional / 组合 / 条件 / 请求与输出并存 / 字段-值双向边界 / 编码空容器 / 递归 / popover 一致；`tests/nuxt/api-docs-type-compat.ts` 以 `@ts-expect-error` 锁定值节点不得带 `optional`。
- 全量组件 419/419，typecheck、build、`registry:validate`、`test:registry`、`test:agent`、`test:consumer` 通过；rebase 后重跑 typecheck 与相关 spec 44/44。
- 浏览器实测 kit 首页第四组与 endpoint / webhook 参考页：明 / 暗、桌面 / 375px、EN / ZH，联合表达式在 ` | ` 处断行，无溢出、无 console 错误。工具合成的 Enter / space 不触发 Reka collapsible，对未改动的旧按钮同样如此，属工具限制；本次不新增交互元素。
- 两轮 opus `caveman-review`：第一轮（文字 token 版）无 blocker，修复 `empty: ''` 空 chip 与 popover 缺 conditional 属性；用户评审后改为记号方案并删掉六个 label 键。第二轮无 blocker，采纳：type span 补 `wrap-anywhere`、`?` 加 sr-only 文案、`'conditional'` 状态无消费者故收敛为布尔、reference 补 FieldAnnotation 行与 utils 清单。

### 受影响 owner

recorder 推导 19 个 owner：`api-docs-field-item`、`api-docs-field-annotation` 为真实功能改动；其余 17 个因共享 `references/kits/api-docs/index.md` 新增输出存在性段落或 registry 拓扑（`api-docs-field-item` 描述）变化而需复审，代码与契约未变，复核无回归。所有 owner 无 open finding，既有 dependency evidence 原样保留。
