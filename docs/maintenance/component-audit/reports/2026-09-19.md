# 2026-09-19 组件审计

- **Round 4**(第 5 个批次;`roundRollover: false`,不做轮末横切);audited main SHA `52e7f1dfcf6c6f25057417606e85bdccf90772d2`(#142 合并后的 `origin/main`)
- **簇**:`api-docs-sidebar-nav`(SidebarNav 647 行 + internal SidebarScenarioTags 259 行)、`api-docs-code-rail`(CodeRail 341 行),合计 1247 LOC(按 `audit-plan` 计数;超 1100 预算是算法「不留孤儿」的下限保证:两项即最小簇)
- **选簇原因**(算法输出):接续上次审计簇——`api-docs-sidebar-nav` 直接依赖 `foundation-split-pane-handle`,与上批三项共享 `foundation-use-split-pane` / `foundation-use-raf-task` / `foundation-split-pane-handle`;`api-docs-code-rail` 同样共享这三项依赖
- **自上次 evidence 以来的变更**:`api-docs-sidebar-nav` evidence `headSha` 为 `1685a6c`(09-17,text-code 迁移 affected 复审),此后 scope 内只有 `app/pages/kits/api-docs/sidebar-nav.vue` 被 #131 改过 class token(已在 09-17 核过);组件源码、internal helper、两个 spec 与 reference **零变更**。`api-docs-code-rail` evidence `headSha` 为 `a3a93e0`(09-18,rich fact affected 复审),此后 scope **零变更**;owning file 自 08-26(modified)起未动。因此本次是两项在 Round 4 的第一次从零全量复审,重心放在上一轮报告遗留的待验证线索(09-11:SidebarNav 分区 header 按钮缺 `touch-manipulation`)与 evidence 完整性(dependency / scope 缺口)
- **浏览器实测限制**:无人值守会话无法启动 preview;运行时核证以 vitest 挂载(`sidebar-nav.spec.ts` / `sidebar-scenario-tags.spec.ts` / `code-rail.spec.ts` 共 23 例,含复审后新增用例)与 gate 产物完成。本次改动是单个 utility class,无视觉 / 布局变化,不需要远端浏览器测量

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `api-docs-sidebar-nav` | modified | verified | 全部 tap 目标(分区 header 按钮、搜索清除按钮、行链接)纳入 `touch-manipulation` 触控契约(09-11 线索闭合);新增 `@nuxt/ui` / `reka-ui` dependency evidence;其余四维度无 finding |
| `api-docs-code-rail` | none | verified | clean;scope 补入 `foundation/utils/breakpoints.ts`(`enabledFrom` 取值域);两条观察不立 finding |

功能提交两个:`54e2542`(分区 header 按钮补齐 `touch-manipulation`)与 `a1ad0cc`(复审后把搜索清除按钮与行链接一并纳入,触控断言改为独立用例);ledger evidence 绑定 `a1ad0cc`;报告与 ledger 为其后的簿记提交。

## 各组件详情

### api-docs-sidebar-nav(SidebarNav + SidebarScenarioTags)

历次:08-04 首审、08-21 ~ 08-25 连续五天(stretched-link、测量式场景标签、active 单源、IME Esc 等)、09-03 `useSplitPane` 收敛(#105,finding `f-12d73e8bc49d` resolved)、09-04 active 派生下沉 `resolved` computed(modified)、09-17 affected 复审。本次四维度:

- **晋升**:无。ADR-005 裁定留在 API Docs kit(数据模型含 `method` / `scenarios` / `endpoints` 领域概念)复核仍成立;`SidebarScenarioTags` 是 internal helper,随 item 复制、不公开自动导入,粒度合理。`registryDependencies` 5 项(`geist-foundation` / `api-docs-method-badge` / `foundation-use-split-pane` / `foundation-use-raf-task` / `foundation-split-pane-handle`)与源码实际引用(`HttpMethodBadge` 自动导入、`useSplitPane` / `SplitPaneHandle` / `useRafTask`)一一对应,无冗余、无缺失。
- **精简**:无。647 行服务单一职责(导航树 + 树内过滤 + 调宽 glue);width resize 的拖拽数学、clamp、Escape 回滚、cookie 持久化已全部在 `useSplitPane` / `SplitPaneHandle`,组件只剩 16 / 48px 步长与 min / max / reset 三个跳转映射(L405–414)。`slug()` 回退、`isComposing` 守卫、hidden measurement layer 全仓各仅一处(见反向横扫)。
- **优化(本轮落地)——tap 目标缺 `touch-manipulation`**。
  - **复现输入**:任意板块 header(gallery「指南」「DIRECT API」),iOS Safari 上快速双击(开→关)。该 `<button>`(base 帧 L505)是分发面 tap 目标中的缺口:FieldItem ×3(L270 / L395 / L628)、FieldValueStructure(L206)、SchemaComposition anyOf(L314,09-11 补齐)、OperationTarget(L185)、foundation 的 AnnotationPopover 触发器(L215)以及**本 item 内** SidebarScenarioTags 的 `+N` 触发器(L192,源码注释明言「drops the 300ms tap delay on touch」)都带该 class;其中 FieldItem ×3 / SchemaComposition / OperationTarget 5 处被 `field-item-structure.spec.ts` L63 / `schema-composition.spec.ts` L97 / `operation-target.spec.ts` L216 的 `classes()` 断言锁定,FieldValueStructure / AnnotationPopover / SidebarScenarioTags 3 处**未**被测试锁定(测试覆盖缺口,见「遗留」)。FieldItem 的 `?` 记号按钮(L325)也不带该 class,但它是 hover / focus-only 的 tooltip 触发器(源码注释「Touch never opens a tooltip」),触屏无交互,不计入。复审第 1 轮指出:本组件的搜索清除 `UButton` 与行 `ULink` 覆盖层同样是 tap 目标,而 kit 惯例已把一次性 tap 控件(复制段、锚点复制、popover 触发器)纳入同一契约,「一次性 tap 不在契约内」站不住——故第二个功能提交 `a1ad0cc` 把这两处一并补齐。
  - **依据**:web-design-guidelines「Touch & Interaction:`touch-action: manipulation`(prevents double-tap zoom delay)」(本次 fetch 的 `command.md`,远端未版本化文档,只引条款原文);`sidebar-nav.md`「Anatomy」把板块 trigger 定义为「chevron(展开转 90°)· 板块标题 · 计数」——与 FieldItem children disclosure 同一 anatomy(09-11 反向横扫表已记录);Nuxt UI `button.ts` / `link.ts` / `collapsible.ts` 生成主题均不含 `touch-action`,原语层不会兜底。
  - **severity**:low(仅触屏、仅快速连击;桌面与键盘无影响;与 09-11 SchemaComposition 同题同级)。
  - **修法**:分区 header `<button>`、搜索清除 `UButton`(经 `class` 透传到根 `<button>`)、行 `ULink` 覆盖层各加 `touch-manipulation`(共 3 处,同一文件);`sidebar-nav.spec.ts` 新增独立用例 `SidebarNav touch contract`,对全部 `button[aria-expanded]`、`a[aria-label]` 与出现 query 后的清除按钮断言 `classes()`(与 SchemaComposition 测试的 `forEach` 写法同款)。props / emits / slots / 行为契约均未变,非破坏性。
  - **未改 reference**:`sidebar-nav.md`「Accessibility」节本就不逐 class 记录触控细节,FieldItem / SchemaComposition 的同类契约也只在测试与源码注释里(09-11 同裁定)。
- **优化(复核无回归)**:`<nav :aria-label>` 地标;板块 trigger 是真实 `<button>`,`aria-expanded` / `aria-controls` 由 `@nuxt/ui` `Collapsible.vue` L32 `<CollapsibleTrigger as-child>` 合并进 slot 内按钮(`reka-ui` `CollapsibleTrigger.js` L29–31 实测输出 `aria-controls` / `aria-expanded` / `data-state`);搜索框 `aria-label` 剥省略号、`type="search"` + 四个 off 属性;`role="status"` polite live region 空 query 静默;stretched-link 结构(ULink 覆盖层 + `pointer-events-none` 内容层,`+N` 触发器 `pointer-events-auto relative z-10` 浮回);active 单源同时驱动 `:active` / 文字色 / `aria-current`;调宽把手 `role="separator"` + `aria-controls` 指回 nav 自身 id(`useAttrs` 保留消费方 id,否则 `useId()` SSR 稳定)。明暗:全部 `bg-elevated/40` / `bg-primary/10` / `text-toned` / `text-dimmed` / `border-default` 语义 token;尺寸:`w-14` 方法色标槽、`min-w-16` label 地板、`max-w-28` 未测量相 cap 均为系统 scale;响应式:`max-sm:hidden` UKbd、`max-lg:hidden` 把手、`lg:w-[var(--api-docs-nav-w)]` 仅 lg+ 生效。性能:`isItemActive` / `itemScenarios` 在 `resolved` computed 内每项一次(09-04),拖拽帧不触 router;SidebarScenarioTags 经 `useRafTask` 合帧、`document.fonts.ready` 后重测、`samples` 只按位数量 `+N` 宽度。
- **重构**:`<script setup>` + TS;`SidebarNavItem` / `SidebarNavSection` / `SidebarNavGroup` / `SidebarNavKind` 从 SFC 导出并有 JSDoc;`groups` 首选 + `sections` 向后兼容归一为单一渲染路径;命名符合 `component-reflow.md`(SidebarNav / ScenarioTags 取自成熟 UI 模式词)。
- **Universal claims 核证**:源码「links containing `#` never self-infer」「sighted users and screen readers always agree」由 `sidebar-nav.spec.ts` active 单源用例钉住;SidebarScenarioTags「a tag always renders WHOLE」(测量后无 cap)、「opening the scenario list never also navigates the row」(`pointer-events-auto` + `z-10`)由 `sidebar-scenario-tags.spec.ts` 与 08-2x 实测覆盖;reference「开合状态**始终受控**」——`UCollapsible` 恒传 `:open="isOpen(entry)"`,结构性成立。
- **dependency evidence(本次补齐)**:reference L112「板块用真实 `<button>` 触发(Reka `UCollapsible` 接好 `aria-expanded` / `aria-controls`)」这条 a11y 契约依赖 `@nuxt/ui` 把 `#default` slot 包进 `CollapsibleTrigger as-child`、以及 `reka-ui` 的 trigger 把 ARIA 合并到 slot 元素——两者此前都未绑定为 dependency evidence(09-11 对 SchemaComposition 的 `unmountOnHide` 前提已用同一机制绑定)。本次以 `{ name, reason }` 提交,recorder 从功能提交 `HEAD` 的 lockfile 解析精确版本(`@nuxt/ui` 4.9.0 / `reka-ui` 2.9.10);后续任一 resolution 变化只让本 owner stale,`reason` 把复审路由到板块 trigger 的 ARIA 与受控开合。
- **为什么没改看起来可改的**:
  - `'/'` 快捷键用手写 `window.addEventListener('keydown')` 而 SiteSearch 的 `⌘K` 走 Nuxt UI `defineShortcuts`——两者不是同一契约:本组件要按 `searchShortcut` prop 动态取键、跳过任何 `INPUT / TEXTAREA / SELECT / contentEditable` 与带修饰键的组合、并配合 IME `isComposing` 守卫,这些都被测试钉住;`defineShortcuts` 的 `usingInput` 语义与 `_` / `-` 组合键语法不等价,换实现会改行为、无规则依据。
  - `<li :key="item.to ?? item.label">` 在同一板块两项同 `to` 时会撞 key——gallery 与 DocsShell 数据无此情形,reference 也未承诺同 `to` 多项,不立 finding。
  - `resultsAnnouncement` 等函数型 prop 默认值——已是 i18n-ready 契约(reference props 表明文),不动。

### api-docs-code-rail(CodeRail)

历次:ADR-010 从 gallery 私有 recipe 晋升为 kit 切片、08-26 Shift 粗步进 + `computeSplitBudgets` claim 收窄(modified)、09-03 / 09-18 因 SplitPane / 页面消费点变化 affected 复审 ×2(另 09-18 的 #141 owner 复审补 DocsShellReference `resize-label`)。本次全量复审:

- **晋升 / 精简**:无。kit 归属理由(量自然高时耦合本 kit 代码卡内部 DOM `.code-surface` / `pre.raw-pre`,foundation 不应知晓)复核成立,reference L164 与源码头注一致;`registryDependencies` 6 项与 `GeistBreakpoint` 类型(`foundation-breakpoints`)、`useBreakpointGate` / `useSplitPane` / `useRafTask` / `SplitPaneHandle` 引用一一对应。341 行(audit-plan 计数)全部服务 split math + 测量:`computeSplitBudgets` 纯函数与 reference L183–197 列出的实现逻辑等价(reference 摘录是 `export` 单行签名 + 中文 JSDoc,源码是模块私有多行签名 + 英文 JSDoc;reference 明言「下面列出以便理解行为,不需要单独 copy」,摘录不是契约,不改 reference);RO + MO 双观察者的增量目标同步(`syncPair` / `syncContentTargets`)是 08-2x 修 pre 重建 / 语义面板互换时引入,三个用例锁定。
- **优化(复核无回归)**:SSR 恒 stacked(`useBreakpointGate` 起 false)→ 挂载后翻转;`topStyle` / `botStyle` 恒返回对象(hydration 不跳过 `undefined → object`);`measure` 经 `useRafTask` 合帧、MO 回调先 `syncContentTargets` 再 schedule;stacked 态**不渲染**把手(避免 `h-3` 占位撑大堆叠间距),fit 态把手 `disabled`(无 separator role、不可聚焦);aria 值 / 边界取**有效**分隔位与 `computeSplitBudgets` 在 0.2 / 0.8 两端重算的真实可达区间;`alignRatio` 在拖拽 / 步进前把存储 ratio 重锚定到有效位置(无死键)。无颜色、无圆角、无任意值(纯布局:`flex min-h-0 flex-col`);`HANDLE_PX = 12` 与 `SplitPaneHandle` 横向 grip `h-3` 一致。三处消费点内外层断点同步(endpoint / webhook 页 `lg` + `lg`、DocsShellReference `xl` + `xl`),各传独立 `storage-key`(`api-docs-endpoint-rail-split` / `api-docs-webhook-rail-split` / `docs-shell-rail-split`)与中文 `resize-label`。
- **重构**:`<script setup>` + TS;三个 prop 均为 `string` 字面量(MDC 友好);`enabledFrom?: GeistBreakpoint` 的取值域由 `foundation/utils/breakpoints.ts` 派生——与 09-18 对 SplitPane 的裁定相同,增删断点名会静默改掉公开 prop 契约,故本次把该文件纳入 scope(此前只在 SplitPane 的 scope 内)。
- **Universal claims 核证**:源码「the rail never claims a sticky viewport-height strip while the outer pane is still stacked」是**条件式**承诺(前提「keep in sync with the gate of the surrounding SplitPane」),三处消费点已同步;「two unnamed rails never fight over one stored ratio」——`useId()` per-instance 回退,同页多实例成立、跨刷新不持久也已在注释声明;「when both fit we render at natural height and never call it」——`budgets` computed 只在 `overflow.value` 为真时调用,结构性成立;「Styles are ALWAYS objects」——两个 computed 的 else 分支都是 `{}`。08-26 收窄后的「无空洞承诺以 `minPane` 地板为界」与 reference「各栏不低于 minPane」一致。
- **观察(不立 finding)——Home / End 的存储 ratio**:`onJump('min' | 'max')` 把 ratio 写成 `±Infinity` → `useSplitPane` clamp 到 0.2 / 0.8,而有效可达区间可能窄得多(spec rig 里是 63% ~ 70%)。用户看到的 aria 值与分隔位都取有效值,下一次拖拽 / 步进前 `alignRatio` 又会重锚定;唯一差异是 cookie 里持久化的是 0.2 / 0.8 而非有效端点——reference 只承诺「Home / End 跳到边界」,未承诺存储 ratio 的语义,且换成写有效端点会让同一 cookie 在内容不同的页面上落到不同位置,反而更难解释。不动。
- **为什么没改看起来可改的**:模板 `:label="props.resizeLabel"` 与同模板其它未加前缀的绑定(`enabled` / `dragging` / `topId`)风格不一——两种写法都合法,无规则依据,不为一致性制造 churn;`storageKey` 挂载时读取一次(09-03 对 SplitPane 的同一裁定:换存储槽语义,reference 未承诺可变);`Math.max(60, …)` 的 surface 最小高与 `MIN_PANE = 120` / chrome 关系——08-26 已核为有意的地板,不动。

## 反向横扫(簇外重复检查)

基线:`52e7f1d` 的 `extraction-candidates.md`(候选 1–9,无待决策)。对今日组件提取的特征模式全仓 `rg` 反查 `foundation/ kits/ app/components/`:

| 模式 | 命中 | 判定 |
|---|---|---|
| `touch-manipulation` tap 目标 | 分发面 8 处(kit 7 + foundation AnnotationPopover 1);SidebarNav 分区 header(L505)、搜索清除按钮、行链接是缺口 | 本日补齐;单一 utility class、无共享逻辑,不是候选 |
| stretched-link(`absolute inset-0` 覆盖层 + `pointer-events-none` 兄弟内容层) | 仅 SidebarNav;FieldItem / FieldValueStructure 的 `absolute inset-0` 是 09-15 已评估的 arrival-cue 装饰 overlay | 语义不同,不抽 |
| 隐藏测量层(`invisible absolute -z-10` + `data-tag` / `data-plus` 样本)+ `document.fonts.ready` 重测 | 仅 SidebarScenarioTags | 全仓唯一 |
| `MutationObserver` + `ResizeObserver` 增量目标同步(`syncPair`) | 仅 CodeRail | 全仓唯一 |
| `computeSplitBudgets` 内容优先重分配 | 仅 CodeRail | ADR-010 刻意留在 kit,全仓唯一 |
| 全局快捷键 `window.addEventListener('keydown')` | SidebarNav(`'/'`,含 typing / IME 守卫)、`useSplitPane`(拖拽中 Escape);SiteSearch `⌘K` 走 `defineShortcuts` | 键语义与守卫条件各异(见上「为什么没改」),不抽 |
| `isComposing` 守卫、ASCII `slug()` 回退 | 各仅 SidebarNav 一处 | 全仓唯一 |
| `router.resolve` | SiteSearch(导航目标归一化)、SidebarNav(active 推断) | 用途不同,不抽 |
| 受控 `UCollapsible` open map、`role="status"` live region 家族、`useId()` storage key 回退 | 09-11 / Round 2 / 09-18 已裁定 | 无新调用点,不重复评估 |

**登记表本次只追加 2026-09-19 扫描记录(无新候选);无待决策候选,不查询 / 不新建 GitHub issue。**

## Gate

完整 gate 跑了两次:第一次在 `54e2542`(全绿),复审修复后在最终功能提交 `a1ad0cc` 全量重跑(下表;本机负载 15–23,其它会话同时在跑 build,未出现超时,各项退出码均显式为 0):

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 58 files |
| `pnpm test:registry` | ✅ 143 pass / 0 fail |
| `pnpm typecheck` | ✅ exit 0 |
| `pnpm build` | ✅ postbuild root CSS check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 isolated closure smoke + legacy-config 迁移 smoke 全部通过(脚本逐项 fail-fast,最后一项 `isolated-api-docs-sidebar-nav` 通过) |
| `pnpm test:component` | ✅ 33 files / 430 tests(含新增的 `SidebarNav touch contract` 用例) |
| `pnpm audit:verify`(record 前,在 `54e2542` 上) | 按预期标出 3 个因 `SidebarNav.vue` 改动而 stale 的 owner(`api-docs-sidebar-nav` 计划项 + `api-docs-method-badge` / `foundation-split-pane-handle` required co-review),其余 26 项 landed |
| `pnpm audit:verify`(record 后) | ✅ 29 项 landed(含本次 2 计划项 + 2 co-review owner);legacy 0、deferred 0、未审计 0 |
| 分发物 U+FFFD 检查 | ✅ 无 |

聚焦 spec(`sidebar-nav` / `sidebar-scenario-tags` / `code-rail`)22 例在第一个功能提交前单独跑过一次;复审修复后 `sidebar-nav.spec.ts` 12 例(含新用例)单独跑过一次;全部通过。

### required co-review owner

功能 diff(`54e2542` + `a1ad0cc`)只改 `kits/api-docs/components/SidebarNav.vue` 与其 spec,前者同时在 `api-docs-method-badge` 与 `foundation-split-pane-handle` 的 base scope 内,recorder 推导两者为 required co-review owner,本次在同一 results 中提交完整复审(非 digest refresh):

- **api-docs-method-badge**:行 `ULink` 覆盖层加 class 不影响其兄弟内容层;HttpMethodBadge 在接口行的渲染(定宽 `w-14` 槽、`size="sm"`、`translate="no"` 透传链路)与 method 归一化契约均未触及;源码、reference L115 与 `http-method-badge.spec.ts` 复核一致;既有两条 dependency evidence(`@nuxt/ui` UBadge 单根 attrs 转发、`vue` 单根 fallthrough)前提未变,原样重绑。无 finding。
- **foundation-split-pane-handle**:SidebarNav 对把手的消费(L630–644)一行未动;把手的 ARIA / 键盘 / edge 形态焦点指示与四份 reference 逐条一致,`split-pane-handle.spec.ts` / `code-rail.spec.ts` 通过;09-18 的修饰键组合观察维持不立 finding。无 finding。

## 遗留与下一步

- 本批无 deferred finding、无人工决策点;09-11 报告留下的「SidebarNav 分区 header 按钮未显式设 `touch-manipulation`」线索本日闭合。
- 一条观察(CodeRail Home / End 的存储 ratio 语义)已写入 ledger notes,不建议改。
- **测试覆盖缺口(非本簇)**:FieldValueStructure(L206)、AnnotationPopover(L215)、SidebarScenarioTags(L192)的 `touch-manipulation` 未被任何 spec 锁定;SidebarScenarioTags 属本 item,但其 spec 以几何 stub 测 fit 循环、`+N` 触发器只在溢出态出现,本次未为一个 class 扩 rig;下次审计 `api-docs-field-item`(FieldValueStructure 随其分发)/ `foundation-annotation-popover` 时顺手补 `classes()` 断言。
- 既有 scope 缺口(09-17 / 09-18 记录,非本簇):`api-docs-field-item` / `api-docs-code-block` / `api-docs-enum-table` 的 scope 尚未含 `webhook-reference.vue`,`foundation-inline-markdown` 未含 gallery 参考页,下次审计相应 owner 时纳入。
- Round 4 剩余 14 个组件按依赖簇继续。

## #144 阶段 2：中性文字契约 affected 复审

本节属于普通功能 PR #148 的受影响 owner 复审，不消费每日审计排程。上述 #146 记录原样保留。

- canonical base：`d126cfe1f6a0551c42738c7fb1d13582de2abc2a`。
- 颜色实现与七阶段验证快照：`172aec714beb448eb01d441eb41c91a0d263d795`（已纳入 #146）。
- 维护者采纳的是候选 A 的实施方向；本次正式实现仍待独立任务复审和最终视觉验收。
- 不合并、不启用 auto-merge、不关闭 #144/#140/#147；未主动发布、部署或更新外部业务 consumer。

### 改动及边界

采用 #144 确认的五个正常文字角色映射，四个中性背景和 token 名称保持原契约。FieldItem / EnumTable 计数去除 `/70`；FieldItem / FieldValueStructure 展开 hover 去除 `/75`，保留完整 primary 并增加下划线。Card、Empty、PageCTA、PageCard 的 solid description，以及 PricingPlan 的 description、discount、billingCycle、billingPeriod、featureTitle、tagline、terms 仅在各自 `variants.variant.solid` 配置 `text-inverted`。未添加全局 description 覆盖，neutral Alert `opacity-90` 保留。

Source checker 直接读取生产 CSS，支持完整 5×4×2 配对并拒绝未知语法、条件覆盖、非法变量和颜色；consumer checker 读取实际复制的 CSS。独立 Node E2E 通过真实 Nuxt 生产构建和 Chromium 验证，不继承 runtime/mountSuspended 配置。测试 fixture 不进入 registry 或普通 gallery 路由。

浏览器保留未舍入 ratio、有效前景/背景及合成层；覆盖 solid/非 solid、表单 placeholder/readonly、portal、Tabs indicator、真实 arrival 峰值、两处计数和两个展开 owner、三个真实 API Docs 页面。未知绘制层、零节点、未触发状态均失败。只有原生 disabled 样本另列例外；readonly、placeholder、未选中与可见 aria-hidden 信息均无自动豁免。

### 已执行的验证

| 验证 | 结果与绑定 |
| --- | --- |
| `pnpm test:registry` | 156/156，含独立 Node 对比度 13/13；功能 HEAD `172aec7` |
| `pnpm test:agent` | 20/20；功能 HEAD `172aec7` |
| `pnpm registry:validate` | 40 items / 58 files；功能 HEAD `172aec7` |
| 生产 CSS 配对 | 40/40，最低 raw `4.795893581026063` |
| `pnpm test:component --maxWorkers=2` | 33 files / 430 tests，集成 HEAD `f61a4e2`；到 `172aec7` 仅浏览器报告写入顺序变化 |
| `pnpm typecheck`、`pnpm build` | 功能 HEAD `172aec7` 通过；正常 root CSS 检查 8 markers |
| 首轮 `pnpm build:contrast && pnpm test:browser` | `172aec7` 全部 22/22，650 条正常记录，最低 raw `4.795896291213259`；source digest `1893204ac217bc2bec3920683ea8d54094cdba3c6695c59436ded3d589594de3` |
| 临时 fresh / update consumer | 独立安装依赖在 `f61a4e2` 通过；`172aec7` 再用已安装依赖通过 all-items copy-in 和 `52e7f1d→172aec7` upgrade，均检查 dry-run / plan digest / guarded write / 实际 CSS / typecheck / build |
| required CI 配置 | 实时 ruleset 确认仍为 `Verify Source-first root`、`Verify consumer (runtime)`、`Verify consumer (isolated)`；浏览器接入既有 root，不改权限或合并规则 |

交付前的干净 CI 安装暴露独立 TypeScript 配置未直接声明 Node 类型依赖（TS2688）。随后仅把 lockfile 已有的 `@types/node@26.1.1` 声明为直接 devDependency，增加 importer 条目，未改变任何包的解析版本或浏览器断言。七阶段证据仍如实绑定 `172aec7`；此类型入口修复由后续干净安装与最终 CI 验证，不将旧实验重标成新 SHA。

所有最终截图来自已实施 A + 局部修正。Chromium `153.0.8010.12` 的 CDP platform-font readback 确认实际 Geist-Regular / GeistMono-Regular 为 custom font；不以 FontFace 声明或阶段1 fallback 截图替代。详细场景 JSON、截图与 trace 已保留在本地最终证据目录；CI 已配置上传其当次常规浏览器产物，本地完整反例包的公开附件上传仍待授权。首次实施原始命令由 `scripts/verify-contrast-mutations.mjs` 重现。

### 五项隔离源码变异

| 阶段 | browser exit | light 目标 raw ratio | dark 目标 raw ratio |
| --- | ---: | ---: | ---: |
| 00-green-A | 0 | 通过 | 通过 |
| 01-old-tokens | 1 | 2.377862252998539 | 4.049687373007745 |
| 02-field-count-alpha | 1 | 3.0373173081104894 | 3.693082464512101 |
| 03-enum-count-alpha | 1 | 3.0373173081104894 | 3.693082464512101 |
| 04-field-hover-alpha | 1 | 3.6490308932088684 | 3.7838035704093707 |
| 05-value-hover-alpha | 1 | 3.6490308932088684 | 3.7838035704093707 |
| 06-green-restored-A | 0 | 通过 | 通过 |

七阶段全部完成，原始目录标识 `geist-contrast-mutations-S9dPIa`。首尾均 Node 13/13、browser 22/22；旧 token 的 Node 同命令 exit 1，复现 10/40 失败。

每项先恢复原始四个文件，再只改一个指定 owner，重新 prepare/build，执行完全相同的 `pnpm test:browser`。source SHA/digest、单文件 diff、生成组件真实路径及全场景报告集合均校验；旧 token 另外执行同一 `node --test tests/text-contrast.test.mjs`。测试测量 JSON 只有在 trace 保存和 context 清理成功后才写出，防止基础设施错误被已有颜色记录掩盖。旧 `34f4f55` 中途运行仅保留诊断，不作为最终交付证据。

### owner 结论

正式 recorder 使用 `--affected --base d126cfe1f6a0551c42738c7fb1d13582de2abc2a`，从已提交 `172aec7` 生成 evidence。保留全部既有 scope、5 条 closed findings 和既有 dependency reasons；CodeRail 保留 #146 新增的 `foundation/utils/breakpoints.ts`。SidebarNav 不属于本轮 19 个 required owner，其 entry 与依赖证据原样保留；全局 round/lastRunAt/lastPicked 不变。

12 项在本次变更范围内 verified：api-docs-code-rail、api-docs-enum-table、api-docs-event-badge、api-docs-field-group、api-docs-operation-target、api-docs-relation-source-path、api-docs-webhook-protocol、foundation-annotation-popover、foundation-inline-code、foundation-split-pane、foundation-split-pane-handle、foundation-theme-toggle。

7 项 deferred，各保留 #147 open finding：api-docs-field-item、api-docs-lifecycle-badge、api-docs-method-badge 使用阶段1已确认且本轮未修复的功能色失败；api-docs-operation-header 直接组合已知失败组件；api-docs-field-annotation、api-docs-lifecycle-notice、api-docs-site-search 则是源码确认的相关消费，尚需独立浮层/状态测量。后四项不得把其他节点的 ratio 冒称自己的本轮实测。

这里的 verified 仅表示本次 affected scope 复审完成，不是每个组件所有颜色状态或整页 WCAG 合规声明。F1–F9 仍由 #147 跟踪并阻止 #140 关闭。独立任务复审和维护者最终视觉采纳尚未完成。
