# 2026-09-07 组件审计(第二次复核 PR #115)

- **Round**:3(`roundRollover: false`,无轮末横切扫描)
- **Base main SHA**:`5353f73a98db06c88a04da6749baf950a1a5749e`(与 09-05 / 09-06 相同,`origin/main` 三日未前进)
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>` → `api-docs-site-search`、`api-docs-operation-header`、`api-docs-event-badge`;`openFindingReentry` / `priorityBacklog` 均为空
- **今日分流**:计划项与仍开放的 [PR #115](https://github.com/abel-2333-org/geist-nuxt/pull/115) 第三天完全重叠。按审计协议 §1 在 `chore/component-audit-2026-09-05` 分支上续审,**不新建同类 PR、不重复 `--record`**。rebase 到 `$MAIN_SHA` 为 no-op,四个既有提交原样保留(`140c5e7` 功能 / `9e72d0f` 簿记 / `166036a` 复审修正 / `3fb379d` 09-06 复核)。

## 今日做了什么(与 09-06 不重复的部分)

09-06 已核证 `wrap-anywhere` utility、UCommandPalette `empty` 槽主题前提、报告数字与 SHA、测试断言、preview 浏览器实测。今日以新 session 从两条 09-05 / 09-06 未显式覆盖的角度补核:

| 核证项 | 结果 |
|---|---|
| **OperationHeader 基线行里徽章的收缩地板**:`items-baseline` 行内 `UBadge` 根节点没有 `shrink-0`(`.nuxt/ui/badge.ts` 只给 icon / avatar 槽),`<code>` 改 `wrap-anywhere` 后 min-content 极小,徽章是否会被挤压 | ✅ 不会。flex item 默认 `min-width:auto` = min-content,徽章 label 是不可断的单词,地板即完整 label;`<code>` 承担全部收缩。改前 `truncate` + `min-w-0` 同样由 code 收缩,徽章行为无变化(09-06 preview 实测徽章完整落首行,与此一致) |
| **warn 断言的 spy 生命周期与来源唯一性**:两条 reject 用例 `toHaveBeenCalledOnce()` 是否可能被 `searchGroupLabel` 缺失 warn 污染 | ✅ 两条用例的 `mountSearch` 均传 `searchGroupLabel: 'Content'`;`afterEach` 含 `vi.restoreAllMocks()`,spy 不跨用例泄漏 |
| **簇外反查(新角度:「分发件截断唯一身份出处」)**:`grep -rn truncate foundation/ kits/` | 命中 SidebarNav(板块标题 L520、条目 label L595)、CodeBlock 工具栏标题 L182、ResponseExample 二进制卡文件名 L625。判定均**不同类**:SidebarNav 行是导航列表,必须单行,label 有 `min-w-16` 可读地板、完整身份在目标页与 ⌘K 结果中重复;CodeBlock 标题下方即代码本体;ResponseExample 文件名卡的下载链接承载完整名。OperationHeader 的特殊性是「webhook 无 OperationTarget 兜底、header 是事件名唯一出处」,其余三处不满足该前提,不记 finding |
| 既有 open finding | 无;`api-docs-site-search` 的 `f-dd631c5b3977` 保持 `resolved` |

**结论:无新 finding,evidence head 保持 `140c5e7`,不重新 `--record`。** 今日新增提交仅本报告。

## Gate(rebase 后重跑)

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 56 files |
| `pnpm test:registry` | ✅ 131 pass / 0 fail |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ root CSS source check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 isolated closure smoke 全部通过 |
| `pnpm test:component` | ✅ 31 files / 343 tests |
| `pnpm audit:verify` | ✅ 29 个 scope-v1 条目证据全部成立;legacy 0、deferred 0、未审计 0 |

与 09-05 / 09-06 逐项一致。preview 部署 SHA 自 09-06 起未变(`3fb379d`),浏览器实测不重做,沿用 09-06 的 375px / 1440px 数值与截图证据。

## 遗留与人工决策点

- **PR #115 已连续三天阻塞审计轮转**:main 上的 ledger 不含 09-05 结论,计划算法每天都会给出同一簇;Round 3 剩余 7 个组件在 PR 合并前不会被审到。CI 全绿、`MERGEABLE`、两轮独立复审均无新 finding,建议尽快评审合并。
- **OperationHeader 视觉取舍**(不变):长标识由单行截断改为多行换行、徽章基线对齐;API / slots 未变,评审时确认接受。
- **gallery 手写 h1 header 镜像**(不变):9 处仍 `truncate`,建议在下次触碰这些页面的功能 PR 里用 `--record --affected` 一并对齐。
- merge、转 ready、auto-merge 全部等人工。
