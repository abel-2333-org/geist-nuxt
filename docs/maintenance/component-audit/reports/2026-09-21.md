# 2026-09-21 组件审计(复核 PR #149)

- **Round**:4(`roundRollover: false`,不执行 §5b 轮末横切;`remainingInRound: 12`)
- **Base main SHA**:`d126cfe1f6a0551c42738c7fb1d13582de2abc2a`(与 09-20 相同,`origin/main` 未前进)
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>` → `api-docs-site-search`、`api-docs-operation-header`、`api-docs-lifecycle-badge`(561 LOC),与 09-20 完全一致;`openFindingReentry` / `priorityBacklog` 均为空
- **今日分流**:计划簇与仍开放的 [PR #149](https://github.com/abel-2333-org/geist-nuxt/pull/149)(`chore/component-audit-2026-09-20`)完全重叠。按审计协议 §1 在该分支上**继续/复核**,不新建同类 PR、不重复 `--record`。分支 merge-base 已是 `$MAIN_SHA`,rebase 为 no-op,四个既有提交原样保留(`3f1540e` / `014c1c8` 功能、`a11f91f` / `2f82d87` 簿记)。开放 PR 另有 #148(Codex 的 #144 中性文字对比度 Draft),不是 audit PR,不触碰
- **结论**:从 09-20 未覆盖的角度独立复核,**无新 finding**;完整 gate 在分支 HEAD 重跑全绿(见下);**ledger 不重录**;本文件只记录复核过程,并向 PR #149 追加一轮 caveman 复审

## 为什么不重录 ledger

与 08-22 / 08-23 / 09-07 的复核日理由相同:计划 item、功能提交(evidence `headSha = 014c1c8`)、registry 拓扑与 scope 内容均未变化,`pnpm audit:verify` 在分支上重算 29 项 landed;重录只会刷新 `lastAuditedAt` / `lastRunAt`,且 recorder 会把 evidence `headSha` 重绑到当时的 `HEAD`(复核日的 `HEAD` 是簿记提交),让证据指向一个纯 docs 提交而非功能快照,反而弱化证据;属无谓 churn。

## 独立复核(本次只做 09-20 未覆盖的部分)

09-20 已完成四维度全量复审并经三轮 caveman 复审收敛(源码、`@nuxt/ui` 4.9.0 上游行为、spec、gallery 消费面、universal claims)。今日以新 session 从三条它未能或未显式覆盖的角度补核:

### 1. 真实浏览器实测(09-20 因无人值守无法启动 preview 而缺失)

以 PR 分支的 Vercel preview(`geist-nuxt-gallery-git-chore-compon-1c937b-…vercel.app`,对应 delivery head `2f82d87`;本日簿记提交只增 docs,代码 / tests / ledger 不变,取证结论不受影响)在内置浏览器上核证;pane 不可见时只能用 DOM / computed style 与程序化事件,不能真实点击、截图或注入键盘。

| 核证项 | 结果 |
|---|---|
| `/kits/api-docs/docs-shell` 顶栏 SiteSearch 触发按钮 | ✅ 根 `<button>` class 含 `touch-manipulation`,computed `touch-action: manipulation`(本 PR 唯一功能改动在真实 DOM 落地);`aria-label="搜索全部文档"`,可见文本「搜索全部文档 ⌘K」;挂载时 `role="status"` polite 区静默 |
| 打开面板 | ✅ `role="dialog"` 由 `aria-labelledby` 指向「搜索全部文档」(UModal `#content` 模式经 `VisuallyHidden > DialogTitle` 命名);input `aria-label="搜索全部文档"`、`autocomplete="off"`、placeholder「搜索指南与接口…」,初始焦点落在 input;`body` `overflow: hidden`(Reka modal Dialog 的 body scroll lock);空查询 2 个 group / 15 个 option |
| 零结果查询 `zzzzqqq` | ✅ 0 option;可见空态「没有匹配的结果」与 live region「没有与“zzzzqqq”匹配的结果」**分别**渲染,不重复(reference「零结果文本由 `noResultsAnnouncement(query)` 生成」) |
| 有结果查询 `payment` | ✅ 1 个 option(`支付成功 · payment.succeeded`),live region 清空(reference「有任何可用 option … 保持静默」) |
| 375px 视口 | ✅ 触发按钮折叠为 28×28 icon-only(`max-sm:hidden` 的文字与 kbd 两个 span 均 `display: none`,`max-sm:px-1.5` 生效 → padding 6px),accessible name 仍为「搜索全部文档」;`<header>` 与 `document` 均无横向溢出。28px ≥ WCAG 2.5.8 Target Size (Minimum) 的 24px;不宣称 AAA 44px |
| `/kits/api-docs` 六态 LifecycleBadge(light / dark) | ✅ 六个徽章均 icon + 文本;light 下 class 为 `bg-success/10 text-success ring-success/25`(new / active)、warning 同形(beta / maintenance)、`bg-elevated text-default ring-accented`(deprecated)、`bg-error text-inverted`(sunset);dark 下 computed 色值随语义 token 切换(success `rgb(108,218,117)`、warning `rgb(244,183,64)`、deprecated `rgb(237,237,237)` on `rgb(31,31,31)`、sunset `oklch(0.205 0 0)` on `rgb(248,114,117)`),无硬编码色 |
| `/kits/api-docs` 两个 OperationHeader | ✅ endpoint / webhook 各一;标题按 `:heading-level="4"` 渲染为 `<h4>`,`<code translate="no">` computed `overflow-wrap: anywhere`,identity 行 `align-items: baseline`,endpoint 形态的 lifecycle 徽章「Active」紧跟标题 |

**实测限制(不计 finding)**:程序化 `KeyboardEvent('keydown', { key: 'Escape' })` 在真实浏览器未触发 Reka dismiss layer 关闭面板,因此 Esc 关闭与焦点回到触发按钮未在浏览器复现。Esc 关闭面板由 `site-search.spec.ts`「clears immediately on close and ignores a late request」在 vitest 下锁定(Escape 后 live region 清空只能经 `open → false` 发生);**焦点回到触发按钮全仓没有断言**,它是 Reka Dialog `onCloseAutoFocus` 的默认行为,本组件只在同页 hash 交接时 `preventDefault`,列为未验证项。dark 模式实测是在 `documentElement` 上临时切换 `dark` class 读 computed 值后立即恢复,未改变站点持久偏好。

### 2. 09-20 未显式核证的上游前提

| 核证项 | 结果 |
|---|---|
| `UModal` 无 `description` 时的 Reka 警告 | ✅ 不会。`Modal.vue` L89 在无 `description` prop / slot 时渲染空 `DialogDescription`,`DialogContent` 的 `aria-describedby` 有落点,不触发 Reka「Missing Description」警告 |
| Web Interface Guidelines「modals 内 `overscroll-behavior: contain`」 | ✅ 不是缺口。`.nuxt/ui/command-palette.ts` 的 `viewport` 只有 `overflow-y-auto`(computed `overscroll-behavior: auto`),但 `UModal` 默认 `modal: true`,Reka 对 `body` 施加 scroll lock(桌面 Chromium 实测 `overflow: hidden`),列表滚到底不会链到页面;上游主题面,组件不重复声明。**限定**:该结论只在桌面 Chromium 下实测;触屏(尤其 iOS Safari 对 `body overflow: hidden` 的橡皮筋 / 滚动链处理)未验证 |
| `UKbd` 对 `shortcutKeys` 的渲染 | ✅ `useKbd` 把 `meta` 映射为 macOS `⌘` / 其它平台 `Ctrl`;`shortcutKeys` 只把单字符大写(`k` → `K`),`UKbd` 原样显示;`v-for :key="key"` 在 `meta_k` / `ctrl_shift_k` 等合法组合下不重复 |
| `UButton` base 无 `touch-action` | ✅ `.nuxt/ui/button.ts` base 不含 `touch-action`,`touch-manipulation` 仍由组件自持(09-20 结论复核成立) |
| 依赖证据前提 | ✅ `pnpm-lock.yaml` 仍解析为 `@nuxt/ui@4.9.0`、`fuse.js@7.4.2`、`vue@3.5.39`,四条 dependency evidence 的内容标识未变 |

### 3. 反向横扫

`origin/main` 与簇均与 09-20 相同,三项 owning file 与 09-20 审计终态(`014c1c8`)同 blob——其中 `SiteSearch.vue` 相对 base main 含本 PR 的触控契约修复(`6ac1946` → `5137c0f`),另两项与 base main 同 blob;09-20 逐项比对的特征模式与命中集合无变化,结论原样成立;登记表只追加一行 2026-09-21 覆盖记录,无新候选、无待决策候选,不查询 / 不新建 GitHub issue。

## 结论一览(与 09-20 一致,ledger 不变)

| item | change | status | 今日复核 |
|---|---|---|---|
| `api-docs-site-search` | modified | verified | 功能改动在真实 DOM 落地;dialog 命名、三态播报、375px 折叠实测成立;无新 finding |
| `api-docs-operation-header` | none | verified | `heading-level` / `wrap-anywhere` / baseline 对齐实测成立;无新 finding |
| `api-docs-lifecycle-badge` | none | verified | 六态 light / dark 实测 icon + 文本齐全、全走语义 token;无新 finding |
| `api-docs-method-badge` | modified(co-review) | verified | 面板 option 的 `#item-leading` 链路未变;无新 finding |

## Gate(分支 HEAD `2f82d87`,功能快照 `014c1c8`)

本机负载 4–5,其它会话同时在跑;各步退出码显式为 0:

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 58 files |
| `pnpm test:registry` | ✅ 143 pass / 0 fail |
| `pnpm typecheck` | ✅ exit 0 |
| `pnpm build` | ✅ postbuild root CSS check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 closure smoke(30 个 isolated + `all-items` / `all-items-path-prefix-false` + 10 个 per-item foundation+kit 闭包)全部通过,`--sha 2f82d87` 一致性通过(首轮 gate 日志每步只留尾 40 行,未截到逐 closure 计数,故单独全量重跑一次 `pnpm test:consumer` 取数,exit 0) |
| `pnpm test:component` | ✅ 33 files / 431 tests |
| `pnpm audit:verify` | ✅ 29 项 landed(含本簇 3 计划项 + 1 co-review owner);legacy 0、deferred 0、未审计 0 |
| 分发物 U+FFFD 检查 | ✅ 无(`rg -l $'\\uFFFD' foundation kits` 为空) |

## 复审循环(caveman-review,opus sub agent)

本日在簿记提交前后各跑一轮(opus sub agent,只读复审 `git diff d126cfe` + 未提交簿记),仍受 §0 约束。

**第 1 轮**(对象:已提交四个提交 + 今日未提交的报告与登记表行):无 🔴;1 条 🟡、5 条 🔵,逐条核证后处置:

- 🟡 采纳(本文件 §1「实测限制」):原文把 Esc 关闭归到「keeps one persistent polite region silent…」用例——该用例 Escape 后只断言 live region 仍为空,Escape 无效也会通过;真正依赖 `open → false` 的是「clears immediately on close and ignores a late request」;且焦点回到触发按钮全仓无断言。已改引正确用例,并把焦点回归明确列为未验证项。
- 🔵 采纳(§3 与登记表新行):「三项 owning file 与 09-20 同一 Git blob」与紧邻的 09-20 行并读会读成「SiteSearch 自 09-05 未变」;改为「与 09-20 审计终态 `014c1c8` 同 blob,其中 `SiteSearch.vue` 相对 base main 含本 PR 修复」。
- 🔵 采纳(§2 overscroll 行):结论限定为桌面 Chromium 实测;触屏(iOS Safari 对 `body overflow: hidden` 的滚动链处理)列为未验证。
- 🔵 采纳(「为什么不重录」):补「recorder 会把 evidence `headSha` 重绑到当时 `HEAD`(簿记提交),弱化证据指向」,这是不重录的更强理由。
- 🔵 采纳:登记表新行分句统一为全角 `；`;`REVIEW_PLACEHOLDER` 由本节替换。
- ✅ 复审已核对:计划输出与 L3 / L5 一致、merge-base = base、四提交与描述一致;lockfile 三个版本;`Modal.vue` L89 / `command-palette.ts` / `button.ts` / `useKbd.js` 四条上游前提;实测表引用的源码(trigger class、两个 `max-sm:hidden` span、`DocsShell.vue` aria-label、payments 域 2 group / 15 item、`payment` 只命中一条、六态 preset、OperationHeader 三个 class、gallery 两个实例);gate 抽查实跑(40 / 58、143、33 / 431、29 landed、U+FFFD 空、consumer 42 = 30 + 12 分解、8 markers);不重录理由对照 README evidence 模型成立;已提交部分无新真实缺陷。

**第 2 轮**(对象:push 后的分支 HEAD `1f9a4e0` + 更新后的 PR 描述):无 🔴 / 🟡;3 条 🔵 全部采纳(本节第 2 轮记录改为实写;「为什么不重录」中「现在是簿记提交 `2f82d87`」在新提交后失真,改为「复核日的 `HEAD` 是簿记提交」;gate 与浏览器实测取证钉在 `2f82d87`、本日提交 docs-only 的说明补进 §1 与 PR 描述)。复审已核对:第 1 轮 6 条全部落地(Esc 用例引用、`SiteSearch.vue` blob `6ac1946` → `5137c0f` 与另两项 base / head 同 blob、overscroll 限定、`audit.mjs` L1065 record 确以 `git rev-parse HEAD` 写 `evidence.headSha`、登记表全角分句);`1f9a4e0` 仅 +94 行 docs,计划元数据与 ledger 四项 evidence 实跑一致;PR 描述三个 SHA 与 `gh api` 一致;工作区干净、远端 head = 本地 HEAD。

**第 3 轮**:只核第 2 轮三条修正是否落地,结果见 PR 复审评论。

## 遗留与下一步

与 09-20 相同,无新增:

- **`touch-manipulation` 契约的覆盖面与文档化(交人工)**:references 无该条款;分发面另有 8 个组件的 `UButton`(CopyButton / CodeBlock / ResponseExample / WebhookProtocol / FieldAnnotation / DocAnnotation / TermAnnotation、AnnotationPopover 的 `#error` retry 按钮)与 gallery 外壳的汉堡 / 域切换 `UButton` 未纳入;建议人工二选一:写进 `references/foundations/focus-a11y.md` 并逐 owner 补齐,或收窄 SidebarNav 的注释。
- **09-05 遗留**:gallery 手写 h1 identity header 9 处 `min-w-0 truncate` 与 kit `OperationHeader` 的「wrap, don't truncate」相反,页面私有 recipe,建议人工功能 PR 顺手对齐并 `--record --affected`。
- 既有 scope 缺口(09-17 / 09-18 / 09-19 记录)下次审计相应 owner 时纳入。
- PR #149 合并前,Round 4 的后续计划会持续重复本簇;建议人工尽快评审合并。

## touch 契约统一 + gallery identity 换行:受影响 owner 复审

功能提交 `d374ce4`(`fix: 分发组件 tap 目标统一 touch-manipulation 契约,gallery 参考页标识改为换行`)+ 第 1 轮复审后的 `24c7d42`(补齐两处扫描盲区、钉死条款边界、闭合 09-19 三处 spec 锁定遗留)+ 第 2 / 3 轮复审后的 `92f87c5`(7 个 USelect 触发器纳入、carve-out 举例改为 UTabs、条款与注释真正写入),均基于 `9b67249` = 合并 #149 后的 main;ledger evidence 绑定 `92f87c5`;本节随 `--record --affected` 的 ledger 更新作为第二个提交。不推进每日排程 round。

- **起因**:#149 把两条「交人工」事项列为遗留,维护者决定按建议处理:先合 #149,再开一个 follow-up PR 把两项一起做、共用一次 `--record --affected`。
- **规则**:`references/foundations/focus-a11y.md`「无障碍硬要求」新增「触控 tap 目标」条款——分发组件里**每个最终渲染为 `<button>` / `<a>` 的元素**都加 `touch-manipulation`,判定看渲染结果、不看有没有 `@click`(仅 hover / focus 触发 tooltip 的记号按钮同样算),含原生标签、stretched-link 覆盖层、`<component :is>` 与 render function 生成的链接、经 `class` 到达根元素的 `UButton` / `ULink` / `UColorModeButton`;两类明示例外:拖拽把手 `touch-none`(`SplitPaneHandle`),以及 Nuxt UI 原语**内部**生成、`class` 到不了的非根交互元素(`UNavigationMenu` 菜单项、`UTabs` trigger)由上游主题决定,根即触发器的原语(`USelect` trigger、`UColorModeButton`)照常纳入;spec 用 `classes()` 锁定。此前它只存在于 SidebarNav 源码注释与 09-11 / 09-19 / 09-20 三次 finding 修复的先例里。第一版条款按枚举写、扫描也按字面标签正则(`<button|UButton|ULink|NuxtLink|a href|UColorModeButton>`)跑,第 1 轮复审指出两处盲区——`RelationSourcePath` 的 `<component :is>` 根链接与 `InlineMarkdown` render function 生成的 ProseA 链接——于是把条款改成结构性定义并把两处补齐;扫描器其余命中(OperationTarget 两段经 `:class="[segment, …]"` 绑定、若干源码注释)为误报。
- **补齐的 tap 目标(23 处 / 15 个组件)**:CopyButton(tooltip / 非 tooltip 两分支)、CodeBlock 换行开关、ResponseExample 紧凑触发器与文件下载 `<a download>`、WebhookProtocol 节奏展开、FieldAnnotation `#actions` ×2、DocAnnotation / TermAnnotation `#actions`、AnnotationPopover `#error` retry、ThemeToggle 的 `UColorModeButton`(它把 `class` 声明为 prop 并经 `useForwardProps` 转发到根 UButton,不是 `$attrs`)、AppHeader 品牌 `NuxtLink`、FieldItem 的 `?` 可省略记号按钮(09-19 曾以「触屏不开 tooltip」豁免,新条款按渲染结果判定,不再留例外)、RelationSourcePath 的 `<component :is>` 根链接、InlineMarkdown 的 ProseA 链接,以及第 2 轮复审指出的 7 个 `USelect` 触发器(ResponseExample ×4、CodeBlock / RequestExample / OperationTarget 各 1——`USelect` 把 `class` 交给 `SelectTrigger` 根 `<button>`,按条款判据属于纳入范围,不能当 carve-out 例子)。对应 spec:`copy-button` 新增 it.each、`code-block` / `webhook-protocol` / `response-example`(紧凑触发器 + 新增 file panel 下载用例)/ `annotation-forms`(Term / Doc / Field 三处)/ `annotation-popover`(retry + 新增触发器用例)/ `field-presence`(`?` 记号)/ `relation-source-path`(两种链接态)/ `inline-markdown`(内部链接)各补 `classes()` 断言,新增 `theme-toggle.spec.ts`,`app-header.spec` 新增品牌链接用例。顺手闭合 09-19 报告留下的三处 spec 锁定缺口:AnnotationPopover 触发器、FieldValueStructure 展开按钮(`field-value-structure.spec`)、SidebarScenarioTags `+N` 触发器(`sidebar-scenario-tags.spec`)。
- **gallery identity 换行(9 处)**:`endpoint-reference.vue` ×3、`webhook-reference.vue` ×3、`DocsShellReference.vue` ×3 的手写 h1 identity `<code class="min-w-0 truncate …">` → `wrap-anywhere`,与 09-05 对 kit `OperationHeader` 的修法一致(responsiveness.md「永远保护主标识」),375px 下不再丢标识尾部。页面私有 recipe,kit 契约零改动。
- **未纳入**:app/ 下 gallery 外壳自身的 tap 目标(`DocsShell` 汉堡 / `DocsDomainSwitcher`)——新条款明确限定「分发组件」,demo shell 是 copy-and-adapt recipe,不随 registry 分发,不强加同一硬要求;需要时由 gallery 侧单独对齐。
- **验证**:完整 gate 在 `d374ce4` 跑通一次;`24c7d42` 上 gate 一轮 + `test:registry` 去变量重跑一次;最终功能提交 `92f87c5` 上再全量一轮(下为最终数字):`registry:validate` 40 items / 58 files、`test:registry` 143 pass / 0 fail、typecheck exit 0、`test:consumer` 42 closure smoke + legacy-config 迁移 smoke、`test:component` 34 files / 439 tests(含新增 `theme-toggle.spec.ts` 与本次补的 `classes()` 断言)。`pnpm build` 前三次退出 1 均为 `@nuxt/fonts` 下载 `fonts.gstatic.com` 的 `UND_ERR_CONNECT_TIMEOUT`——本机经本地代理上网,`curl` 遵守 env proxy 而 Node 22 的 undici `fetch` 默认不读,与代码无关;改用 `NODE_USE_ENV_PROXY=1 pnpm build` 后通过(8 markers)。record 后 `pnpm audit:verify` 全部 landed。
- **recorder 推导 23 个 owner**(第 1 轮复审修复后新增 `api-docs-relation-source-path`、`foundation-inline-markdown`、`api-docs-sidebar-nav`;`api-docs-code-block`、`api-docs-code-rail`、`api-docs-event-badge`、`api-docs-field-annotation`、`api-docs-field-group`、`api-docs-field-item`、`api-docs-lifecycle-badge`、`api-docs-method-badge`、`api-docs-operation-header`、`api-docs-operation-target`、`api-docs-request-example`、`api-docs-response-example`、`api-docs-webhook-protocol`、`foundation-annotation-popover`、`foundation-copy-button`、`foundation-doc-annotation`、`foundation-split-pane`、`foundation-split-pane-handle`、`foundation-term-annotation`、`foundation-theme-toggle`),按命中原因三类复审:① owning file 被改的 14 个(code-block / response-example / request-example / operation-target / webhook-protocol / field-annotation / field-item / annotation-popover / copy-button / doc-annotation / term-annotation / theme-toggle / relation-source-path / inline-markdown)逐一核对只增 class 与 spec 断言、props / emits / slots / 行为契约未变;② 只因 gallery 参考页 identity 换行命中的(code-rail / event-badge / lifecycle-badge / method-badge / operation-header / operation-target / request-example / split-pane 等)核对该页对组件的调用参数与用法未变;③ 只因 `focus-a11y.md` 命中的 split-pane-handle(条款明文排除 `touch-none` 把手)、经 WebhookProtocol 命中的 field-group、经 `sidebar-scenario-tags.spec` 命中的 sidebar-nav 核对与新条款一致。`foundation-theme-toggle` 的 scope 补入新建的 `theme-toggle.spec.ts`(锁定该契约的唯一文件不能落在 scopeDigest 之外)。open finding 为 0;resolved / wont-fix finding 由 recorder 自动沿用(`mergeFindings` 只要求显式携带 open finding,wont-fix 在 results 中重复提交会触发 `--allow-wont-fix` 守卫),既有 dependency evidence 原样重绑。
- **复审(caveman-review,opus sub agent)**:第 1 轮 6 条 🟡 全部采纳——两处扫描盲区(RelationSourcePath / InlineMarkdown)补齐并把条款改为结构性定义;ThemeToggle 注释与 spec 注释「经 `$attrs`」改为「`class` prop 经 `useForwardProps` 转发」;报告「无剩余缺口」重述;theme-toggle.spec 纳入 scope;09-19 三处 spec 锁定遗留闭合。2 条 🔵 采纳(计数 11 → 16 处、FieldItem `?` 注释补充 class 来源)。2 条 ❓ 采纳写进条款(判定边界按渲染结果、Nuxt UI 原语内部链接 carve-out)。第 2 轮 2 条 🟡 采纳——carve-out 举例 `USelect` 与 `Select.vue` 源码相反(其 `props.class` 落到 `SelectTrigger` 根 `<button>`),改为 `UTabs` 并把 7 个 `USelect` 触发器补齐 + spec 锁定;`test:component` 计数 445 系抄错(gate 日志为 437)。3 条 🔵 采纳(PR 描述「10 个既有 spec」→ 12、InlineMarkdown 注释与 ThemeToggle 同措辞、gate 措辞)。第 3 轮(对 `ecd856b` + 簿记提交)未收敛,抓到 2 条 🔴 都是我的执行失误:第 2 轮商定的条款改写因修改脚本在 ResponseExample 解析异常处中断而**没有落进分支**(`ecd856b` 的 commit subject 却声称改了),InlineMarkdown 注释同理;报告漏替换一个占位符;2 条 🟡 中一条(报告 / PR 按尚不存在的条款复述)随条款写入一并解决,另一条:response-example「every USelect」用例实际只覆盖宽屏 2 个 select(fixture 单 body、紧凑面板未打开)。三项均在重建的功能提交 `92f87c5` 中修正:条款与注释真正写入、占位符删除、用例改为「多 body fixture + 打开紧凑面板」覆盖全部 4 个 ResponseExample select。第 4 轮只核这三项是否落地,结果见 PR 复审评论。
