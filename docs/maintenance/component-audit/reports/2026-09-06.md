# 2026-09-06 组件审计(复核 PR #115)

- **Round**:3(`roundRollover: false`,无轮末横切扫描)
- **Base main SHA**:`5353f73a98db06c88a04da6749baf950a1a5749e`(与 09-05 相同,`origin/main` 昨日至今未前进)
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>` → `api-docs-site-search`、`api-docs-operation-header`、`api-docs-event-badge`,`openFindingReentry` / `priorityBacklog` 均为空
- **今日分流**:计划项与仍开放的 [PR #115](https://github.com/abel-2333-org/geist-nuxt/pull/115)(09-05 审计)完全重叠 —— main 上的 ledger 尚不含昨日结论属设计而非缺陷。按审计协议 §1「上一轮 audit PR 仍开放且覆盖同类 item 时优先继续 / 复核该 PR」,今日在 `chore/component-audit-2026-09-05` 分支上复核,**不新建同类 PR、不重复 `--record`**。

## 做了什么

1. worktree 检出 PR 分支并 rebase 到 `$MAIN_SHA`:base 未变,rebase 为 no-op,三个提交(`140c5e7` 功能 / `9e72d0f` 簿记 / `166036a` 复审修正)原样保留。
2. 在 rebase 后的分支重跑 §8 完整 gate(结果见下)。
3. 以新 session 的视角对功能提交 `140c5e7` 做独立复审(caveman-review),重点核证昨日报告隐含但未显式验证的前提;并在 PR 的 Vercel preview(部署自 delivery head `166036a`)上重做浏览器实测。
4. 复审记录以一条评论写入 PR;本报告作为 09-06 簿记提交进同一 PR。

## 复审结论:无新 finding,PR 结论维持

| 核证项 | 结果 |
|---|---|
| `wrap-anywhere` 是否为本仓 Tailwind 真实 utility(而非无效 class) | ✅ `tailwindcss` 4.3.2(≥ 4.1 提供 `overflow-wrap: anywhere`);preview 上 `<code>` 计算样式 `overflow-wrap: anywhere`。且它已是 kit 的既定换行约定:FieldItem、FactRow、FieldAnnotation、WebhookProtocol、RelationSourcePath、EnumTablePanel 与 foundation 的 DocAnnotation / TermAnnotation 共 8 个分发文件在用,OperationHeader 的改动是向家族对齐 |
| SiteSearch `#empty` 去掉内层 class 后是否依赖主题存在等价排版 | ✅ `.nuxt/ui/command-palette.ts`:`empty` base `text-center text-muted`,md 档 `py-6 text-sm`;ledger 的 `@nuxt/ui` 4.9.0 dependency evidence 已写明此前提 |
| 报告 / PR 描述的宣称与代码一致 | ✅ `site-search.spec.ts` `it()` 13 条;`SiteSearch.vue` 350 行;三处 SHA(evidence `140c5e7` / delivery `166036a` / synthetic merge)与 `gh` 实时一致;gallery 9 处 `truncate` 行号(`endpoint-reference.vue` L532/686/718、`webhook-reference.vue` L296/425/456、`DocsShellReference.vue` L175/270/310)全部仍准确 |
| 测试断言与改动匹配 | ✅ 两条 reject 用例的 `toHaveBeenCalledOnce()` 只在 `search + searchGroupLabel` 齐备的挂载下断言,warn 来源唯一;`afterEach` 的 `vi.restoreAllMocks()` 收回 spy |
| 模板内联箭头 `:no-results-announcement="(q) => …"`(gallery 两处) | 🔵 nit 驳回:与同页 SidebarNav 实例既有写法一致,demo recipe 非分发物,不为此制造 churn |

### 浏览器实测(PR preview,delivery head `166036a`)

| 项 | 实测 |
|---|---|
| OperationHeader 375px `/v1/customers/{customer_id}/payment_methods/{id}` | 2 行,`scrollWidth === clientWidth`(无溢出),徽章 top 与首行 top 差 1px |
| OperationHeader 375px `checkout.session.async_payment_succeeded` | 2 行,无溢出,徽章落首行 |
| OperationHeader 1440px 同两条 | 各 1 行,无溢出 |
| SiteSearch 空态 `[data-slot="empty"]`(sidebar-nav 页,query `zzzqqq`) | 高 64.6px,外层 padding 24/24、内层 0/0,内层无 class |
| SiteSearch 播报 / 命名 | `role="status"` = `没有匹配“zzzqqq”的文档`;input `aria-label` = `搜索 API 文档`;placeholder 以 `…` 结尾 |

与 09-05 报告数值一致;昨日「pane 隐藏态无法截图」的缺口今日以 375px 截图补齐(见 PR 复审评论)。

## Gate(rebase 后重跑)

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 56 files |
| `pnpm test:registry` | ✅ 131 pass / 0 fail |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ root CSS source check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 isolated closure smoke 全部通过(含 `isolated-api-docs-site-search` / `-operation-header` / `-event-badge`) |
| `pnpm test:component` | ✅ 31 files / 343 tests |
| `pnpm audit:verify` | ✅ 29 个 scope-v1 条目证据全部成立;legacy 0、deferred 0、未审计 0 |

与 09-05 结果逐项一致;evidence head 保持 `140c5e7`,今日不产生新的功能提交,因此不重新 `--record`。

## 遗留与人工决策点(不变)

- **OperationHeader 视觉取舍**:长标识由单行截断改为多行换行、徽章改基线对齐(短标识时徽章相对原位置下移约 1~2px)。API / slots 未变,评审时确认接受。
- **gallery 手写 h1 header 镜像**:9 处仍 `truncate`,落在 4~9 个 item 的 scope 内,建议在下次触碰这些页面的功能 PR 里用 `--record --affected` 一并对齐。
- merge、转 ready、auto-merge 全部等人工。PR 未合并前,main 上的 ledger 不含 09-05 结论,明日计划仍会给出同一簇 —— 这是设计;合并后 Round 3 剩余 7 个组件继续。
