# 2026-09-23：#147 分批审计收口记录

本文按批次保留记录时的事实。首批已由 [PR #155](https://github.com/abel-2333-org/geist-nuxt/pull/155) 合入 main；首批段落中的本地限制、未推送及未合并描述属于当时状态。第二批记录见文末。

## 授权、范围与来源

维护者确认解除“保留七个 deferred”的状态限制，本轮仅授权按当前计划正式记录首批三项、验证并本地提交。剩余四项等待后续 canonical 计划；push、创建 PR、merge、主动发布/部署、tag、关闭 #147、外部 consumer 与 v0 memory 更新仍不在本轮授权范围。

正式记录前重新 fetch，`origin/main` 与 merge-base 均为 `23bed8705a425300e6dcc9d2c81ae54a6285e29a`。工作区干净，原 ledger 与该 canonical 基线字节一致。已审功能提交为 `9674ef684769fe0eec1acc05b6422ea519867edd`，内容摘要为 `6f1814dd7ec5e26bd3381eb3c5b18a656f54d41fcba0a8f7e41589ca4028e415`。

功能提交只增加 SiteSearch PUT 与 OperationHeader 的真实组件测试覆盖；运行时代码、颜色、公开契约、registry、lockfile 及测量器不变。此前完成七项完整 owner 独立复审，本轮采用已审处置候选中的首批结果，没有重新选色或扩大测量器允许范围。

## 首批正式处置

| Owner | 原 finding | 本地记录结果 |
| --- | --- | --- |
| `api-docs-method-badge` | `f-8c9c3be3a38e` | finding `resolved`，owner `verified` |
| `api-docs-site-search` | `f-f388d18e9a16` | finding `resolved`，owner `verified` |
| `api-docs-operation-header` | `f-0fd303fa92c4` | finding `resolved`，owner `verified` |

每条 finding 的原 id、severity、claim 与历史 evidence 均保留，仅将上述三条的 disposition 从 open 改为 resolved。SiteSearch 原已 resolved 的状态播报 finding `f-dd631c5b3977` 完整保留。新 notes 说明复审结论及当前证据来源，不把旧失败文字重写为新实测。

完整 scope 保留原路径，补入实际依赖的颜色真源、组件组合、相关测试、构建接线与测量代码。各 owner 的 dependency 声明来自实际安装行为核对；版本、itemDigest、scopeDigest 由正式 recorder 从已提交功能快照和 lockfile 自动生成。

## 证据与复审

- SiteSearch：保留原 GET/POST/PATCH/DELETE 的 168 个真实搜索样本；补 PUT 的 light/dark × 1440/390 × route-active/inactive × visible/hover/keyboard-highlighted，共 24 个唯一状态样本。light 最低 `6.130913255950162`，dark 最低 `7.472288094124483`。
- PUT 原始记录含真实 beforeBackground、highlighted、ariaSelected 与背景合成链。ariaSelected 全为 false、ariaCurrent 全为 null；当前路由项初始可见时已被原语高亮，未将 visible 写成未高亮，也未合成持久 selected 状态。
- OperationHeader：补 GET+beta、PUT+deprecated、PATCH+sunset、DELETE+maintenance，并采集 gallery 中原 POST+Active 与 webhook 的方法/EVENT、identifier、summary、description 及存在时 lifecycle，共 116 个真实 HEADER 节点，最低 `5.503473649450167`。默认 h2、gallery h4 与 webhook 无 lifecycle 分支均验证；未借用 endpoint-reference 的手写 h1。
- 完整浏览器 9 specs、130/130；556 份 JSON，去重普通/正向测量 5128 条，最低 `4.537610583837748`，普通测量 fail/unresolved/unverified 均为 0。原 118 项与 544 个文件路径保留，旧 4988 条主记录的主题、视口、组件、状态、路由状态和文字身份均无缺失；新增恰为 140 条。
- 232 条正向旁路副本不重复计数。预期拒绝、数值负控、几何记录、alpha 诊断和 disabled 例外分别保留；#148 中性、生成绘制/字形/伪元素、正常/透明/零宽/移除恢复控制及 optional trigger 的 Tab/Tooltip/交叠/反转回归完整。
- 功能提交 component 477/477、registry 160/160、typecheck、contrast build 与浏览器 TypeScript 检查通过。完整 owner scope/依赖/默认行为由两位独立 reviewer 分工复核；第三位未参与实现者复核新增测试、最终原始数据、去重、保留状态及处置候选，无未决技术 finding。

上述运行证据继续绑定 `9674ef6`，不改标为本次台账提交。旧 `471c523` 与 CI `ec18ef4` 证据保留各自来源。旧阶段报告中的 5224 不是去重计数；本轮采用已核验的旧 4988 + 新 140 口径，旧封存内容保持原样。

补验与独立复审已封存为 1345 个文件，manifest SHA256 为 `fe35ab94a8eeeaa7fe6ccd449c9340002e2dc2b81d0efe0cbfa7acf50e2502cd`。处置候选 SHA256 为 `631d4f921e8de91cd888ce69d10c30f20c38540e7a4c6662b44b915a3713e596`。完整原始 JSON、截图、trace、失败/重试、逐节点定位与独立报告在本任务本地证据交付中；本轮尚未远程上传，不将本地材料冒充公开附件。

## 正式记录与验证

实时官方计划恰为 HttpMethodBadge、SiteSearch、OperationHeader。真实三文件功能 diff 没有命中其他 owner 已记录的 base scope、registry topology 或 dependency 身份，required co-review 集合为空；因此使用普通计划记录，不使用 `--affected`，也不额外塞入七项。

```text
node scripts/audit-plan.mjs --record <首批结果数组> --base 23bed8705a425300e6dcc9d2c81ae54a6285e29a
```

- recorder exit 0，恰好记录三项；evidence.headSha 均为功能提交 `9674ef684769fe0eec1acc05b6422ea519867edd`。
- `audit:verify`：exit 0，25 holds、4 deferred、0 stale、0 legacy、0 unaudited。
- `test:audit`：31/31，exit 0，无 skipped/cancelled。
- 逐对象比对：三项对应 open → resolved，其余 26 个 owner 完整不变；原 scope 保留，其他历史 resolved/wont-fix 不变。
- 本次是计划模式，lastRunAt 和 lastPicked 按正式排程更新，global round 保持第 4 轮；不是保持排程字段不变的 affected 模式。
- 台账提交仅包含 ledger 与本报告；功能源码、浏览器原始证据及旧封存目录不变。提交后再次执行 exact-head 审计回读并独立复审本次 metadata。

## 剩余边界

本地仍 deferred 的四项为 `api-docs-lifecycle-badge`、`api-docs-field-item`、`api-docs-field-annotation`、`api-docs-lifecycle-notice`；这些项的完整复审候选已准备，但没有在本轮记录。首批后续交付至 main 后，须从届时最新 canonical 台账重新推导计划、差异与证据适用范围，不能在本地假定 main 已更新后连续记账。

本轮没有新增 consumer 浏览器结论。此前 accepted fresh/upgrade 没有 SiteSearch navigation、HttpMethodBadge、OperationHeader 场景，LifecycleBadge 仅 New；根 gallery 与测试 fixture 不替代消费端实测。任意调用方 slot、外部主题覆盖及全部 props 组合不属于本轮实测保证。

当前状态仅在本地分支成立；canonical main 的七个 deferred 尚未改变。#147 保持 OPEN，不更新原 checkbox；本轮没有 push、PR、merge、主动发布/部署、tag、外部 consumer 或 v0 memory 写入。


## 第二批：LifecycleBadge、FieldItem

### 授权与当前基线

维护者确认推进第二批正式记录、验证、独立复审和 PR 交付，并在适用门禁通过后合并。范围为官方计划中的 `api-docs-lifecycle-badge` 与 `api-docs-field-item`，不包含主动发布/部署、打 tag、关闭 #147、外部 consumer/v0 更新或记录其余两项。

独立分支从实时 `origin/main` `199c276a0a03b18cf5bfcbc550664b90ecb60e4b` 建立；正式记录前再次 fetch 并确认 HEAD、main、merge-base 一致，工作区干净、ledger 与 canonical 基线一致。没有组件、测试、运行时、registry 或 lockfile 改动，因此两项均为 `change: none`。

### 证据适用性与范围补齐

两位原 owner reviewer 分别完成本批独立适用性复核。LifecycleBadge 最终 35 个 scope、FieldItem 最终 49 个 scope 均逐 Git blob 与已完整实测的 `9674ef684769fe0eec1acc05b6422ea519867edd` 相同；两项原 canonical scope 分别 13、26 个，完整保留。当前 `contrastSource` 内容摘要仍为 `6f1814dd7ec5e26bd3381eb3c5b18a656f54d41fcba0a8f7e41589ca4028e415`，registry/package/lock 内容也一致。

- LifecycleBadge：复核六生命周期 preset、label fallback、icon/variant/sm 默认、实际 UBadge 类生成及 Header 组合；直接回读封存最终 84 个真实徽标节点，六状态均覆盖 light/dark × 1440/390，全部通过，最低 `4.8224578533887295`。补入实际组合承载层 `OperationHeader.vue` 和 tokens/data-display 权威契约，绑定原五项依赖及实际参与 UBadge 类生成/合并的 tailwind-variants、tailwind-merge。
- FieldItem：复核字段/值结构、条件/requiredness、field/value caveat、嵌入生命周期、递归/本地化、anchor 和 optional trigger；直接回读 16 份 API matrix 的 96 个相关节点，全部通过，最低 `4.551121906832344`。补入 `app/app.config.ts`、`foundation/config/nuxt.ts` 的实际配置接线，以及 Nuxt/Tailwind/Vue 的具体依赖行为；原 @nuxt/ui、reka-ui、marked 记录保留。
- 依赖版本由正式 recorder 从当前 Git lock blob 解析：@nuxt/ui 4.9.0、vue 3.5.39、reka-ui 2.9.10、nuxt 4.4.8、tailwindcss 4.3.2、tailwind-variants 3.2.2、tailwind-merge 3.6.0、marked 17.0.6。各 owner 仅记录与其结论有关的依赖。

复用 `9674ef` 的 component 477/477、browser 130/130、registry 160/160 和 typecheck 回执，没有把原测试重新标记为 `199c276a` 新运行。封存文件 hash 和逐节点来源已重新核对。此前 [PR #155 CI](https://github.com/abel-2333-org/geist-nuxt/actions/runs/35822161915) 也在 synthetic merge `f8a33fc9cbfe09bf10dc1e9bbdcdd27a3a34db18` 通过 component 477/477 与 browser 130/130，其文件树与 `199c276a` 相同；它与本批后续 CI 分别保留来源。

旧 `471c523`、`ec18ef4`、`9674ef` 原始证据及历史失败文字未改写；正常文字仍使用未舍入的 `>=4.5` 判定。LifecycleBadge 消费端仅 New 有既有浏览器实测，不外推全部生命周期、自定义 label/size/attrs/slot、外部主题或维护者视觉验收；FieldItem 的消费端和自定义覆盖边界也保持原样。

### 正式记录结果

本批 base..功能 HEAD 无 diff，没有 required co-review owner，使用普通官方计划记录而非 `--affected`：

```text
node scripts/audit-plan.mjs --record <第二批结果数组> --base 199c276a0a03b18cf5bfcbc550664b90ecb60e4b
```

- 原 `f-aab0322ebade`、`f-73feb0baf6dd` 的 id/severity/claim/evidence 完整保留，仅 disposition 改为 resolved；两 owner 标记 verified。
- 正式 evidence 的 baseSha/headSha 均为 `199c276a0a03b18cf5bfcbc550664b90ecb60e4b`，记录时间为 `2026-09-23T06:16:13.357Z`；scope/item/dependency 内容标识全部由 recorder 生成。
- 逐对象回读仅这两 owner 改变，其余 27 个 owner 完整不变；round 仍为 4，lastPicked 按正式计划更新为这两项。
- 记录后 `audit:verify` exit 0：27 holds、2 deferred、0 stale/legacy/unaudited。审计工具回归记录前后均 31/31 通过；提交后验证与独立 metadata 复审绑定本批提交另行留证。

剩余 deferred 为 `api-docs-field-annotation`、`api-docs-lifecycle-notice`，原记录未改变。本段描述本批分支记录时状态，合并后才成为 canonical；后续须从届时新 main 重新生成计划，不能提前把剩余两项塞入本批。#147 保持 OPEN。
