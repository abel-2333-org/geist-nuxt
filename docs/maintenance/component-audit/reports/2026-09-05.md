# 2026-09-05 组件审计

- **Round**:3(本轮第 6 天,`roundRollover: false`,无轮末横切扫描)
- **Base main SHA**:`5353f73a98db06c88a04da6749baf950a1a5749e`
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>`,`openFindingReentry` / `priorityBacklog` 均为空(全仓 open finding 队列为空)
- **选簇原因**:接续上批簇——`api-docs-site-search` 与 `api-docs-operation-header` 都直接依赖上批的 `api-docs-method-badge`;`api-docs-event-badge` 是 OperationHeader 的直接依赖,作为关系分最高的邻居扩入(共 494 LOC)
- **附注**:任务中途因权限流中断、系统清空 `/private/tmp` 而重建过 worktree;分支、base SHA 与日期均未变,重跑 preflight 后继续,无残留状态

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `api-docs-site-search` | modified | verified | 空态 `#empty` 槽与 UCommandPalette 主题重复的 padding 去掉(110px → 64.6px);诊断收敛为 kit 惯例 `dev \|\| test` 并补 warn 断言;gallery 两处实例补中文 `noResultsAnnouncement`、placeholder 补省略号 |
| `api-docs-operation-header` | modified | verified | identity 标识 `truncate` → `wrap-anywhere`,行对齐改基线;375px 下真实长度的 path / 事件名不再丢尾部 |
| `api-docs-event-badge` | none | verified | clean,无新 finding |
| `api-docs-method-badge` | modified(co-review) | verified | scope 内 SiteSearch / OperationHeader / sidebar-nav 页被改,method badge 调用链路零变化 |
| `api-docs-sidebar-nav` | modified(co-review) | verified | scope 内 DocsShell / sidebar-nav 页只改 SiteSearch 实例 props,SidebarNav 零变化 |

三个计划项都不是首审(SiteSearch 计划项 4 次 + co-review 3 次;OperationHeader 计划项 2 次 + co-review 4 次;WebhookBadge 计划项 2 次 + co-review 5 次),行为契约、a11y 播报、`translate=no`、preset 分层都已闭合。本轮落地的是三处有实测证据的窄屏 / 排版 / 一致性收敛,其余看起来可改的点逐一给出不改理由。

## 各组件详情

### api-docs-site-search(SiteSearch)

- **晋升 / 精简 / 重构**:无。ADR-009 裁定 SiteSearch 留在 API Docs kit(复用 method badge、围绕指南 / 端点 / method / scenario 组织结果)复核仍成立;344 行单文件承担 trigger → UModal → UCommandPalette 三层装配 + 同步 Fuse 过滤 + 异步源 + 三态播报 + hash 焦点交接,每段各有其 reference 契约条目,不拆。registry `packageDependencies` 只声明 `fuse.js`、`registryDependencies` 只声明 `api-docs-method-badge`,与源码 import / 模板一致,无冗余闭包。
- **优化 ①(本轮落地)——空态双倍内边距**。`#empty` 槽内层 `<div class="py-6 text-center text-sm text-muted">` 与 `@nuxt/ui` 4.9.0 CommandPalette 主题 `empty` 槽(base `text-center text-muted` + md 档 `py-6 text-sm`)逐类重复。**复现输入**:任意 gallery 页打开 ⌘K,输入 `zzzqqq`;实测 `[data-slot="empty"]` 外层 padding 24/24 + 内层 24/24,空态高度 **110.2px**。**修法**:内层只保留 `ref="empty"`(它是播报 watch 的观察对象),排版交还主题。修后内层 padding 0、空态高度 **64.6px**,与 Nuxt UI 默认空态一致。severity low(纯视觉,空态在明暗双主题下多出一倍留白)。**依据**:nuxt-ui skill「优先使用主题变体而非叠加重复的 utility」;frontend-design「间距体系一致」;审计协议 §5「硬编码尺寸未走系统档位」。`site-search.spec` 既有断言只读该槽文本,原样通过。
- **优化 ②(本轮落地)——诊断条件对齐 kit 惯例**。两处 `console.warn`(search 源 reject、传 `search` 未传 `searchGroupLabel`)是 kit 中唯一仍用 `import.meta.dev` 单条件的实现,EnumTable / FieldAnnotation / ResponseExample / TermAnnotation 均为 `import.meta.dev || import.meta.test` 并有 `vi.spyOn(console, 'warn')` 断言。08-16 报告已把它标为「留待该组件所在簇排期时处理」,今日即该簇。**修法**:两处条件对齐,spec 中两条 reject 用例加 warn spy 并断言恰好一次(原先该 warn 在测试环境静默不可断言,现在若不 spy 会成为噪音),新增「缺 `searchGroupLabel` 时 warn 一次」与「齐备时静默」两条。**行为契约**:生产构建下 `import.meta.test` 为 false,无任何运行时变化。
- **gallery 修正(本轮落地)**:`sidebar-nav.vue` 与 `DocsShell.vue` 的 SiteSearch 实例传了中文 `emptyLabel`,却没传 `noResultsAnnouncement`——实测 live region 播报英文默认 `No results for “zzzqqq”`,可见空态却是「没有匹配的文档」;同页的 SidebarNav 实例早已传中文播报函数。两处补 `:no-results-announcement`,修后播报「没有匹配“zzzqqq”的文档」/「没有与“…”匹配的结果」。`sidebar-nav.vue` 的 placeholder「搜索指南、接口或场景」补结尾省略号(08-25 标为「#105 合并后顺手对齐」,#105 已合入)。依据:gallery.md「demo 单语中文直写」的双语对等原则、voice-content.md 省略号字符条款。
- **复核无回归的既有契约**:trigger UButton 可访问名恒为 `ariaLabel ?? triggerLabel`,`max-sm` 只剩图标时仍有名;UModal `#content` 模式下 `title` 经 VisuallyHidden `DialogTitle` 给 dialog 命名;搜索 input `aria-label` = `modalTitle`(实测「搜索 API 文档」)、`autocomplete="off"`;所有 group 以 `ignoreFilter: true` 交付、Fuse 同步过滤 + 全局 `resultLimit`、`postFilter` 收当前查询、`matches` 驱动 `<mark>` 高亮;异步源 `setTimeout` debounce + `onCleanup` 陈旧取消;持久 `role="status" aria-live="polite" aria-atomic`,有可用 option / 空查询 / 关闭时静默;hash 焦点交接 `router.resolve` 单次解码、`tabindex=-1`、`prefers-reduced-motion`。`site-search.spec.ts` 15 用例全绿。
- **Universal claims 核证**:spec「never announces a matching query while replacing a previous empty state」由 MutationObserver 用例锁定;reference「静态导航 groups 始终可用」指数据源常在(有查询时按查询过滤),与实现一致;「有任何可用 option、查询为空或面板关闭时保持静默」三条件由 3 条用例覆盖。
- **为什么没改看起来可改的**:
  - `paletteGroups` 每次键入 `new Fuse(items)` 重建索引——docs 索引量级(几百项、4 个 key)下单次索引 < 1ms,08-25 已复核;缓存 Fuse 实例要引入 `setCollection` 生命周期管理,收益不可测,不动。
  - `ignoreFilter` 的 extraGroups 也会被 UCommandPalette 按 `resultLimit` 切片——reference 只承诺「保持消费方排序」,未承诺不限量;真实消费者的 pinned 组不会超过 12 项,不为此加分支。
  - `onCloseAutoFocus` 与 `useFieldAnchor` 的「补 `tabindex=-1` → `focus({ preventScroll })` → reduced-motion 判定」同形(反向横扫命中)——`useFieldAnchor` 归 `api-docs-field-item` item,为 4 行复用让 site-search 依赖整个 FieldItem 闭包(FieldItem + EnumTable + InlineMarkdown…)比重复本身更贵,与 08-16 trailing-slash 的 wont-fix 权衡同构;记观察不记 finding。
  - `aria-label` 与 `triggerLabel` 分离——消费方可能传不同文本造成 label-in-name 偏差,但 prop 的存在是为 `max-sm` 图标态提供完整名,两处 gallery 均传同文本,属消费方责任。
  - 未为空态 padding 补 DOM 测试:happy-dom 无布局,断言「内层无 class」只会锁实现;证据是真实浏览器的计算样式测量(上文数字)。

### api-docs-operation-header(OperationHeader)

- **晋升 / 精简 / 重构**:无。「单组件双形态」「与 OperationTarget / LifecycleNotice 正交」「`headingLevel` 上限 2」三条裁定(index.md「Operation identity 分层」、08-05 报告)复核仍成立;`kind` discriminated union 用 `never` 封死反向字段,非法组合是调用点类型错误。registry 三条 `registryDependencies`(method / event / lifecycle badge)逐一有真实模板消费。
- **优化(本轮落地)——identity 标识由截断改换行**。原 `<code class="min-w-0 truncate …">` 在窄屏上吃掉标识尾部且无任何 tooltip / 展开手段。**复现输入**:375px 视口,`/kits/api-docs` 的 OperationHeader story,把标识替换为真实长度的 `/v1/customers/{customer_id}/payment_methods/{id}`(50 字符)与 `checkout.session.async_payment_succeeded`(40 字符,Stripe 常见形态):实测 `scrollWidth 403 / clientWidth 252`、`336 / 246`,分别丢 **18** 与 **11** 个字符——正好是 `{id}` 与 `_succeeded` 这些区分操作身份的尾段。webhook 形态没有 OperationTarget 兜底,header 是事件名的唯一出处;触屏没有 hover tooltip。**修法**:`truncate` → `wrap-anywhere`(`overflow-wrap: anywhere`,允许在任意字符断行且参与 flex 收缩);行对齐 `items-center` → `items-baseline`,使徽章在单行与多行时都落在标识首行,`#actions` 容器加 `self-center` 保持原居中。修后 375px 两行完整可读(徽章 top 与首行 top 差 2px、bottom 差 2px),1440px 两个长标识仍单行、无截断;短标识渲染与修前仅差基线对齐带来的 2px 徽章偏移。severity low(窄屏信息丢失,无功能缺陷)。**依据**:responsiveness.md「分级降级:完整 → 缩略(截断 + tooltip)…」「永远保护主标识…核心 label 留可读地板」;Web Interface Guidelines「Text containers handle long content」;审计协议 §5「390px→宽屏响应式」。**与 08-05 结论的关系**:当日判「`min-w-0 truncate` 符合 flex 截断条款」评的是截断的实现方式正确,未评估窄屏 + 真实长度标识的信息丢失;本轮是不同维度的补充,不是推翻其对 `min-w-0` 的判定。
- **API 与行为契约**:props / emits / slots / `headingLevel` 全部未变;index.md 该行只写「identity 行(徽章 + mono 标识 + 右对齐 `#actions` 槽)」,从未承诺单行或截断,因此不构成 reference drift,也不为此改动共享索引(它在 12 个 item 的 scope 内,一处括注会触发大面积联审)。源码 anatomy 注释同步写明「wraps, does not truncate」。
- **复核无回归的既有契约**:`translate="no"` 在原生 `<code>`(operation-header.spec 两形态断言);`lifecycle` 收 `EndpointLifecycle`(含 beta)、`lifecycleLabel` 覆盖与空白回落(chrome-labels.spec);标题 `text-balance`、描述 `max-w-prose text-pretty`;`$slots.actions` / `$slots.description` 条件容器不留空壳;配色全走语义 token。
- **Universal claims 核证**:注释「lifecycle badge carries icon + text, never color alone」——`lifecyclePreset` 六态全有 icon,`label` 空白回落 preset 文案,结构性成立(08-01 已核,未变)。
- **为什么没改看起来可改的**:
  - gallery 四页手写的 h1 identity header(`endpoint-reference.vue` ×3、`webhook-reference.vue` ×3、`DocsShellReference.vue` stub ×3)仍用 `min-w-0 truncate`——它们是页面私有 recipe 而非分发物,且分别落在 4 / 7 / 6 / 9 个 item 的 scope 内,为一处 class 触发十余个无关 item 的强制联审不成比例;记入「遗留与下一步」交人工顺手对齐。
  - `headingLevel` 不开放 1——endpoint-reference / webhook-reference 注释明言「standalone 单 operation 页手写 h1、外壳多 operation 用 OperationHeader」是刻意分层,08-05 已裁定,不重议。
  - 未补「不截断」的 DOM 测试:happy-dom 无布局,溢出行为只能在真实浏览器测量,证据见上。

### api-docs-event-badge(WebhookBadge)

- **晋升 / 精简 / 重构**:无。44 行常量包装层,`EVENT_BADGE` 内联常量符合 index.md「单值词汇的退化形态……词表长出第二个值时再抽文件」;`size` 默认 `sm` 是与 HttpMethodBadge / LifecycleBadge 同排对齐的显式记录(08-01 裁定)。
- **优化**:无。neutral + subtle 置于五方法色之外,词形 EVENT 是主信号;`translate="no"` 经 SemanticBadge 单根 fallthrough 落到 UBadge(依赖证据 `@nuxt/ui` 4.9.0 / `vue` 3.5.39,lockfile 未变);`font-mono tracking-wide tabular-nums` 与 HttpMethodBadge 逐字一致。
- **消费面复核**:OperationHeader `kind="webhook"` 分派、`webhook-reference.vue` ×3、`index.vue` ×2、`structured-relations.vue` ×2(callback / relay 身份槽,`class="self-center"`)——全部零参或仅传 class,无绕过 preset 直接拼 UBadge 的调用。
- **Universal claims 核证**:注释「so the badge never competes with them for meaning」——neutral 不在 `methodPreset` 五色内,结构性成立;「a11y: never color alone」——label 为常量非空文本,成立。
- **为什么没改看起来可改的**:`EVENT` 不可本地化——index.md 明言它是「领域词汇非文案」,与 HTTP 动词同级,chrome-labels 契约(issue #29)针对的是结构 chrome 文案,不适用;`SidebarNav` 条目以 `method: 'EVENT'` 走 HttpMethodBadge fallback 的过渡态——index.md 已记为「数据层 stopgap,`item.kind` 泛化是后续事项」,属 sidebar-nav 的产品决策,不在本 item 重复记录。

### api-docs-method-badge(HttpMethodBadge)— required co-review

scope 内三个文件被改:`SiteSearch.vue`(empty 槽 class、诊断条件)、`OperationHeader.vue`(标识换行 + 基线对齐)、`app/pages/kits/api-docs/sidebar-nav.vue`(SiteSearch 实例 props)。三处都不触及 `<HttpMethodBadge>` 的调用参数(SiteSearch `#item-leading` 仍只传 `method`,OperationHeader 仍只传 `method`)、`trim + toUpperCase || 'UNKNOWN'` 归一化、`methodPreset` 色调、`translate=no` 链路;`items-baseline` 只影响徽章在行内的位置,不改徽章自身。`http-method-badge.spec.ts` 5 用例原样通过,registry topology 未变,依赖证据(`@nuxt/ui` / `vue`)原样重录,无新 finding。

### api-docs-sidebar-nav(SidebarNav)— required co-review

scope 内 `DocsShell.vue` 与 `sidebar-nav.vue` 被改,但改动全部落在同页的 `<SiteSearch>` 实例(补 `noResultsAnnouncement`、placeholder 省略号);`<SidebarNav>` 的调用点(`groups` / `search-placeholder` / `no-results-announcement` / 调宽 props)、组件实现、`useSplitPane` 持久化与 `sidebar-nav.spec.ts` 均一字未动。既有 finding `f-12d73e8bc49d` 保持 `resolved`(#105 已收敛),无新 finding。

## 反向横扫(簇外重复检查)

对今日组件提取的特征模式在 `foundation/ kits/ app/components/ app/pages/ app/utils/ app/composables/ playground/` 全仓反查:

| 模式 | 命中 | 判定 |
|---|---|---|
| `min-w-0 truncate font-mono text-sm text-highlighted` identity 标识 | OperationHeader(本轮已改)+ gallery 手写 h1 header 9 处 | recipe 镜像,交人工顺手对齐(见遗留) |
| `import.meta.dev` 单条件诊断 | 仅 SiteSearch(本轮已对齐为 `dev \|\| test`) | 其余 4 处已是 kit 惯例 |
| `tabindex=-1` + rAF focus + reduced-motion | SiteSearch、`useFieldAnchor`(kit)、DocsShell(gallery) | 三处生命周期与滚动策略各异;抽取要跨 field-item 闭包,不划算(同 08-16 权衡) |
| `role="status" aria-live="polite"` sr-only 播报 | 7 个文件 | 一行模板套路,无共享逻辑(08-22 / 09-04 结论不变) |
| `defineShortcuts` / `router.resolve` / `Fuse` | 仅 SiteSearch(SidebarNav 的 `router.resolve` 在 computed 内,09-04 已收敛) | 无重复 |
| `font-mono tracking-wide tabular-nums` 徽章排版 | HttpMethodBadge、WebhookBadge | 镜像原子既定对齐(index.md「平行原子」) |
| `headingLevel?: 2 \| 3 \| 4` + `` `h${…}` `` | OperationHeader、FieldGroup、WebhookProtocol(经 FieldGroup)、SchemaComposition(`HeadingLevel` 型) | 家族约定,类型字面量各自内联;抽公共 type 需新 registry item,收益不足 |
| `text-balance` 标题 / `ml-auto flex shrink-0` 行尾动作位 | OperationHeader + 3 个 gallery h1;OperationTarget | token 级排版约定,非逻辑重复 |
| `defineOptions({ inheritAttrs: false })` + 多根 | SiteSearch、CopyButton | 两者 attrs 目标不同(UModal vs 按钮),无共享 |
| `translate="no"` 字面标识保护 | 8 个分发文件 | 契约级约定(08-19 已横向对齐) |

## Gate

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 56 files |
| `pnpm test:registry` | ✅ 131 pass / 0 fail |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ postbuild root CSS check 8 markers |
| `pnpm test:consumer` | ✅ 42 个 isolated closure smoke 全部通过(含 isolated-api-docs-site-search) |
| `pnpm test:component` | ✅ 31 files / 343 tests(+2 warn 用例) |
| `pnpm audit:verify` | ✅ recorder 绑定功能提交 `140c5e79c37d04d81fef7723dfcc98e26ea79a16` 后重算一致;`api-docs-method-badge`、`api-docs-sidebar-nav` 为自动推导的 required co-review owner;deferred 0、未审计 0 |

浏览器实测(WT 上 `nuxt dev`,JS 读取计算样式;Browser pane 处于隐藏态无法出图,证据为数值):

| 项 | 修前 | 修后 |
|---|---|---|
| SiteSearch 空态 `[data-slot="empty"]` 高度 | 110.2px(外 24/24 + 内 24/24) | 64.6px(外 24/24,内 0/0) |
| SiteSearch 零结果播报(sidebar-nav 页) | `No results for “zzzqqq”` | `没有匹配“zzzqqq”的文档` |
| OperationHeader 375px `/v1/customers/{customer_id}/payment_methods/{id}` | 截断,丢 18 字符 | 2 行完整,徽章落首行 |
| OperationHeader 375px `checkout.session.async_payment_succeeded` | 截断,丢 11 字符 | 2 行完整,徽章落首行 |
| OperationHeader 1440px 同两条 | 单行 | 单行,无截断 |
| console errors | 0 | 0 |

## 遗留与下一步

- 无新增 open finding;全仓 open finding 队列保持为空。
- **人工顺手项(不记 finding)**:gallery 四页手写 h1 identity header 的 `truncate` 可与 kit 同步改为 `wrap-anywhere`(`endpoint-reference.vue` L532/686/718、`webhook-reference.vue` L296/425/456、`DocsShellReference.vue` L175/270/310);因这些页面落在 4~9 个 item 的 scope 内,建议在下次触碰这些页面的功能 PR 里用 `--record --affected` 一并处理。
- 本日有代码改动,PR 以 Draft 交付,等人工评审合并。
- Round 3 记录本批后剩余 7 个组件待复审,明日按依赖图继续扩张。
