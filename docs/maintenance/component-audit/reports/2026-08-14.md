# 2026-08-14 组件审计

- **Round**:2(本日为 round rollover 首日,已执行 §5b 全仓横切扫描)
- **Base**:`origin/main` @ `f56dd74c6126052dfcf77a2c50d9f376e086fe53`
- **选簇原因**:接续上次 Annotation 簇——`api-docs-field-annotation` 直接依赖 `foundation-annotation-popover`,并与上批共享 `foundation-inline-markdown`;`api-docs-field-item` 是它的直接依赖(带入 `FieldNode` 模型与 `useFieldAnchor`)。
- **规则源**:geist-nuxt skill、vue-best-practices、nuxt-ui、frontend-design、web-design-guidelines(Web Interface Guidelines),及 `references/kits/api-docs/index.md`、`references/method/component-reflow.md`。

## 结论一览

| item | change | status | findings |
|---|---|---|---|
| `api-docs-field-annotation` | none | deferred | 1 open(low):trailing-slash 归一化与 SidebarNav 重复 |
| `api-docs-field-item` | none | verified | 无 |

今天**无代码改动**:两个组件均达标,唯一 finding 是跨组件抽取决策,属人工判定范围,不制造无谓 churn。

## api-docs-field-item(FieldItem.vue + useFieldAnchor.ts + utils/field.ts,1209 行)

上次审计 2026-07-28;其后经历 #51 EnumTable labels 透传与 #52 中立命名重构,当前行为面稳定。本次全量复审四维度:

- **晋升**:无候选。`useFieldAnchor` 绑定字段树领域语义(active path、collapsible 揭示、字段行 flash),留 kit 正确;数据模型已在 `utils/field.ts` 单一真源。
- **精简**:模板内 4 处 fact-row `<dl>` 与 lifecycle detail 双位置(gate vs band 尾)是**有意重复**——源码有 keep-in-sync 注释,`field-item-structure.spec.ts` 有 wrap/对齐 parity 测试锁住;抽局部子组件会给单文件 slice 增加 registry 文件,收益为负。`childPaths` 每行 O(subtree) 重算在真实文档规模(数百字段)下可接受,未见性能证据不预优化(vue-best-practices §4:性能是后置 pass)。
- **优化**:a11y 核过——行焦点 `outline-hidden` + `focus-visible:outline-primary`(tested);锚点按钮 aria-label 含字段名且随 copied 态切换;图标 `aria-hidden`;高亮 cue 用 boxShadow 而非 outline(不吞键盘焦点环,WCAG 2.4.7,tested);`prefers-reduced-motion` 受尊重;色彩全语义 token,琥珀阶梯(CONDITIONAL 标签→条件行边框→caveat 填充→Beta 徽章)与 references 色彩分配一致;响应式走 `@container/field` 容器查询。
- **重构**:`<script setup>` + TS 质量良好;props 契约由 `FieldItemProps` 显式承载;`fieldRequiredState` 冲突语义(`required: true` 胜出、condition 照常渲染)有文档与测试。
- **Universal claims 核证**:`copyLink` 「只写剪贴板,从不导航」(`field-anchor.spec.ts` never-navigates 用例)、「stale positioning 不可能在用户离开后滚动」(navigation token + superseded / old-route-scope 用例)、「空 path 直接被拒」(invalid selector 用例)——均有测试反例防护,claim 与实现一致。

**为什么没改看起来可改的**:lifecycle detail 双位置去重、fact-row 抽局部组件、`childPaths` memo 化——三者都有明确的反面成本(registry 拓扑膨胀 / 无性能证据),且现状有测试与注释钉住意图,改动属 churn。

## api-docs-field-annotation(FieldAnnotation.vue + useFieldSource.ts,206 行)

昨日(2026-08-13)刚审计并修复(dev 告警、panel shrink),其后源码零变更。本次复审确认:降级策略(未命中 ref → 纯文本 + dev warn,`annotation-forms.spec.ts` 覆盖)、`fieldRequiredState` 共享派生(popover 与字段行不漂移,orphan condition 用例覆盖)、跨页 `{page}#{path}` 编码往返(`res_state%25` 用例)均达标。

**新 finding(low, open)**:§5 反向横扫命中簇外重复——`FieldAnnotation.vue:82` `normalizePath` 与 `SidebarNav.vue:207` `stripSlash` 各自实现 trailing-slash 归一化,且边界行为不一致(输入 `'//'`:前者得 `'/'`,后者得 `''`)。两处当前互不交互,行为不一致风险低。建议修法:统一为 kit util(`utils/route-path.ts`)或人工判 wont-fix(维护者视角:对一行函数新增 registry 共享文件不成比例,**倾向 wont-fix**)。抽取涉及 `api-docs-sidebar-nav` 的 scope(将触发其完整 co-review),本轮不动,交人工决策。依据:审计任务 §5 反向横扫「命中簇外组件的记为 finding」;SKILL 精简维度「相关组件重复逻辑该抽 composable/util」。已同步列入横切 tracking issue [#93](https://github.com/abel-2333-org/geist-nuxt/issues/93) 候选 2。

## §5b 轮末横切扫描(roundRollover)

产物:GitHub tracking issue [#93「组件抽取候选」](https://github.com/abel-2333-org/geist-nuxt/issues/93)(本轮新建)。要点:

1. **rAF 合并测量调度器**(SplitPane / CodeRail / EnumTable / ResponseExample / SidebarScenarioTags 5 处同构样板)→ 建议抽 foundation `useRafTask`,唯一建议采纳的候选;
2. trailing-slash 归一化(即上述 finding,倾向 wont-fix);
3. chrome labels 合并 idiom(11 组件)与 requiredness 标记 markup(2 处)——已评估**不建议抽取**,记录以免每轮重复评估。

覆盖维度与遗留(demo 私有组件、compositions 相似度)已写入 issue 正文。横切扫描未改代码、未写 ledger。

## Gate 输出

| gate | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ |
| `pnpm test:registry` | ✅ |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ |
| `pnpm test:consumer` | ✅ |
| `pnpm test:component` | ✅ |
| `pnpm audit:verify`(record 前后各一次) | ✅ |

无代码改动,gate 在 main snapshot 上全绿,证明基线健康。

## 遗留与下一步

- **人工决策点 1**:`api-docs-field-annotation` 的 low finding——判 wont-fix(推荐)或授权抽取 kit util(需连带 SidebarNav co-review)。
- **人工决策点 2**:issue #93 候选 1(`useRafTask`)是否立项单独实现 PR。
- `api-docs-field-annotation` 因 open finding 保持 deferred,将按算法重回 pool;判定 wont-fix 后自然回归 verified 流。
