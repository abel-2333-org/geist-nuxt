# 2026-08-19 组件审计

- **轮次**：round 2（`roundRollover: false`）
- **基线**：`origin/main` @ `c969781a09a6b574a302eabeef668965dd87a19a`
- **功能证据**：`fba99851bd2331f88305b1b932e9c647806181d5`
- **本批组件**：`api-docs-lifecycle-badge`、`api-docs-operation-header`、`api-docs-event-badge`（共 244 LOC）
- **co-review**：`api-docs-method-badge`、`foundation-semantic-badge`
- **结论**：发现 2 个同类翻译保护缺口，均已修复并由最终 DOM 测试锁定；5 个 owner 全部 `status: verified`，无 open finding、无 deferred。

## 修复结论

### api-docs-operation-header

`OperationHeader` 的 endpoint path 与 webhook event identifier 是机器可读 literal token，原实现的 `<code>` 未声明 `translate="no"`，浏览器翻译可能破坏可复制标识。现已在原生 `<code>` 节点直接增加保护，并新增 endpoint/webhook 两种判别联合分支的 focused test，断言全文和最终 DOM 属性。

### api-docs-event-badge / api-docs-method-badge

`WebhookBadge` 的固定 `EVENT` token 同样缺少翻译保护；横向复核时发现镜像组件 `HttpMethodBadge` 也应保持一致。两个 wrapper 均增加 `translate="no"`，并在 `http-method-badge.spec.ts` 中断言属性最终落到可见 badge 根节点。既有 tone、fallback、字号和镜像排版契约保持不变。

### lifecycle 与 foundation co-review

`LifecycleBadge` 的 preset、label fallback、`LifecycleNotice` 共词表以及类型穷尽性复核无回归。`SemanticBadge` 继续作为单根 `UBadge` wrapper；本次属性透传依赖已按当前仓库版本记录为 `vue@3.5.39` 与 `@nuxt/ui@4.9.0`，并由最终 DOM 断言覆盖。

## 反向横扫

对 literal token、`font-mono tracking-wide tabular-nums` 镜像徽章和动态 operation identity 全仓反查后，将修复收敛在 `OperationHeader`、`WebhookBadge` 与其直接镜像 `HttpMethodBadge`。未增加通用 wrapper 或新配置：三个节点的消费者和生命周期不同，直接声明属性更清晰，也不扩大 registry 依赖闭包。

## Gate 输出

- `pnpm test:agent`：17 tests 通过
- `pnpm registry:validate`：39 items / 54 files 通过
- `pnpm test:registry`：131 tests 通过
- `pnpm test:component`：29 files / 310 tests 通过；首次并行运行仅 `useSplitPane` 出现一次 timeout，随后 focused rerun 与 standalone full suite 均通过
- `pnpm typecheck`：通过
- `pnpm build`（含 postbuild root CSS check）：通过，8 markers；字体 metadata 网络警告不影响构建完成
- `pnpm audit:verify`：通过，无 legacy、无 deferred，未审计 1 个
- `pnpm test:consumer`：默认完整 gate 通过；覆盖 legacy config migration、12 个 runtime 场景与 30 个 isolated component closure，使用当前兼容依赖 Nuxt 4.5.2 / Vue 3.5.41 构建
- 分发物 U+FFFD 检查：通过

## 遗留与交付边界

- round 2 本批记录前 remainingInRound 为 14（含本批 3 个），记录后剩 11 个；`foundation-use-raf-task` 仍是唯一未审计 owner。
- 功能修复独立提交在 `fba99851bd2331f88305b1b932e9c647806181d5`；ledger 与本报告作为后续簿记提交交付，避免证据自引用。
