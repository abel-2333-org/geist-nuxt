# 2026-08-15 组件审计

- **Round**：2（第 2 天，`roundRollover=false`，无横切扫描）
- **Base（audited main SHA）**：`14ef6e980878f46da867ca77abfb71ce40f62f33`
- **功能提交**：`123dd7211ee239cc5ae3e4822f97ec922afb73a2`
- **计划组件**：`api-docs-enum-table`（374 loc）、`foundation-inline-markdown`（135 loc）、`api-docs-webhook-protocol`（357 loc）
- **required co-review owner**：`api-docs-field-group`
- **选簇原因**：接续 2026-08-14 簇（api-docs-field-annotation / field-item）；三者经 `foundation-inline-code`、`foundation-inline-markdown` 共享依赖与 field-item 直接依赖关联。`priorityBacklog` 为空，`openFindingReentry` 中的 api-docs-field-annotation（low）未被算法排入本批，不绕开算法自行扩围。
- **结果**：2 个 `change: "none"`、1 个 `change: "modified"`，全部 `verified`；功能 diff 命中的 field-group 完成同批 co-review，无 open finding。

## Base 前进与 PR #96

[PR #96](https://github.com/abel-2333-org/geist-nuxt/pull/96) 已在本轮交付前合并。旧审计提交基于 `08f58ab`，与 #96 对 EnumTable、registry、共享 reference 和 ledger 的改动冲突，因此未手工拼接旧 digest：分支重新基于 `14ef6e9` 生成计划，功能改动先提交，再由 recorder 从 Git blobs 重算全部 evidence。

EnumTable 的 `itemDigest` 现已包含 `foundation-use-raf-task` registry dependency，scope 同时绑定 hook 源码与 focused test，确保 scheduler 内容变化会让 owner stale。WebhookProtocol 保留直接依赖的 CodeBlock / FieldGroup 与 consumer gate scope；它与 FieldGroup 的共享 scope 绑定到同一个功能提交。`audit:verify` 在新 ledger 上重算通过。

## api-docs-enum-table —— clean（verified，legacy-v1 → scope-v1）

- **归属**：领域组件继续位于 API Docs kit；demo 数据保留在 gallery 页面，分发面只有组件与 enum util。
- **交互**：`id` 继续作为 locale-neutral tab identity；语言切换、重排和分组消失回落均有稳定测试。
- **筛选与 a11y**：单次 `filteredVariants` 保证 badge、body 与 live-region 计数同源；真实 DOM overflow 决定滚动框 tab stop，focus-visible 与 7/8 阈值边界保持覆盖。
- **#96 集成**：合帧调度已收敛到 `useRafTask`，registry closure、reference、组件测试与 composable 测试一致；evidence scope 显式包含 `foundation/composables/useRafTask.ts` 与 `tests/component/use-raf-task.spec.ts`，本轮不重复建立第二套调度抽象。

## foundation-inline-markdown —— clean（verified，补 marked dependency evidence）

- **安全契约**：协议白名单（相对地址 / http / https / mailto）、无 `v-html` 与 injection matrix 保持一致；unsafe scheme 和 raw HTML 仍按字面文本呈现。
- **技术文本边界**：CommonMark delimiter-run、intraword 星号、Unicode identifier 与反斜杠 escape 均有 pinned tests。
- **dependency evidence**：解析结论直接依赖 `marked` 的 `Lexer.lexInline` Token 形状与 delimiter 语义；ledger 由 lockfile 自动记录 `marked@17.0.6`，版本或 patch 变化只让该 owner stale。

## api-docs-webhook-protocol —— improved（verified）

三段独立省略、ACK 三语义、schedule 双层可访问呈现与 rich fact 显式 opt-in 均保持原行为。本轮只固化辅助文案契约，不改变渲染 DOM：

- `description` 与 `note` 是次级说明层，始终按纯文本呈现；Markdown 标记不会被隐式解释。
- 需要链接、行内代码或强调时，内容进入 `fact.value` 并显式使用 `format: 'inline-markdown'`。
- 若未来出现辅助说明必须承载富文本的真实需求，应新增显式 format opt-in；不得改变现有字符串的默认解释。
- 契约同步写入 `WebhookProtocolSectionData` 类型注释与权威 reference，并用黑盒 component test 断言 description 中的 link/strong Markdown 保持字面文本。
- evidence 继续绑定直接消费的 `CodeBlock.vue`、`FieldGroup.vue` 与 `scripts/check-registry-consumer.mjs`，避免依赖实现或 consumer closure gate 变化绕过 owner stale 检查。

该取舍在 finding 首次进入 canonical ledger 前已由人工确认，因此本轮将其记录为 by-design verified contract，而不是先制造一个已决定不修的 open finding。recorder 也禁止新 finding 直接标记 `wont-fix`，避免自动流程伪造人工处置历史。

## api-docs-field-group —— required co-review（verified）

FieldGroup 的既有 scope 包含 `WebhookProtocol.vue`，因此类型注释变更触发 required co-review。复审确认 FieldGroup 源码、公开 API、布局与 a11y 行为均未变化；recorder 将其 evidence 与三个计划组件统一绑定到 `123dd721`。

## 反向横扫（每日）

- **rAF 合并测量调度**：SplitPane / CodeRail / EnumTable / SidebarScenarioTags 已由 #96 收敛到 `useRafTask`，未发现遗留的第二套 owner 内实现。
- **authored copy 渲染路径**：FieldItem / EnumTable 的富文本正文与 WebhookProtocol 的显式 `fact.value` format 属于各自公开契约；description/note 的纯文本边界已记录，不再作为不明行为重复提出。
- **section 省略 / show-more**：WebhookProtocol 与 SchemaComposition 的语义域不同，无同构抽取价值。

## Gate

`registry:validate` ✅（39 items / 54 files）· `test:registry` ✅（131 tests）· `test:component` ✅（27 files / 301 tests）· `typecheck` ✅ · `build` ✅（root CSS 8 markers）· `test:consumer -- --skip-install` ✅（legacy config migration + all-items runtime closure）· `audit:verify` ✅。

Build 中外部 font provider 因沙箱 DNS 不可达产生非阻断 warning；本地 icon collection、client/server build、Nitro output 与 root CSS gate 均完成。

## 遗留与下一步

1. `api-docs-field-annotation` 仍有 1 个 low open finding，待算法重排。
2. `foundation-inline-code` 仍为 legacy-v1，待下次排程升级。
3. 当前仍有 1 个未审计组件；不在本批算法结果中，不手工扩围。
