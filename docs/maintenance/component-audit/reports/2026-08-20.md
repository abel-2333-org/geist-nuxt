# 2026-08-20 组件审计

- **轮次**:round 2(`roundRollover: false`,不触发轮末横切扫描)
- **基线**:`origin/main` @ `3c079e678acea2fce11fe0d18ace831ece73bfa3`
- **本批组件**:`api-docs-lifecycle-notice`、`api-docs-code-block`、`api-docs-response-example`(共 1095 LOC)
- **选簇原因**:接续上次审计簇——LifecycleNotice 直接依赖上批的 lifecycle-badge 并共享 badge-utils;ResponseExample 直接依赖 CodeBlock。
- **结论**:无代码改动(metadata-only)。两个 item `verified`;`api-docs-response-example` 记 1 条新 low finding(与簇外 RequestExample 的受控 seam 重复),转 `deferred` 交人工决策。

## 各组件结论

### api-docs-lifecycle-notice — clean(change: none, status: verified)

50 行 `UAlert` 薄包装,契约全部成立:

- preset 派生:color/icon/label 均取自 `lifecyclePreset` + `BADGE_TONE_COLOR`,与 LifecycleBadge 共词表(`references/kits/api-docs/index.md` 「同词表的多形态」条款),改词汇一处生效两形态;
- 空白 `title` 覆盖回落 preset 文案(`props.title?.trim()` 判断),满足 `references/components/data-display.md` 「LifecycleBadge / LifecycleNotice 空覆盖回落 preset 文案」的非空文本契约;
- 不可 dismiss、copy-agnostic(props/slot 注入,无硬编码业务文案);`#default` slot 优先于 `description` prop,与注释一致;
- 含义由 icon + 非空 title 承载,不单靠颜色;
- `tests/component/chrome-labels.spec.ts` 已锁定共词表与 localizable title 行为。

### api-docs-code-block — clean(change: none, status: verified)

前两轮修复(语言选择器空态可见性、代码区键盘可达、#79 双主题 fragment)均由 `tests/component/code-block.spec.ts` 持续锁定,本轮逐条复核无回归:

- 语言选择器是选择控件,>1 语言时空态仍可见;换行/复制等内容绑定控件随无 code 隐藏;
- 代码滚动区是 `tabindex="0"` 命名 group(非 landmark),`<pre translate="no">`;
- `trustHighlightedHtml` 显式 trust gate:未开启时转义直出,开启时复制真源仍是 `variant.code`;dark 切换仅命中声明 `--shiki-dark` 的 token;
- `labels` 合并保持 `copySuccess ?? copied` 兼容层,本地化不拼半句;`langLabel` 回退链(overrides → preset → capitalize)与 `code-sample.md` 一致;
- `useCodeWrap` 共享 + cookie 持久化,SSR 安全,`useState` 单一真源。

**为什么没改看起来可改的**:`props.variants ?? []` 在 `withDefaults` 下是死分支,但无行为影响,删除属无谓 churn;空态面板样式与 ResponseExample 语义面板相似(见下),判为可接受重复。

### api-docs-response-example — deferred(change: none,1 条新 low finding)

行为契约逐条核过,与 `response-example.md` 及 505 行 focused tests 一致:

- 三级选择(scenario → status → body)均为派生收敛:fallback 只派生不回写不发事件;身份键(id/status)稳定跨重排,移除即丢弃、重加不复活;
- controlled seam 挂载时一次性判定;status 刻意留在受控口外;
- dev 模式重复身份键 `console.warn` 按消息去重;
- a11y:常驻 sr-only `role="status"` live region、彩色 badge 文本+颜色双通道、三选择器带 aria-label、窄容器触发器 aria-label 串全三维、download 是真实链接含文件名;
- 容器查询(`@xl/response`)窄化为单触发器 + 基数分型 popover(场景 select、status/media radio);ResizeObserver 在触发器隐藏时关闭已 portalled 的 popover,卸载时 disconnect。

**新 finding `f-a35a4e313b64`(low,open)**:反向横扫命中簇外 `RequestExample.vue`——scenario 受控 seam(controlled 检测、localScenario、收敛 computed、仅显式选择 emit 的 setter、pre-flush id 收敛 watch)两处逐行同构,且 RequestExample 注释明写 "keeps the seam aligned with ResponseExample",靠人工纪律维持对齐;ResponseExample 内部 status/body 两维又重复同一收敛形状。建议抽成 api-docs kit composable(如 `useConvergedSelection`)供两个 example 共享。抽取涉及两个组件 + registry 依赖闭包变更,属跨组件破坏性改动且 RequestExample 在当日簇外,按契约记 deferred 交人工决策;若人工采纳,建议单独开实现 PR 并把 RequestExample 一并纳入 co-review。

**为什么没直接修**:两处实现当前行为一致(风险是未来漂移而非现存 bug),且同 PR 改完所有调用点会把簇外组件拉进本批,违反「与当日簇无关的改动不塞进本 PR」。

## 反向横扫(其余维度)

- 空态/语义面板模式(`flex flex-col items-center justify-center … py-12 text-center` + 8 号 dimmed 图标 + 标题 + hint):仅命中本簇内 CodeBlock 空态与 ResponseExample 三个语义面板,语义刻意不同(「缺示例」≠「协议空正文」),抽公共原子收益低于间接成本,不记 finding;
- `USelect` xs/neutral/subtle + `min-w-fit` content 工具栏形态:命中 RequestExample/CodeBlock/ResponseExample 三处,属同一工具栏家族的既定形态约定(reference 已成文),非逻辑重复;
- `langLabel` / `warnOnce` / ResizeObserver 各命中面均已在簇内或有独立职责,无新增候选。

## Gate 输出

- `pnpm registry:validate`:39 items / 54 files 通过
- `pnpm test:registry`:通过(cancelled/skipped/todo 均 0)
- `pnpm test:component`:29 files / 310 tests 全通过
- `pnpm typecheck`:通过
- `pnpm build`:通过(含 postbuild root CSS check)
- `pnpm test:consumer`:通过
- `pnpm audit:verify`:record 前后各跑一次,均通过
- 无代码改动,无功能提交;本 PR 仅含 ledger + 报告簿记提交

## 遗留与建议下一步

- `api-docs-response-example` 的 open finding `f-a35a4e313b64`(受控 seam 抽取)待人工判定:采纳则单独开实现 PR(涉及 RequestExample、ResponseExample、可能新增 composable registry item);不采纳可由人工标 `wont-fix`。
- round 2 本批记录前 pool 为 14(含本批 3 个),记录后剩 11 个;`foundation-use-raf-task` 仍是唯一未审计 owner,等待后续排程。
