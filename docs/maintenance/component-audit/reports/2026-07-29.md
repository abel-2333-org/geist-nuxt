# 2026-07-29 组件审计

第 1 轮 · 第 2 天。本轮剩余 23 / 28 个组件。

## 为什么是这一簇

昨天审计的是 `foundation-inline-markdown` + `api-docs-field-item`。今天沿依赖边扩张：

- **`api-docs-enum-table`** —— 直接依赖昨天的 seed `foundation-inline-markdown`，
  又被昨天的 `api-docs-field-item` 直接依赖，处在昨天那条边的正中间；
- **`api-docs-field-annotation`** —— 与上述两者共享 `foundation-inline-code` +
  `foundation-inline-markdown`，且依赖 `api-docs-field-item`（借它带入 `FieldNode`
  与 `useFieldAnchor`）；
- **`foundation-inline-code`** —— 前两者的共同依赖，被 5 个 item 依赖，是这簇的公共底。

三者合计 405 行，是天然的连通子图：一个原子 + 一个消费它的表格 + 一个消费它的注释形态。

| item | verdict |
|---|---|
| `api-docs-enum-table` | **improved** |
| `api-docs-field-annotation` | **improved** |
| `foundation-inline-code` | **clean** |

---

## api-docs-enum-table — improved

`kits/api-docs/components/EnumTable.vue`（153 行）+ `kits/api-docs/utils/enum.ts`（45 行）。
被 `api-docs-field-item` 嵌在每一个带 enum 的字段行里，所以它的缺陷会按字段数量放大。

### 改动 1：`EnumVariant.when` 声明了却从不渲染（静默丢信息）

`utils/enum.ts` 里 `EnumVariant.when` 带着 JSDoc「When this group of values applies
(already localized)」，`references/kits/api-docs/index.md` 也把 variants 形态描述为
「取值随条件变化」——但 `EnumTable.vue` 的模板**从头到尾没有读过这个字段**。作者写了
这句话，读者永远看不到。

这不是「少一个可选特性」，是契约层已经承诺、实现层没有兑现。修法选渲染而非删字段：
删 `when` 是 public type 的破坏性收窄，且丢掉的正是 variants 形态存在的理由
（tab 标题只**命名**分组，`when` 才回答「我是不是在这一组」）。

渲染位置在 variant 选择器之下、值表之上，读序为「有哪几组 → 这一组何时适用 → 组内取值」。
经 `InlineMarkdown` 渲染，与本 kit 所有作者文案同一条通路。

**刻意用中性色而不是 amber**：`FieldItem.vue` 里那套注释（第 2 条 Condition 规则）
把 amber 家族按**填充度**分了四级——文字标签 CONDITIONAL → 带边线规则 → 填充 callout
→ Beta 徽章。EnumTable 是嵌在字段行**内部**的，这里再加一个 amber 对象会把那套分级压平。
依据：`references/foundations/focus-a11y.md`「不要只用颜色表达状态」+ FieldItem 既有的
amber 阶梯注释。

### 改动 2：筛选没有 live region

`SidebarNav.vue`（同 kit 的另一个可筛选列表）早就有 `resultsAnnouncement` /
`noResultsAnnouncement` + `sr-only role="status" aria-live="polite"`；EnumTable 的
筛选框重写整张表却是静默的，屏幕阅读器用户得不到任何反馈。

依据：Web Interface Guidelines「Async updates (toasts, validation) need
`aria-live="polite"`」+ 仓库自身先例。**契约形状照抄 SidebarNav**（`(count: number) => string`
/ `(query: string) => string`），不另发明一套——kit 里两个可筛选列表用一个契约形状。

默认文案取本表的领域词而非 SidebarNav 的「result」：空态本来就写作
`No matching values`，播报就是 `N values found` / `No matching values for “q”`。

### 改动 3：滚动框键盘不可达

`filterable` 时值表进 `max-h-80 overflow-y-auto` 的有界滚动区，而框内**全是静态文本**
（`<dl>` / `<dt>` / `<dd>`），没有任何可聚焦元素。结果是键盘 only 用户根本滚不到折叠线
以下的取值——这正是长枚举（≥30 项）才会触发的路径，也就是最需要滚动的场合。

依据：`references/foundations/focus-a11y.md`「键盘可达：所有可点击元素用真正的
`<button>`/`<a>` 或带 `tabindex`/`role` 的元素」；WCAG 2.1.1。

修法是给滚动框本身一个 tab stop：`tabindex="0"` + `role="group"` + 用 `label` 命名
+ 系统 focus 环（`focus-visible:outline-2 outline-offset-2 outline-primary`，
照 focus-a11y.md 的自建交互元素规格）。**只在 `filterable` 时加**——短表不滚动，
给它一个无意义的 tab stop 反而是噪音。

### 改动 4：一次筛选，两处使用

改前 tab badge 的计数和面板实际渲染的列表**各跑一次 `filterValues`**，两者数值一致
只是巧合。改成单个 `filteredVariants` computed 同时喂 badge 和 body，一致性由数据结构
保证。

顺带修掉一个边界：`variants` 缩短时 `activeTab` 会滞留在已消失的 tab 上，渲染出空态
而不是回落到第一组。`activeIndex` 现在做 clamp，且 `UTabs` 从 `v-model="activeTab"`
改为 `:model-value="String(activeIndex)"` + `@update:model-value`——高亮的 pill 与
渲染的面板读同一个 clamp 后的值，两者不可能不一致。若只 clamp 渲染而 `v-model` 仍读
原始 ref，会出现「面板显示第一组、但没有任何 pill 高亮」的状态。

依据：`vue-best-practices`「Keep source state minimal, derive everything possible
with `computed`」「Avoid recomputing expensive logic」。

### 连带改动：FieldItem 的透传键

`references/kits/api-docs/index.md` 第 285 行立了规矩：「nested chrome 本地化走同一个
`labels` 对象，不 fork 组件」。新增两个播报函数若不透传，双语消费方会拿到英文播报，
破坏那条约定。故 `FieldItemLabels` 加 `enumResults` / `enumNoResults` 两个透传键
（纯增量，可选，无默认值——沿用「默认字符串只存在于子组件一处」的既有做法）。

`typecheck` 当场逮到 `PassthroughLabel` 联合类型没同步，已补。这个类型正是为了防止
新透传键漏进 `t` 的默认值对象而存在的，它按设计工作了。

### 为什么没改看起来可改的地方

- **`filterValues` 每次查询为每个 variant 分配新对象数组**——文档规模（单表几十到
  上百项）下是噪音级开销。`vue-best-practices` 的性能章明确说性能优化是行为正确之后
  的独立 pass，这里没有可测量的问题。
- **播报只反映当前 variant 的命中数**，而非全部 variant 的总数。有意为之：live region
  的职责是描述**已渲染区域**发生了什么；其他 tab 的命中数由 badge 承载。若播报总数，
  当前 tab 空而别处有命中时，读者会听到一个和眼前空态矛盾的数字。这一点已记为 open
  finding，等有真实双语 + 屏幕阅读器实测再定。
- **`<dl>` 没有 `aria-labelledby` 指向上方的 `LABEL (N)`**——`role="group"` 已带
  `aria-label`，再挂一层会造成重复播报。

---

## api-docs-field-annotation — improved

`kits/api-docs/components/FieldAnnotation.vue`（148 行）+
`kits/api-docs/composables/useFieldSource.ts`（41 行）。

### 改动：未命中 ref 在 dev 下静默

组件头部注释写着「Unresolvable `field-ref` degrades to plain text（same policy as
TermAnnotation）」，但只抄了策略的一半：`TermAnnotation.vue` 在 `import.meta.dev` 下
`console.warn` 未命中的 glossary id，`FieldAnnotation` 没有。

降级对**读者**是正确的运行时行为（stale markdown 永不破页），但对**作者**必须留下诊断
——否则一个 `provideFieldSource` 里漏登记的 id，表现就是页面上少了个浮层，没人会发现。
家族两个形态共用降级策略，诊断也该共用。

依据：`references/components/overlays.md` 的 Annotation 家族契约（降级策略是家族级的）
+ 组件自身注释声明的 parity。

告警只在 `fieldRef` 存在且解析不到时触发——直传 `field` 而 `field` 为空是调用方自己的事，
不该由组件唠叨。

gallery 里 `field-ref="create-deployment.removed"` 是**有意**的降级演示，现在会打一条
dev warning；这与 `app/components/gallery/Overlays.vue` 里 `TermAnnotation
id="does-not-exist"` 的既有行为完全对称，是预期的。

复查时另补了跨页 fragment 的 round-trip：`FieldAnnotation` 对 raw `FieldNode.path`
编码一次；Vue Router 解析字符串 URL 后，`to.hash` 已经是 DOM id 所需的字面值，
`app/router.options.ts` 不再二次 `decodeURIComponent`。契约测试覆盖了包含字面 `%`
的 path，避免链接 href 正确、落地滚动却回到页首。

### 为什么没改看起来可改的地方

- **`t` computed 的 labels 合并模式在三个形态里重复**（`TermAnnotation` /
  `DocAnnotation` / `FieldAnnotation` 各写一遍 4 行 spread）。抽成共享 helper 要跨
  foundation ↔ kit 边界再加一个自动导入符号，换来省 12 行样板——收益不抵间接层，
  且 `AGENTS.md` 的 kit 依赖规则下这个 helper 只能落 foundation，让 kit 组件为了
  4 行 spread 多一条依赖边。不改。
- **`normalizePath` 是本文件私有的 5 行函数**，只有一个调用点，没有第二个消费者，
  不构成抽公共 util 的理由（`component-reflow.md` 采纳标准第 3 条「至少有明确的复用场景」）。
- **`useFieldSource` 注入的是普通对象而非 reactive**——与 foundation 的 `useGlossary`
  完全同构，是家族一致的刻意选择（页面级静态数据）。单方面改会让两个孪生 composable 漂移。
- **未动 props / emits / slots 公开契约**：`dependedBy` 为空，但已 copy-in 的消费项目
  不可见，破坏性变更的收益这里不存在。

---

## foundation-inline-code — clean

`foundation/components/InlineCode.vue`（18 行）。被 5 个 item 依赖
（`foundation-inline-markdown`、`api-docs-enum-table`、`api-docs-field-item`、
`api-docs-field-annotation`、`api-docs-webhook-protocol`），是这簇里最底层的原子。

**逐条过了四个维度，没有可做的改动：**

- **晋升**：已在 foundation 根，位置正确（跨场景裸名原子，符合 `component-reflow.md`
  第 3 节「跨场景裸名组件可放 `foundation/components/` 根」）。
- **精简**：18 行、单一职责、零 props。它把 `ProseCode` 的两处主题默认拉回 Geist
  基线——圆角回控件档（`rounded-sm`，6px）、字号回 Copy 13 Mono（`text-code`，
  对应 `foundation/assets/css/main.css` 的 `--text-code: 0.8125rem`）。两处偏离都
  在源码里写明了原因，没有无据硬编码。
- **优化**：非交互元素，无 focus / aria / 键盘面。明暗主题由 `ProseCode` 的语义
  token 承担。换行策略**刻意不内建**：调用方按上下文传 `break-all`（EnumTable 的值列，
  窄列要断得狠）或 `wrap-anywhere`（FieldAnnotation 的浮层，宽度自由），单根节点
  attrs fallthrough 自动合并 class。这比在组件里塞一个 `wrap` prop 更对——
  `vue-best-practices`「Fallthrough attributes: wrapper/base components must
  forward attrs/events safely」。
- **重构**：无 script 逻辑可写坏；命名符合 `component-reflow.md` 三原则（`InlineCode`
  是成熟 UI 模式词，描述形态而非内容）。

**没有为了产出而制造 churn。** 唯一一处犹豫是空的 `<script setup lang="ts">` 块
（只装注释），但它承载的是两处主题偏离的**决策记录**，删掉会丢掉「为什么不用
`ProseCode` 默认值」这个信息，得不偿失。

---

## Gate 结果

全绿。

```
pnpm registry:validate  → exit 0   Registry valid: 39 items, 51 files
pnpm test:registry      → exit 0   # tests 52 / # pass 52 / # fail 0
pnpm typecheck          → exit 0   （首跑 exit 2，见下）
pnpm build              → exit 0   Σ Total size: 14 MB (4.08 MB gzip)
                                   Root gallery CSS source check passed: 8 markers
pnpm test:consumer      → exit 0   44 个 smoke 场景全过
pnpm test:component     → exit 0   Test Files 11 passed / Tests 132 passed
```

`test:consumer` 的 44 个场景里，今天动过的两个切片各有专属场景，都是在独立 Nuxt 项目里
按 registry copy-in 后 typecheck + build：

```
Consumer closure smoke passed (api-docs-field-item)
Consumer closure smoke passed (api-docs-field-annotation)
Consumer closure smoke passed (all-items)
```

这条比根 build 更有意义：`EnumTableProps` / `FieldItemLabels` 这些类型住在 `utils/`
而非 SFC 里，正是为了在源码仓库（`kits/api-docs/utils`）和 copy-in 之后
（`app/utils`）两种拓扑下裸引用都能解析。新增的两个播报 props 走的是同一条缝，
consumer gate 是唯一能证明它没漏的地方。

`typecheck` 首跑 exit 2，报 `FieldItem.vue(79,11) TS2769`：新增的 `enumResults` /
`enumNoResults` 没加进 `PassthroughLabel` 联合类型，于是被 `Required<Omit<…>>`
要求提供默认值。补上联合类型后通过——这正是那个类型存在的目的。

新增 10 条 `tests/component/enum-table.spec.ts`（variant `when` 渲染与切换、无 `when`
时不渲染空节点、滚动框 tab stop 的有无、live region 的命中 / 未命中 / 空闲三态、
单复数、badge 与 body 计数一致、variants 缩短时回落第一组且扩容不复活旧选择、
筛选框随数据缩短消失时清理隐藏 query），并在
`chrome-labels.spec.ts` 补了两个播报函数的中文透传断言。

### 未做的验证（如实记录）

**没有跑真实浏览器预览。** 预览工具绑定 session 的主工作目录（用户的
`/Users/abel.wang/projects/geist-nuxt`），而不是审计用的隔离 worktree；本轮不允许动
用户工作区，所以明暗主题 / 390px→宽屏 / 键盘 focus 环是**按 token 契约推断**而非实测：

- 新增的 `when` 说明用 `text-xs leading-relaxed text-muted`，focus 环用
  `outline-primary`——全是语义 token，没有引入任何硬编码颜色、圆角或尺寸，明暗两套
  由 token 系统承担（`references/foundations/tokens.md`）；
- 没有新增媒体查询；gallery 里那个 `lg:grid-cols-2` 用的是系统断点，`lg` 以下自然堆叠；
- tab stop 与 live region 的存在性、命名、class 由 9 条组件测试断言（见上）。

评审时如需实测，在本分支跑 `pnpm dev` 打开 `/kits/api-docs` 的「Enum 值表」一节即可。

### 环境 flake

首次跑 `pnpm test:component` 时出现 7 个文件级 `Test timed out in 5000ms` /
`Hook timed out in 10000ms`（`response-example`、`schema-composition`、
`operation-target`、`annotation-popover` 等**未改动**的文件）。第二次运行同一命令
全绿，`transform 340s → 35s`。判定为冷 transform 导致的环境 flake，与本次改动无关。

## 遗留待办

- **`api-docs-enum-table`**：live region 只播报当前 variant 的命中数；其他 variant
  的命中仅由 tab badge（视觉）承载。等真实屏幕阅读器实测再定是否补一句「其他分组还有 N 项」。
- **`api-docs-field-annotation`**：dev-only 的 `console.warn` 分支在
  `@nuxt/test-utils` 下不可达（`import.meta.dev` 为 false），故无测试覆盖。
  `TermAnnotation` 的同类告警有同一盲区。若要覆盖，需要么在 vitest 环境注入 dev 标志，
  要么把诊断抽成可直接单测的 util——两者都超出本次范围，且会影响 foundation 侧组件。
- **`foundation-inline-markdown`**：本次审计起点 `b63513b` 上仍有昨天的两条 delimiter-run
  finding；随后 PR #50 已用 inline lexer + 受控 token renderer 修复，并于 `b497e08`
  合入 `main`。本 PR rebase 后同步清空对应 v1 `openFindings`。

## 建议的下一步

沿今天这簇继续扩张，优先 `foundation-annotation-popover`（今天 FieldAnnotation 的壳，
且是 Annotation 家族三形态的公共交互层，焦点管理逻辑最密集）与
`foundation-term-annotation` / `foundation-doc-annotation`——三形态一起看才能判断
上面那条「`t` computed 重复」到底该不该抽。
