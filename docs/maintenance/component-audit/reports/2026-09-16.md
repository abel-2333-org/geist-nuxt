# 2026-09-16 组件审计

- **Round**:4(第二天,`roundRollover: false`,不执行 §5b 轮末横切;`remainingInRound: 24`)
- **Base main SHA**:`2a3c941c5494f9bee2a58f961ded7a6b64d976be`
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>`,`openFindingReentry` / `priorityBacklog` 均为空(全仓 open finding 队列为空)
- **选簇原因**:seed 取与上批(`api-docs-field-item` + `api-docs-enum-table`)关系分最高的 `api-docs-field-annotation`(直接依赖 field-item,并共享 `foundation-inline-code` / `foundation-inline-markdown`);扩张取其直接依赖 `foundation-inline-markdown`,再取共享两个 foundation 依赖的 `api-docs-webhook-protocol`,合计 698 LOC,本批 3 个组件
- **开放 PR 检查**:`gh pr list --state open` 为空,无需继续既有 audit PR
- **自上次 evidence 以来的变更**:`api-docs-field-annotation` 与 `api-docs-webhook-protocol` 的 evidence `headSha` 均为 `5f0fe3a`(09-10 co-review,当时明确「不是从零重审全部实现」);`foundation-inline-markdown` 为 `239b6a8`(08-31 全量 clean)。`git diff <headSha>..HEAD` 对三项 owning files、tests、references 与 gallery 页面均**零变更**。因此本次对两个 kit 组件是**首次从零全量复审**(它们上一次全量审计分别在 08-31 前),对 InlineMarkdown 是同一快照的第二次全量复审

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `api-docs-field-annotation` | none | verified | clean;四维度无 finding。两条既有 `wont-fix`(trailing-slash 归一化、labels 合并 idiom)原样保留 |
| `foundation-inline-markdown` | none | verified | clean;四维度无 finding;`marked` 仍解析为 17.0.6,dependency evidence 前提未变 |
| `api-docs-webhook-protocol` | **modified** | verified | 唯一改动:收窄 FactRow 注释与 reference 中「随多个 registry 切片分发 / 所有含 FactRow 的切片」这条与 registry 事实不符的 universal claim;组件行为、API、测试零变化 |

## 各组件详情

### api-docs-field-annotation(FieldAnnotation + useFieldSource,206 行)

历次:08-1x 首审、08-29 modified(PR #110:panel 内长文本收缩换行、未登记 field-ref dev 告警)、09-10 co-review。本次按五份规则源重过四维度:

- **晋升**:Annotation 家族的字段形态,绑定 `FieldNode` + `useFieldAnchor`,不满足下沉 foundation 的跨领域条件;kit 归属正确。registry 拓扑核对:`files[]` 2 个文件与源码 import 图一致(`useFieldSource` 随本切片分发;`FieldNode` / `fieldRequiredState` / `useFieldAnchor` 经 `api-docs-field-item` 闭包传入,不重列);`registryDependencies` 4 项与模板实际引用(AnnotationPopover / InlineCode / InlineMarkdown / UButton)一致;无 kit → kit 边。
- **精简**:`useFieldSource` 与 foundation `useGlossary` 互为镜像(源码注释自述「Mirror of useGlossary」),差异只在 entry 形状;可抽的只有一个 `InjectionKey + provide + inject` 工厂,评估结论登记为候选 9「不建议抽取」(见下方反向横扫)。`normalizePath` 与 SidebarNav 的 trailing-slash 重复是既有 `wont-fix`(`f-31f690a2dbaa`,候选 2),labels 合并 idiom 是既有 `wont-fix`(`f-fd7282669072`,候选 3),均不重新评估。
- **优化(复核无回归)**:trigger 由 `AnnotationPopover` 提供(`aria-haspopup="dialog"` + `aria-expanded`,`focus-visible:outline-2 outline-primary`);popover 内必填标记同时用 uppercase 文案与 `text-error` / `text-warning`,不只靠颜色;类型 / format 两个 span 与描述段落 `wrap-anywhere min-w-0`,长 union 类型不撑破固定宽 panel(`annotation-forms.spec.ts` 断言);跨页动作是 `UButton :to` → `<a>`(导航用链接,不用 button 伪装),同页动作是 `<button>`;色彩全语义 token,字号走 `text-code`(13px,`--text-code` 语义 utility),下划线 `decoration-(--ui-primary)/50` 是 Tailwind v4 变量 + 透明度修饰符——已在部署 gallery 用 computed style 核证编译为 `oklab(… / 0.5)`,不是静默失效的 class。
- **重构**:`<script setup>` + TS;`FieldAnnotationLabels extends AnnotationPopoverLabels`,`t` computed 收窄为 `Required<Pick<…>>`;props / emits / slots 契约未变;`if (import.meta.dev || import.meta.test)` 包裹的 `watchEffect` 告警只诊断本组件的解析步骤,不进 kit 公共面(09-09 已裁定 5 处 dev 诊断样板不抽)。
- **Universal claims 核证**(源码注释 / reference / 测试名):
  - 「Unresolvable `field-ref` degrades to plain text … so stale ids **never** break the narrative」—— `degrades an unresolved field ref to plain slot text` 用例 + 部署 gallery 中 `legacyRegion` 渲染为纯文本(无 button),成立;
  - 「the target page's initFromHash() expands + scrolls + flashes on arrival」—— 跨页 href 用 `encodeURIComponent(path)`,`useFieldAnchor.initFromHash` 对 `location.hash` 做 `decodeURIComponent`,`res_state%25` → `res_state%2525` → 解回 `res_state%25`,编解码对称(`renders a cross-page field action with its target hash` 用例);
  - 「Focus lands on the target row」—— 部署 gallery 真实指针点击 `repoId` → 「查看字段详情」:popover ≈150ms 关闭(焦点先按 `AnnotationPopover` 的 close 规则回到 trigger),≈900ms 后(等待折叠的 `gitSource` 祖先展开并稳定 4 帧)滚动到目标行(`top: 96px`,即 `scroll-mt-24`)且 `document.activeElement` 为 `#body_gitSource_repoId`,成立。**注意**:用 `element.click()` 合成点击 trigger 时该流程不会完成(popover 不关闭、不滚动),这是合成事件绕过 reka 指针状态机的伪影,不是组件缺陷;记录在此避免下次复审误判。
- **为什么没改看起来可改的**:`:labels="labels"` 把含 `category` / `required` 等字段形态专属键的整份 labels 透传给 `AnnotationPopover`——壳只 spread 自己认识的键,多余键无副作用,收窄成 `Pick` 只增加一层映射;`crossPageTo` 在显式传 `to` 时即使目标就是当前页也渲染为链接——这是 `to` 的文档化语义(「Explicit cross-page link; overrides」),调用方显式覆盖不应被二次猜测。

### foundation-inline-markdown(InlineMarkdown + parse.ts,135 行)

历次:07-xx 首审、08-1x delimiter-run 改造(两条 low finding resolved,改用 `marked` Lexer)、08-31 round 3 全量 clean。本次全量复审:

- **晋升**:foundation 归属正确,被 6 个 item 依赖(Term/Doc/Field 三形态 + EnumTable + FieldItem + WebhookProtocol);`packageDependencies.marked ^17.0.6`,lockfile 解析仍为 17.0.6,dependency evidence 显式重录。
- **精简**:`parse.ts` 74 行只做「白名单 token → Node」的窄适配,`InlineMarkdown.vue` 61 行只做「Node → VNode」映射,职责已拆;`render(parse(props.text))` 在渲染函数内直接调用而非 `computed`——组件只有一个 string prop,父级以相同字符串重渲时 Vue 的 props 浅比较已跳过子组件更新,`computed` 不带来额外缓存,不改。
- **优化(复核无回归)**:外链 `target="_blank"` 由 `ProseA` → `ULink` 自动补 `rel="noopener"`(`routes internal hrefs and opens external ones in a new tab` 用例);内链走客户端路由;`InlineCode` 输出 `translate="no"`;无任何视觉 class,主题 / 响应式由容器与 Prose 组件承担。
- **重构**:使用 `defineComponent` + `h` 渲染函数而非 `<script setup>`——递归 VNode 映射是模板无法表达的场景(vue-best-practices §3.2 render-functions 的正当使用条件),`resolveComponent('Prose*')` 在 setup 内解析一次;`Node` 是判别联合,`parseToken` 对未知 token 一律 `{ type: 'text', value: token.raw }` 兜底(fail-closed)。
- **Universal claims 核证**:「Raw HTML is never rendered」—— 8 种注入 payload 矩阵(`renders no raw HTML for %j`),成立;「Safe inline markdown subset」(registry description)—— 协议 allowlist(`http:` / `https:` / `mailto:` / 相对地址)+ 4 种 unsafe scheme 用例,成立;「never re-parses a code span」—— `` `a*b*c` `` 用例,成立;「Backslash escapes are preserved verbatim」—— 3 个正则 / glob 用例,成立;reference「不能以“输出中没有 `*` / `_`”作为正确性判断」与 `preserves unmatched excess delimiters according to CommonMark` 用例一致。
- **为什么没改看起来可改的**:HTML 实体(如 `&amp;`)按字面显示而不解码——这是「不注入 marked 的 HTML 输出、不使用 `v-html`」策略的直接结果,与 `<!-- c -->` 等字面保留同属一条规则;为实体单独开解码路径会让「Raw HTML is never rendered」的边界变得需要逐 token 解释,收益是 API 文案里几乎不出现的实体写法,不改。

### api-docs-webhook-protocol(WebhookProtocol + FactList / FactRow + utils/webhook-protocol,357 行)

历次:08-10 抽 FactList / FactRow internal(e9ebe19)、08-11 rich fact contract + toggle 动作文案(PR #77,issue #76)、08-29 finding `f-b1838c3b046a` resolved、09-10 co-review(ACK labels 键改名)。本次是 PR #77 之后**首次从零全量复审**:

- **晋升**:领域件,demo 数据全部内联在两个 gallery 页;registry 拓扑核对:`files[]` 4 个文件与源码 import 图一致(`WebhookProtocol.vue` → `../internal/Fact{List,Row}.vue` / `./CodeBlock.vue` 类型导入 / 自动导入 `hasWebhookProtocolContent` `collapseScheduleSteps`,copy-in 后 `app/components` → `app/internal` 相对路径同构);`registryDependencies` 5 项与模板实际引用(FieldGroup / CodeBlock / InlineMarkdown / InlineCode(经 FactRow)/ UIcon UBadge UButton)一致。
- **精简 → 本次唯一改动(措辞收窄)**:`FactRow.vue` 头注释写「FactRow ships with multiple registry slices, and baking InlineMarkdown in would drag foundation-inline-markdown (+ marked) into **every** slice」,`webhook-protocol.md` §Registry 写「避免**所有**含 FactRow 的切片连带 `foundation-inline-markdown` 依赖」。按 universal-claim 触发器核证:`registry.json` 自 e9ebe19(08-10)引入起,**只有 `api-docs-webhook-protocol` 一个 item** 列出 `internal/FactRow.vue`,且该 item 本就依赖 `foundation-inline-markdown`;`endpoint-reference.vue` / `webhook-reference.vue` / `structured-relations.vue` 三个 gallery 页经相对路径直接引用 internal,不是切片。「多个切片」在 registry 层面从未成立,依据它推导「下沉会污染其它切片」会误导后续复审。处置:把两处措辞收窄为「FactRow 是随任何采纳它的切片一起分发的共享 internal(目前只有本 item 列出它,gallery 参考页经相对路径直接引用),下沉会让此后每个采纳切片连带该依赖」;**format 判别留在 WebhookProtocol 的设计决策与 #76 的再评估条件(第二个已采纳组件真实需要 rich fact)原样保留**。这是 2 处注释 / 文档改动,组件行为、公开类型、测试零变化;因 FactRow 是分发文件,下游 `geist:update` 会看到一行注释 diff,已在 PR 描述注明。
  - *为什么不顺势下沉 `format` 到 FactRow*:前提失效不等于结论反转——下沉后 FactRow 会内置 `InlineMarkdown`,任何未来采纳 FactRow 的切片都连带 `marked`;#76 / PR #77 的人工裁决明确把下沉条件定为「第二个已采纳组件需要 rich fact」,该条件今天仍未满足,改设计决策超出审计授权。
- **优化(复核无回归)**:`headingLevel` 经 FieldGroup 接入大纲(部署 gallery 主 fixture 为 h3、变体为 h4,层级递进);facts 用 `<dl>/<dt>/<dd>`;schedule chips 与箭头逐个 `aria-hidden`(部署 gallery 8 个 chip 全部 `aria-hidden="true"`,箭头图标同样);展开按钮可见文案即可访问名(无 `aria-label` 分裂,chevron `aria-hidden`),`aria-expanded` 切换后 `<dd>` 内追加 sr-only 全序列(部署 gallery 实测「1 分钟 → 5 分钟 → … → 12 小时」);375px 暗色下 `document.documentElement.scrollWidth === 375`、320px stress 容器 `scrollWidth === clientWidth`,无横向溢出;chips `font-mono tabular-nums`;色彩全语义 token,`max-height="16rem"` 是 CodeBlock 的既有 prop 契约值。
- **重构**:`<script setup>` + TS;公开类型 `WebhookProtocolFact` 等内联在 SFC 并由 reference 逐字镜像;`code` 布尔别名已删除的 breaking change 有迁移段落;`factFormat()` 默认 `'text'` 与 reference「未提供 format 的 fact 始终按纯文本渲染」一致。
- **Universal claims 核证**:「绝不渲染空卡片或 "none"」—— `hasWebhookProtocolContent` + `只传标题或空 facts 的 section 不进入文档大纲` 用例 + gallery `omittedVerification` 变体,成立;「渲染行为只由该字段驱动,绝不由 value 内容触发」—— `defaults to plain text: markdown syntax in value stays literal` 用例,成立;「unsafe scheme 与 raw HTML 不会被执行」—— `keeps InlineMarkdown safety` 用例,成立;「总结句是可访问真源」—— 折叠态无 sr-only、展开态才追加(`expanding reveals the full sequence` 用例),成立;「非法值安全退化为不折叠」—— `非法阈值时不折叠` 用例,成立。
- **为什么没改看起来可改的**:
  - `#value` slot 的 inline-markdown 分支手写了与 FactRow 默认 slot 相同的 3 行 `note` 段落——把 `note` 挪到 FactRow slot 之外会改变 internal slot 契约(所有 `#value` 消费者都会多出一段 note),3 个 gallery 页直接引用 FactRow,收益 3 行,不改;
  - `scheduleExpanded` 在 `steps` 变化时不复位——纯展示件,steps 变化只来自调用方换数据,展开态保留反而避免读者视角跳变;
  - `:key="fact.term"` 在重复 term 时触发 Vue duplicate-key 警告——与 09-15 EnumTable `:key="item.value"` 同一裁定:作者数据错误在 UI 上本就是可见的重复行,不加第二层 dev 诊断;
  - schedule 只有 `summary` 为空白时 `hasWebhookProtocolContent` 判无内容、而模板在 section 因其它正文进入大纲时仍渲染空 summary 的 schedule 行——`summary: string` 是必填类型,空白是类型系统之外的作者错误,reference 已把总结句定义为「可访问真源」,不为其增加运行时守卫。

## 反向横扫(簇外重复检查)

基线:`2a3c941` 的 `extraction-candidates.md`(候选 1–8,无待决策)。对今日组件提取的特征模式全仓 `rg` 反查:

| 模式 | 命中 | 判定 |
|---|---|---|
| `InjectionKey<Record<string, Entry>>` + `provide` / `inject(KEY, {})` 页面级映射 composable | `useGlossary`(foundation)、`useFieldSource`(kit) | **新增候选 9,不建议抽取**:可共享的只有一个 ≈8 行工厂,两处各省 ≈5 行,但 entry 类型与 `InjectionKey` 显式声明仍逐处保留,且 kit 会新增一条 foundation util 隐式依赖;与候选 3 尺度同构。两处降级 / 告警行为一致,无契约漂移,不记 finding |
| aria-hidden 视觉 chips + `sr-only` 全序列文本 | WebhookProtocol(仅展开态追加,折叠态真源是总结句)、`SidebarScenarioTags`(始终追加) | 播报时机与真源不同,属同一 a11y 契约的两种实现;抽共享原子需参数化「何时播报」,不抽 |
| `has*Content` section 省略判定 | 仅 `hasWebhookProtocolContent` | 全仓唯一,无重复 |
| `format: 'text' \| 'code' \| 'inline-markdown'` 判别 | 仅 WebhookProtocol | 全仓唯一;FactRow 下沉条件见 #76 与本次收窄后的 reference |
| `line-clamp-4 wrap-anywhere leading-relaxed text-muted` 描述段落 | FieldAnnotation、DocAnnotation | Annotation 家族形态矩阵刻意共享的 class 组合(壳统一 panel 宽度,形态统一描述截断),不抽 |
| `decoration-(--ui-primary)/50 hover:decoration-(--ui-primary)` 触发器下划线 | FieldAnnotation、DocAnnotation(TermAnnotation 用 `--ui-text-dimmed`,概念形态刻意更弱) | 同上,家族矩阵的形态档位,不抽 |
| `normalizePath` trailing-slash | FieldAnnotation、SidebarNav | 候选 2 `wont-fix`,不重复评估 |
| labels 合并 `{ ...defaults, ...props.labels }` | 11 处 | 候选 3,不重复评估 |
| `text-error` / `text-warning` uppercase 必填标记 | FieldItem、FieldAnnotation | 候选 4,不重复评估 |
| `import.meta.dev \|\| import.meta.test` + `console.warn` | 5 处 | 09-09 已裁定不抽 |
| `resolveComponent('Prose*')` | 仅 InlineMarkdown(渲染器映射) | 09-15 已比对,语义不同于可选依赖查找,不抽 |
| fact-row `<dl>` 族(FactRow `w-36` flex reflow vs FieldItem `fit-content(8rem)` grid) | — | Round 2 / 3 覆盖史已比对,不重开 |

**登记表本次新增候选 9(不建议抽取)并更新覆盖史;无待决策候选,不查询 / 不新建 GitHub issue。**

## Gate

在功能提交 `7b95b99` 上执行:

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 40 items / 58 files |
| `pnpm test:registry` | ✅ 131 pass / 0 fail |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ postbuild root CSS check 8 markers |
| `pnpm test:consumer` | ✅ 43 个 closure smoke(含 legacy-config 迁移)全部通过 |
| `pnpm test:component` | ✅ 32 files / 401 tests |
| `pnpm audit:verify`(record 前) | 预期 ❌:仅 `api-docs-webhook-protocol` 因本次 scope 改动 stale,其余 28 项成立 |
| `pnpm audit:verify`(record 后) | ✅ 29 个 item evidence 全部成立;legacy 0、deferred 0、未审计 0 |

功能提交 `fix(audit)` = evidence `headSha`(见 ledger);簿记提交在其后。

### 浏览器核证

已部署 gallery `https://geist-nuxt-gallery.vercel.app`(main 自 09-10 起本簇分发文件零变更,与 evidence 快照内容一致):

- `/kits/api-docs/webhook-protocol` 375×812 暗色:`scrollWidth === 375`,320px stress 容器无溢出;8 个 chip 与全部箭头 `aria-hidden="true"`;4 个 `aria-expanded` 按钮均无 `aria-label`、chevron `aria-hidden`;点击「展开其余 3 档」→ `aria-expanded="true"`、文案变「收起」、`<dd>` 内出现 sr-only 全序列、chip 由 5 变 8;rich fact 内链 `href="/kits/api-docs/webhook-reference"` 无 `target`;
- `/kits/api-docs` 1280 宽浅色:3 个 FieldAnnotation trigger `aria-haspopup="dialog"`、`text-decoration-color` 为 50% 透明 primary、字号 13px;`state` popover 动作为 `<a href="/kits/api-docs/endpoint-reference#res_state">`;`repoId` popover 真实指针点击「查看字段详情」→ popover 关闭 → `gitSource` 祖先展开 → 滚动至 `top: 96px` → 焦点落在 `#body_gitSource_repoId`;未登记的 `legacyRegion` 为纯文本。

## 遗留与下一步

- 无新 finding、无 deferred;全仓 open finding 队列仍为空。
- **人工决策点**:本 PR 的分发文件改动仅是 `FactRow.vue` 一行注释重写(下游 `geist:update` 会看到 diff),若认为不值得让消费项目同步,可只合并 reference 与簿记、回退 FactRow 注释——但届时注释与 reference 仍不一致,建议整体合并。
- 09-15 遗留的「FieldItem gutter 图标与引导线重叠」已由人工裁定保持现状,后续不再作为决策点提出。
- 09-11 记录的「SidebarNav 分区 header 按钮未显式设 `touch-manipulation`」线索仍待该簇排期时核证。
- Round 4 剩余 24 个组件按依赖簇继续。
