# 2026-08-08 组件审计（复核轮，含后续人工修复）

- **基线**：`origin/main` = `4b0d9ea5c950f450a69021c469e88d0b749cff56`
- **形态**：PR #71 仍开放；planner 从同一 base 推导出完全相同的组件簇，因此继续复核本 PR，不创建重复修复 PR
- **最终 evidence headSha**：`57b42f8ca6183196a9c253f1398d1224e7116fc6`

## 最终复核结论

- **api-docs-webhook-protocol / f-b1838c3b046a**：默认 `Show ${overflow} more steps` 可访问名成立；`collapseScheduleSteps` 折叠时 overflow 恒不小于 2，英文默认不存在单复数错误路径。
- **api-docs-response-example / f-985ff00a90aa**：blank label / mediaType 逐级回落成立，宽窄两套 media 控件都获得非空标签。
- **api-docs-response-example 收敛链**：scenario、body id 与 body context watcher 统一以 pre-flush 消费每次更新的最终状态；原地重排 scenario 数组时，仍有效的 scenario/status/非首 body 均保持选中；跨更新删除仍会丢弃旧选择。
- **api-docs-request-example**：id-list watcher 能观察 reactive 原地增删；原地重排保持选择，跨更新删除后重加不复活。
- **Co-review**：`api-docs-code-block` 与 `api-docs-field-group` 本体及公开契约均未被改变。

## 为什么重新 record

两轮人工复审都改变了 RequestExample / ResponseExample、对应 tests 与 references，旧 evidence head 与 scope digest 因而失效。最终 ledger 从 canonical base 重新经官方 recorder 生成，统一绑定 `57b42f8ca6183196a9c253f1398d1224e7116fc6`；没有手改 digest 或复用旧 evidence。

## Gate

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 36 items, 49 files |
| `pnpm test:registry` | ✅ fail 0 |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ postbuild CSS 检查 8 markers |
| `pnpm test:consumer` | ✅ isolated smoke 通过 |
| `pnpm test:component` | ✅ 18 files / 234 tests |
| `pnpm audit:verify` | ✅ scope-v1 evidence 重算一致 |

## 遗留与人工边界

- PR 保持 Draft，等待人工决定是否转 Ready / 合并。
- `api-docs-site-search` 与 `foundation-term-annotation` 的 open finding、三个 legacy-v1 item 均不属于本轮修复。
