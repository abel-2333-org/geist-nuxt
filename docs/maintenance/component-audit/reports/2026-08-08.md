# 2026-08-08 组件审计(复核轮)

- **基线**:`origin/main` = `4b0d9ea5c950f450a69021c469e88d0b749cff56`(与昨日相同,main 未前进)
- **形态**:PR #71(2026-08-07 审计)仍开放未合并;今日 planner 从同一 base 推导出**完全相同的簇**(`api-docs-webhook-protocol`、`api-docs-response-example`、`api-docs-request-example`,两个 open finding 重入)。按审计契约「上一轮 audit PR 仍开放且覆盖同类 item 时优先继续/复核该 PR,不得重复创建同类修复 PR」,本轮为 **PR #71 复核轮**,在其分支上执行。
- **Rebase**:PR 分支基于 `4b0d9ea` = 当前 `origin/main`,rebase 为 no-op。

## 复核结论:三处修复全部核证通过,无新 finding

在 PR 分支头(evidence headSha `5fd038fb`)重读三个组件全文、utils、references 与新增 specs:

- **api-docs-webhook-protocol / f-b1838c3b046a**:`aria-label` 回退 `Show ${overflow} more steps` 正确;额外核证 `collapseScheduleSteps` 折叠时 `overflow = total - (max-1) ≥ 2` 恒成立(util 注释亦载明),英文默认文案不存在单复数错误路径。视觉 `+N`/`−` 与可访问名分离符合 WCAG label-in-name(符号型可见文案不受约束)。resolved 判定成立。
- **api-docs-response-example / f-985ff00a90aa**:`label?.trim()` → `langLabel(...).trim()` → `codeBodyTitle` 逐级回落,`mediaType?.trim() ||` 同规则;spec 覆盖 blank label → `'JSON'` 与 blank mediaType + blank language → `'Code example'` 两条路径。resolved 判定成立。
- **api-docs-request-example**:`watch(() => scenarios.value.map(s => s.id), …, { flush: 'sync' })` 依赖逐索引读取,reactive 原地 `splice`/`push` 均触发;`flush: 'sync'` 消除「移除→同名重加」被批处理合并导致旧选择复活的时序窗口;`localScenario === undefined` 分支覆盖初始空列表后置填充。与 ResponseExample 行为对齐,spec 镜像成立。

Co-review 两项(`api-docs-code-block`、`api-docs-field-group`)复核:功能 diff 未触及二者本体与契约,判定不变。

## Gate 重跑(今日,PR 分支头)

| 检查 | 结果 |
|---|---|
| `pnpm registry:validate` | ✅ 36 items, 49 files |
| `pnpm test:registry` | ✅ fail 0 |
| `pnpm typecheck` | ✅ |
| `pnpm build` | ✅ postbuild CSS 检查 8 markers |
| `pnpm test:consumer` | ✅ isolated-api-docs-sidebar-nav smoke 通过 |
| `pnpm test:component` | ✅ 18 files / 231 tests |
| `pnpm audit:verify` | ✅ 证据成立 16 item |

## 为什么今日不重新 record

分支 ledger 已含本簇基于同一 `baseSha`/`headSha` 的 scope-v1 证据,`audit:verify` 重算一致;recorder 要求「当前 ledger 与 base 上的 canonical ledger 一致」,在已 record 过的 PR 分支上二次 record 会 fail closed——这是设计(一个 PR 一份证据),复核轮不产生新功能提交时无需也不能重录。本报告即今日运行的完整交付。

## 遗留与建议(不变)

- **人工决策点**:评审并合并 PR #71;两个 resolved finding 的判定复核。合并前 canonical ledger 不含本簇结论,planner 会持续把同簇排为最高优先——这是设计而非缺陷。
- 仍 deferred:`api-docs-site-search`(1 open)、`foundation-term-annotation`(1 open);legacy-v1 待升级:`api-docs-enum-table`、`api-docs-field-item`、`foundation-inline-code`;未审计剩 7 item。
