# 2026-09-01 组件审计

- **Round**:3(本轮第 2 天,`roundRollover: false`,无轮末横切扫描)
- **Base main SHA**:`239b6a8276c639299e704329a9d8531252f6cb8a`
- **计划来源**:`node scripts/audit-plan.mjs --json --base <mainSha>`,无 `openFindingReentry` / `priorityBacklog` 插队
- **选簇原因**:接续上次审计簇——`api-docs-field-annotation` 直接依赖 `api-docs-field-item`(上批 item),并与 `foundation-inline-markdown`、`api-docs-webhook-protocol` 共享 `foundation-inline-code` / `foundation-inline-markdown` 依赖

## 结论一览

| item | change | status | 结论 |
|---|---|---|---|
| `api-docs-field-annotation` | none | verified | clean,无新 finding |
| `foundation-inline-markdown` | none | verified | clean,无新 finding;marked 仍为 17.0.6 |
| `api-docs-webhook-protocol` | none | verified | clean,无新 finding |

三个组件均为 round 2 已 verified 的存量件,本轮按晋升/精简/优化/重构四维复审后全部记 clean。**宁缺毋滥:未发现需要改动的依据,不制造 churn。**

## 各组件详情

### api-docs-field-annotation(FieldAnnotation + useFieldSource)

- **晋升**:无。组件归属正确——绑定 `FieldNode` + `useFieldAnchor` 领域模型,落 kit 而非 foundation 是 `references/components/overlays.md` 明文契约。
- **精简**:两处候选均为既有 wont-fix finding,维持人工判定不重开:
  - `f-31f690a2dbaa`(trailing-slash 归一化与 SidebarNav 重复,边界不一致但互不交互);
  - `f-fd7282669072`(labels spread 合并模式家族重复,抽取收益不抵跨边界间接层)。反向横扫确认该 spread 模式在 12 个分发组件中是既定惯例,与 finding 判定一致。
- **优化**:popover 内长内容 `wrap-anywhere min-w-0` 有测试锚定(annotation-forms.spec.ts:171);必填标记同时用文字(REQUIRED/CONDITIONAL)与颜色,不靠纯颜色传意;跳转后焦点落在目标行(`focus: true`),符合键盘续航契约。
- **重构**:无。双绑定(`field` 直传 / `field-ref` 经 provide/inject)、未命中降级为纯文本 + dev `console.warn`、跨页 `{page}#{path}` 编码(测试锚定双重编码行为)均与 `references/kits/api-docs/index.md` 的「FieldAnnotation 的字段引用约束」逐条一致。gallery demo 覆盖同页、嵌套自动展开、跨页、降级四态。
- **为何没改两个几乎相同的 UButton 分支**:`canJump`(@click 跳转)与 `crossPageTo`(:to 导航)语义不同,合并成动态 props 会掩盖「关闭再导航」的注释意图,属无谓 churn。

### foundation-inline-markdown(InlineMarkdown + parse.ts)

- **晋升/精简**:无。marked Lexer + 窄 VNode adapter 的架构决策在 `references/kits/api-docs/index.md` 有完整论证(不用 MDC/HTML renderer 的理由),本轮核证仍成立。
- **优化**:渲染层只输出语义元素(ProseStrong/ProseEm/ProseA/InlineCode/原生 del),无硬编码颜色;`translate="no"` 由 InlineCode 原子默认提供(上轮 co-review 收敛)。
- **重构**:无。`parse.ts` 的三层防御(协议 allowlist、`token.raw.startsWith('[')` 排除 autolink、intraword 星号降级)各自有 pinned 测试。
- **Universal claims 核证**:registry description「Safe inline markdown subset」与组件注释「Raw HTML is never rendered」由 inline-markdown.spec.ts 的注入 payload 矩阵(8 种 raw HTML、4 种 unsafe 协议)与字面保留矩阵锚定,无已知反例。
- **依赖证据**:`marked` lockfile 解析仍为 17.0.6,与既有 dependency evidence 一致,前提未失效;本次 record 显式重申该依赖绑定。

### api-docs-webhook-protocol(WebhookProtocol + FactList/FactRow + utils)

- **晋升**:无。FactRow 的簇外消费方只有 gallery 私有 recipe(webhook-reference / endpoint-reference / structured-relations 页面),`format` 判别不下沉 FactRow 的决策(#76,避免所有含 FactRow 切片连带 marked 依赖)仍然成立——第二个已采纳组件需要 rich fact 前不动。
- **精简**:反向横扫比对了 `collapseScheduleSteps`(计数阈值折叠)与 `SidebarScenarioTags` 的折叠(ResizeObserver 宽度实测 fit),两者机制本质不同(纯函数计数 vs 测量循环),共享面仅「+N chip + sr-only 真源」约定;强行抽象会把两种机制塞进一个 API,违反最简耐久原则,不记 finding。
- **优化**:a11y 契约完整——`<dl>` 语义、chips 逐个 `aria-hidden`、展开按钮可见文案即可访问名(Label in Name)、展开时 sr-only 全序列,均有组件测试锚定;上轮 resolved 的 `f-b1838c3b046a`(aria-label 回退 +N)修复仍在位(默认文案 `Show N more`)。
- **重构**:无。`change: "modified"` 的历史包袱(`code` 布尔别名)已删净,`format` 是唯一判别字段,reference 迁移段落与实现一致。
- **Universal claims 核证**:「绝不渲染空卡片或 none」由 `hasWebhookProtocolContent` 的 node 测试(空壳/空 facts/空白 summary 全排除)锚定;「渲染行为绝不由 value 内容触发」由 fact format 默认纯文本测试锚定。demo 页覆盖省略规则、ACK 三语义、无 steps、阈值 1、320px stress 五组变体。
- **为何没改 schedule 行传入又被 slot 覆盖的 `value`**:`fact.value` 同时是 FactRow 默认渲染的语义兜底,slot 覆盖是 FactRow 的公开机制,删掉反而破坏 fact 对象的完整性。

## 反向横扫记录

对今日三组件提取的特征模式在 `foundation/ kits/ app/components/` 全仓反查:

- **labels spread 合并**(`...props.labels`):12 个分发组件命中,家族惯例,已有 wont-fix finding 覆盖,不重复记录;
- **折叠 + overflow 计数**:仅 SidebarScenarioTags 相似,机制不同(见上),不构成抽取候选;
- **sr-only 序列真源**(join 后整段给 SR):WebhookProtocol 与 SidebarScenarioTags 各一处,共享面一行 `join`,无抽取价值;
- **降级 + dev 告警**(未命中 ref 降纯文本):TermAnnotation / FieldAnnotation 家族共用策略,组件注释已明文记录「保持 local、不进公共 auto-import 面」的理由,不动。

## Gate

- `pnpm registry:validate && pnpm test:registry && pnpm typecheck && pnpm build && pnpm test:consumer`:全绿
- `pnpm test:component`:全绿
- `pnpm audit:verify`:全绿(record 前后各一次)
- 分发物 U+FFFD 检查:无
- 全 clean、零代码改动,无功能提交;evidence `headSha` 即簿记提交的父提交(= base main SHA 上的干净树)

## 遗留与下一步

- 无新增 deferred finding;既有 wont-fix(FieldAnnotation ×2)维持人工判定。
- 下一批按选簇算法继续 round 3 剩余 item(remainingInRound: 22 → 19)。
